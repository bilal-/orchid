---
schema: 1
id: T014
title: Fix the stat portability that breaks every lock acquisition on Linux
status: done
archetype: feature
scaffold: false
branch: task/T014
worktree: /Users/bilal/workspace/personal/orchid-orchid-T014
run_id:
depends_on:
attempts: 3
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 140cef8c9b783a16937d135ebe81401d7f424ff5
candidate_sha: 72ed28eaa6d485745d91016ae2af732eb47d2922
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 5 — OPERATOR DECISION, and it removes the thing that has blocked you three times. Your stub fix from attempt 4 was CORRECT: modelling GNU stat -f as exit 0 with non-numeric output is right, and it stays. The remaining failure is tests/test_common.sh's RED assertion that arithmetic on that value 'must fail under set -u'. DROP THAT ASSERTION. Reason, established by the operator with direct measurement: whether the vulnerable idiom explodes, and with what status, depends on the shell and the surrounding context — the same value yields 127 under /bin/bash 3.2, 1 under zsh, and 0 inside this suite's own execution context. A test that pins the precise failure MODE of the bug is therefore unreliable by construction, and it has now cost three rework rounds without ever testing the fix. What IS deterministic, and what actually protects the codebase, is the fixed behaviour: keep and strengthen the GREEN assertions that the corrected helper REJECTS a non-numeric result before it can reach arithmetic, and REJECTS an empty result before it can reach arithmetic (the operator verified separately that an empty but SET variable evaluates to zero silently, so empty is a distinct hazard from a word, and both must be rejected). Assert the helper's OUTPUT and BEHAVIOUR, never the interpreter's reaction to malformed input. Replace the dropped assertion with a comment recording exactly why: the failure mode is shell-dependent, the fix's guarantee is not. THE REST OF THE TASK IS UNCHANGED and is the actual deliverable: apply libexec/orchid-start:162-165's result-based selection to lib/common.sh:465, libexec/orchid-doctor:439, libexec/orchid-answer:53, libexec/orchid-jobs:224 and libexec/orchid-jobs:407. MANDATORY OPERATOR HAND-OFFS: do not re-pin Formula/orchid.rb, do not chmod, do not run any verifier.
engine: claude
effort: medium
acceptance_criteria: The idiom 'mt=$(stat -f %m PATH 2>/dev/null || stat -c %Y PATH)' selects the platform on EXIT STATUS, which is wrong: GNU stat's -f is filesystem mode, so on Linux it can SUCCEED with non-numeric output, the fallback never runs, and the arithmetic that follows treats that word as a variable name — observed in CI as 'lib/common.sh: line 466: File: unbound variable' under set -u, taking down lock_acquire and cascading into the drive, gc, epoch-fencing and resume tests. The correct form already exists in this repo: libexec/orchid-start:162-165 selects on the RESULT, falling back when mt is empty or non-numeric, with a comment explaining exactly this hazard. Apply that form everywhere the raw idiom remains: lib/common.sh:465, libexec/orchid-doctor:439, libexec/orchid-answer:53, libexec/orchid-jobs:224 and libexec/orchid-jobs:407. Add a focused test that a non-numeric stat result cannot reach arithmetic. This is the same class as lesson L016: a correct pattern existed and nothing made it apply beyond the file that discovered it. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them.
verification_commands: /bin/bash tests/test_verb_lock.sh && /bin/bash tests/test_jobs.sh && /bin/bash tests/test_init_doctor.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-09T16:52:03Z
created: 2026-08-08T23:25:26Z
updated: 2026-08-09T20:38:11Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
