---
schema: 1
id: T004
title: Stop the release rehearsal passing vacuously in an extracted archive
status: done
archetype: feature
scaffold: false
branch: task/T004
worktree: /Users/bilal/workspace/personal/orchid-orchid-T004
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 0
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 9bf480ebe347702f6548510c13c767e755db5399
candidate_sha: c33301c7b79e3f36a8f0792f5519f36d304cce7b
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: tests/run.sh globs test_*.sh so tests/test_e2e_release_rehearsal.sh is part of the suite, and the suite is deliberately runnable inside an extracted release archive (tests/test_ci_release.sh skips its Git-dependent checks there by design, see its comment near line 158). In that context the rehearsal's source-checkout snapshots compare a tree that was never at risk, so they pass without proving anything — and docs/install.md line 150 prescribes running the rehearsal as a release-day step. Detect the extracted-archive context and record the source-checkout snapshot as not-tested there, or make it meaningful in that context. Do not simply delete the assertion. NOTE FOR ACCURACY: scripts/release.sh does NOT itself run the suite; the exposure is the documented release-day procedure plus the archive-runnable suite, not the release script. Add a focused test for whichever behaviour is chosen. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. THIS TASK OWNS docs/install.md's release-day section: it prescribes running the rehearsal at line 150, and this change alters what that run proves. Update that prose in the same candidate so the documented procedure and the rehearsal's actual guarantee cannot disagree. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_e2e_release_rehearsal.sh && /bin/bash tests/test_ci_release.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-20T14:45:38Z
created: 2026-08-08T23:20:13Z
updated: 2026-08-22T01:42:45Z
refused_envelopes: T004-a1-implementer.2.json T004-a1-implementer.3.json T004-a1-implementer.4.json
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

<!-- orchid:rework-brief candidate=ed38c352909e6c0257e2048df9cda3f4640145c8 superseded -->
**Superseded rework brief (candidate `ed38c352909e`) — 19 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-18T03:14:01Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.
