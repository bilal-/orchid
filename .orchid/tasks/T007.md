---
schema: 1
id: T007
title: Make the CI gate fire without each task opting in
status: done
archetype: feature
scaffold: false
branch: task/T007
worktree: /Users/bilal/workspace/personal/orchid-orchid-T007
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 9
infra_failures: 6
session_id:
implementer_engine_id: claude
base_sha: 34edca09e0913d68489b4f32a3d9c32d2dfe013a
candidate_sha: ff3dbc71716444125a7a6b00d2c83c2d8b501b64
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T007 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: scripts/ci-local.sh existed for the whole of r-001 and only two of eight tasks invoked it, because verification_commands is authored per task and a repo-wide gate added mid-run reaches only tasks written after it AND told about it. Seventeen ShellCheck findings accumulated behind a green suite, and the run-level criterion 'the configured ShellCheck gate passes' was failing on the integration branch the entire time (lesson L016). Verified still true today: grep -c ci-local over libexec/orchid-merge and runners/orchid-drive returns 0 for both. Make the gate fire without an author choosing to include it: in the merge path, in a shared verification fragment every task inherits, or in the plan-time task template. Whichever is chosen must not double-run the suite on every task for no reason — say plainly what the cost is and why it is acceptable. A task dispatched WITHOUT ci-local.sh in its own verification_commands must still be gated by it before merge; prove that with a focused test. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. GATE MUST BLOCK, NOT MERELY RUN: a candidate that fails the gate must not advance the integration ref. Running the gate and then merging anyway is the failure mode this task exists to remove, so the focused test must prove BOTH halves — that a task with no ci-local in its own verification_commands is still gated, AND that a FAILING gate leaves the integration ref exactly where it was. SEQUENCING, why this depends on T010: once the gate fires for every task, its failures become the rework path for every task, and until T010 carries exact lint locations into the brief those failures are unactionable by an implementer that cannot run the linter. Landing this first would make every subsequent task's rework unsatisfiable. RECURSION GUARD REQUIRED, second order: if the gate runs from the merge path, and tests/test_merge.sh exercises orchid merge, and orchid merge runs the gate, the suite re-enters itself. Guard the nesting explicitly — an env marker the outer invocation sets and the merge path checks — and assert the guard holds, exactly as T013 must for its own harness. This is the same trap in a different loop, so solve it the same way rather than discovering it at runtime. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_merge.sh && /bin/bash tests/test_ci_release.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T14:44:57Z
created: 2026-08-08T23:20:20Z
updated: 2026-08-29T15:23:40Z
refused_envelopes: T007-a2-implementer.json T007-a2-implementer.2.json T007-a2-implementer.3.json
attempt_budget: 10
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

<!-- orchid:rework-brief candidate=ad448c21b1bc993c54b81e0f39f80ab9fc4f9b03 superseded -->
**Superseded rework brief (candidate `ad448c21b1bc`) — 15 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-18T03:14:01Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-28T08:29:52Z):** Track 0 is merged through e8dd137b; the false delivery/attempt block is repaired. The clean idle branch was rebased to current integration. Re-derive the CI-gate task on this base; T030 supersedes the old Formula handoff, so do not add a candidate-local Formula pin.

**Operator guidance (2026-08-29T01:32:24Z):** Grant two convergence rounds for the current medium evidence-attribution defect: separate or classify successful merge-gate output so findings extraction cannot treat it as a failing verifier log, and preserve current-candidate actionable guidance.

**Operator guidance (2026-08-29T01:58:39Z):** ledger item: a persistently red repo-wide merge_gate currently takes the uncharged merging-to-rework edge forever, so T007 must add bounded candidate-attempt accounting and cap escalation for gate_failed without charging merge conflicts or conflating validation_failed; also preserve gate_failed in drive status. Current candidate evidence is 4d78013f402c.

**Operator guidance (2026-08-29T04:27:03Z):** Environment recovery, not a candidate judgment: host suspend killed four unrelated live jobs together. Your interrupted edits are preserved in operator-handoff commit 5af1de53ce2c; inspect that commit, complete the bounded gate_failed accounting and focused tests, then commit your final correction. Do not discard the preserved work.

**Operator guidance (2026-08-29T06:52:24Z):** Host sleep resumed inside an already-running drive pass, so the wrapper's pre-drive stale-heartbeat grace could not run before jobs check killed the live job. No attempt was charged. Interrupted work is preserved cleanly at operator-handoff commit 38789a01c73eec80d277f2e3a88e4f3df05d3ffb; validate and finish that tree, and do not create a candidate-local Formula pin after T030.

**Operator guidance (2026-08-29T07:20:23Z):** Ledger item: repair the two confirmed attempt-8 defects. When a self-hosted merge changes orchid.config, make the committed config live only if its pre-merge working/index bytes were safe to refresh; preserve and explicitly report any pre-existing operator config edit. Also ensure a merge failure that occurs before fresh evidence is written cannot be classified from an older <task>-merge.log: bind classification to the current invocation/candidate or invalidate the stale log before branching. Add focused self-hosted config activation/preservation and stale-log conflict regressions. Two delivery rounds are authorized; retry grants budget without consuming an attempt.
