---
schema: 1
id: T017
title: Require every gate to ship a RED case proving it detects its failure
status: done
archetype: feature
scaffold: false
branch: task/T017
worktree: /Users/bilal/workspace/personal/orchid-orchid-T017
run_id:
depends_on: T013
attempts: 3
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: c1d7550b970b9f4db952a7d9ce5cc62d77067ae7
candidate_sha: 4a10b7fb939c612877ae41bf782b265617ca5f73
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REWORK BRIEF, reconstructed by the operator because the kernel deleted the verify log while naming it (lesson L023) — you have now reworked TWICE without ever being shown why you failed, which is almost certainly why you failed twice. THIS IS ATTEMPT 3 OF 3: a further verify failure blocks this task permanently, with no verb able to recover it. Make exactly the change below and nothing else. THE ONLY FAILURE is a ShellCheck PARSE error, SC1073/SC1072, at tests/inv/test_INV-14_engine_neutrality.sh line 127. That line is: green_case "the same pattern left an assignment (backtick v=<engine>,other backtick, a config default table) alone, so the match above is detection rather than a pattern that hits every line naming an engine". The backticks are inside a DOUBLE-quoted string, so the shell reads them as command substitution and tries to parse 'v=<engine>,other' as code; '<engine>' is a redirection and the parse dies. Your prose is being executed. FIX: make that annotation a single-quoted string so nothing inside is interpreted — green_case 'the same pattern left an assignment (v=<engine>,other, a config default table) alone, ...' — or drop the backticks entirely. Do NOT add a shellcheck disable, do not restructure the test, do not touch the red_case on line 126, and change no other file. Everything else in your chain passes: tests/test_docs.sh is green and ci-local.sh reaches the ShellCheck stage cleanly. WHILE YOU ARE THERE, the operator's standing objection on this task is still open and reviewers have been told not to approve until it is met: tests/helpers.sh:108 gates the RED-case requirement with 'case $0 in */tests/inv/test_*.sh)', which ENGAGES on an absolute path but is SILENTLY SKIPPED both for a bare filename run from inside tests/inv and for 'tests/inv/test_...' from the repo root — it fails open, and your own RED case uses an absolute path so it cannot detect that. If you have already fixed that, say so explicitly in your report and name the test that proves it. MANDATORY OPERATOR HAND-OFFS: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. OPERATOR HAND-OFF COMMITS: any commit trailered 'Orchid-Handoff: operator' is the operator's, not yours.
engine: claude
effort: medium
acceptance_criteria: Across r-001 and r-002 the repeated failure was a check that reported success without having tested anything: review envelopes with an empty findings[], a probe that greps the reply for the string it fed into the prompt, rehearsal snapshots comparing a tree never at risk, doctor reporting outbound ok without reading the config its plugin requires, and an inbound line whose output was identical whether or not a gateway existed. Each was written in good faith and none could fail. Establish the rule and enforce it where it can be enforced: a check that gates anything must ship a RED case demonstrating it DETECTS the failure it exists for, and that RED case must itself be exercised by the suite. tests/test_hermetic_suite.sh is the model — it proves the vendor-free run fails when the suite is not hermetic, and records what it cannot prove as not-tested rather than as a pass. Write the rule into docs/specs/ and PROTOCOL.md where checks are specified, and add whatever mechanical enforcement is achievable (for instance a docs or lint assertion that a new tests/inv/ file carries both a RED and a GREEN case). MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. === CARRIED FROM T013's ARBITRATION, three low findings against tests/test_hermetic_suite.sh that are this task's subject exactly. (1) THE FAILURE PATH PRINTS AN EMPTY DIAGNOSTIC AND DELETES THE LOG IT NAMES — the same shape as lesson L023, reproduced inside the very harness built to prove things are proven; a gate whose failure output is destroyed cannot demonstrate that it detects its failure, so fix it here and cover it with a RED case. (2) The PATH mirror symlinks tools into a scratch directory, which can break a wrapper that resolves its own location via dirname $0 — a mirrored tool may therefore behave differently from the real one, which would make the hermetic proof itself unsound. (3) A comment and the docs credit tests/run.sh with unsetting ORCHID_ACTOR, ORCHID_REPO and ORCHID_EPOCH, but tests/helpers.sh:20 is what actually does it; correct the attribution so a future reader does not delete the real guard believing it lives elsewhere. Also fold in the finding first raised at T013's earlier review and promoted during its rework: ORCHID_HERMETIC_PROOF was a bare truthy marker, so any stray environment value made the whole proof no-op and exit 0 — verify the strict-marker fix actually shipped and carries its own RED case. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === UNRESOLVED OPERATOR OBJECTION, RAISED 2026-08-10T18:19Z, WHICH MUST BE EXPLICITLY MET BEFORE THIS TASK MAY BE APPROVED BY ANYONE. The operator arbitrated request-changes on attempt 1 after REPRODUCING this: tests/helpers.sh:108 gates the RED-case requirement with 'case $0 in */tests/inv/test_*.sh)', so the gate ENGAGES when the file is invoked by absolute path (prints 'red-cases: N demonstrated') and is SILENTLY SKIPPED both when invoked as a bare filename from inside tests/inv and as 'tests/inv/test_...' from the repo root, because the pattern needs a component before /tests/inv/. It fails OPEN, without a word. Its own RED case uses an absolute path, so the one mechanism meant to prove the gate can fail cannot detect this. TO ANY REVIEWER: do not return approve on this task until you have confirmed that enrolment no longer depends on the invocation path AND that a RED case exercises the relative and bare-filename forms. Judge 'was the operator's named objection fixed', not merely 'does this diff look reasonable'. If it was not fixed, return request-changes and say so, with at least one structured finding at medium or above - a prose-only summary with an empty findings[] does not reach the severity gate and will be read as no objection at all.
verification_commands: /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-10T23:50:41Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-11T02:08:02Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
