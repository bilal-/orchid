---
schema: 1
id: T027
title: Stop a failed launch from retrying silently forever
status: done
archetype: feature
scaffold: false
branch: task/T027
worktree: /Users/bilal/workspace/personal/orchid-orchid-T027
run_id:
depends_on: T013
attempts: 7
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: f875aee1781be8283044e0c1ed58dec290882fae
candidate_sha: 3caba9d3584aed86bd3e88da42d7d25c71c4495e
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT-8 REWORK, SUPERSEDING PRIOR REVIEW GUIDANCE: preserve the fully verified attempt-7 candidate and its honest parent-vs-candidate proof. BLOCKING: PLANNING still has the original silent-launch failure shape. Plan critiques and planning hook jobs are launched outside drive_launch, while the planning pre-pass runs jobs gc --reap-prepared before the normal reconcile/check/escalation section. An aged pid-0 manifest can therefore be deleted with no durable journal receipt and no escalation; the launcher failure disappears exactly as F29 describes. Close this path without making PLANNING permanently wedge: every physical planning launch failure must become durable, visible, and exactly-once accounted before its manifest is reaped, including the reserved plan id where no task counter exists. Add an honest RED/GREEN planning-phase proof that fails on candidate 4493cb52 and passes the fix; cover crash recovery between launch failure and accounting, and preserve the bounded reap escape. Do not broaden into the two already-recorded low ledger candidates unless required by this fix. Do not run tests, chmod, or pin Formula/orchid.rb; operator handoff follows your committed fix.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F29 (wasiyyat-schedule-c) — F23 recurring, plus a far worse dimension. A launcher that fails BEFORE the job runs is invisible through orchid drive: input_overflow (76086 bytes over a 65536 budget) made every launch fail, drive moved on and re-attempted next pass, and after 73 passes there were 73 manifests all pid 0 pgid 0 started_at 0, no logs, jobs check reporting them 'prepared' forever, orchid status showing the engine ok, and NOT ONE journal entry mentioning a failure. The escalation ladder never engages because it triggers on dead/stalled/timeout and a job that never started is none of those; the ledger never marks the engine because no envelope is ever produced. Compounding it, 'jobs gc --older-than-s 0' cannot reap them — it reaps pids already dead and pid 0 never satisfies that — so the operator deleted 74 files by hand. REQUIRED: treat a non-zero launcher exit as a job failure that is journaled and escalated; make jobs check report a pid-0 manifest with no log as never-started rather than prepared; make jobs gc reap never-launched manifests; refuse to prepare a second manifest for a (task, attempt, role, operation) that already has an unlaunched one. ALSO from the same finding: the effective pack budget was 65536 although the orchid root's own orchid.config sets pack_budget_bytes=131072 and the target repo set none — the resolved value matched neither, so check resolution order and have orchid doctor print the resolved pack budget, which has now twice been demonstrated able to wedge a run. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === STILL REPRODUCIBLE, SECOND RUN RUNNING (dogfood F41, 2026-08-11): 'orchid jobs gc --older-than-s 0' still cannot reap manifests for jobs that never launched or that died without an envelope. That operator removed them by hand again this run, using the same 'pid == 0 || ! kill -0' check they used last run. This is the same defect as F29 and it is now costing manual cleanup on every run - so the fix must cover BOTH shapes: a manifest whose pid is 0 (never launched) and a manifest whose pid is gone (died). Prove it with a RED case for each.
verification_commands: /bin/bash tests/test_jobs.sh && /bin/bash tests/test_launch.sh && /bin/bash tests/test_drive.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-26T06:22:51Z
created: 2026-08-10T01:50:57Z
updated: 2026-08-26T08:27:57Z
refused_envelopes: T027-a3-implementer.json T027-a3-implementer.2.json T027-a3-implementer.3.json
handoff_ack: 3caba9d3584aed86bd3e88da42d7d25c71c4495e
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T02:42:34Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run whatever covers what you changed and confirm each exits 0 — at minimum the tests/ files for the areas you touched, plus: shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure and blocks this task again.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery, charging the job-delivery ladder. It caught twenty-six tasks including this one, every one of them at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget is untouched by it. The fix is in flight under T040.

BUDGET, PLAINLY: infra_failures is kernel-owned and never reset, so you resume at the cap. One further delivery failure re-blocks this task. That is why step 3 matters — if you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply. Do not answer with an empty round.
