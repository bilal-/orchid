---
schema: 1
id: T025
title: Feed the previous attempt's failure back into rework
status: done
archetype: feature
scaffold: false
branch: task/T025
worktree: /Users/bilal/workspace/personal/orchid-orchid-T025
run_id:
depends_on: T013
attempts: 17
infra_failures: 5
session_id:
implementer_engine_id: claude
base_sha: feb06ac97ef4d35070d700d3598181d9dd6ccae2
candidate_sha: b6eddd40c17f1b9a0826bd7c0642459781aa9aff
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T025 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F27 (wasiyyat-schedule-c). A task failed verify three times with the SAME two assertions failing the SAME way; the defect was an order-sensitive assertSame on arrays round-tripping through a MySQL json column, and the implementer kept changing production code to satisfy it. Three attempts produced a BYTE-IDENTICAL failure signature — strong evidence the rework loop feeds back the same information and gets the same answer. REQUIRED, in value order: include the previous attempt's verify output, or a diff of it, in the rework pack, so the brief can say 'you already tried this and got exactly this'; on a second identical signature, route the rework to a different engine in the role's failover chain; and treat an unchanged signature as evidence the loop is not converging rather than as a fresh failure. Coordinate with T019 (attempt classification) — a rework that produced an identical signature is a strong candidate for not consuming an attempt, but decide that deliberately rather than by accident. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. === ROOT CAUSE PROVEN ON THIS RUN (lesson L023), use it: libexec/orchid-task:346 deletes reviews/<id>-verify.log on EVERY advance to rework, while the same call journals the reason 'verify failed: see .orchid/reviews/<id>-verify.log'. The pointer dangles the instant it is written. That is why rework arrives with nothing to act on — not because the brief forgets to mention the failure, but because the failure output is destroyed before anything can read it. r-002 T023 hit exactly this: demoted to rework with the log already gone, and re-running its verification by hand produced one actionable line the implementer never saw. The deletion is deliberate (it arms INV-11's gate so a stale log cannot satisfy a later advance), so DO NOT simply stop deleting it: capture the failing output into the rework brief, or into an attempt-scoped log that the INV-11 evidence gate does not accept, BEFORE the delete. Ship a RED case: fail a task's verification, advance it to rework, and assert the next attempt's brief actually contains the failing output. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_pack.sh && /bin/bash tests/test_drive.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-30T08:14:35Z
created: 2026-08-10T01:50:57Z
updated: 2026-08-30T09:17:05Z
refused_envelopes: T025-a4-implementer.4.json T025-a4-implementer.5.json T025-a4-implementer.6.json
attempt_budget: 19
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T11:35:42Z):** auto-blocked on ShellCheck SC2318 at lib/pack.sh:44 — 'local state=$1 task=$2 tf=$state/tasks/$task.md', where the earlier assignments have not taken effect within the same 'local', so tf is built from the outer scope. Unlike most of this run's lint strandings this is a genuine latent bug and the warning is correct, so the fix is a real fix rather than a reword: split the declaration. Everything else in the chain passes. Unblocked with the exact file, line and remedy.

**Operator guidance (2026-08-11T18:29:18Z):** stranded on the same ShellCheck SC2318 pattern it fixed last round, now at tests/test_drive.sh:1387 - 'local name=$1 dir=$WORK/eng/$name'. Instance fixed, class missed; the same shape as T017 fixing backticks in one file and leaving an identical case in another. The operator ran shellcheck across every file changed since 69f3f18 and that is the ONLY remaining SC2318 in the diff, so the brief now carries an exhaustive list rather than an instance. Auto-recovery correctly skipped this one: the verify log has zero FAIL lines (the failure is a ShellCheck warning, not an assertion), and the loop only recovers blocks whose failures are all the operator's stale pin.

**Operator guidance (2026-08-12T01:49:36Z):** STRANDED BY AN OPERATOR DEFECT, NOT ITS OWN WORK, AND THE OPERATOR CAUSED IT DIRECTLY. All 176 non-pin failures cascade from one line in the log: 'orchid: unknown command task'. libexec/orchid-task was mode 644 in this worktree — not executable — so every verb the suite invokes failed. THE OPERATOR STRIPPED IT: the script used to resolve this candidate's rebase conflict wrote each file through 'awk ... > tmp && mv tmp file', and the redirection creates the temp at the default 644 while mv replaces the original, so any executable it touched lost its mode. Restored, re-pinned and rebound. THE SAME DEFECT HIT T027 (tests/test_drive.sh), also restored. AND IT REQUIRES A CORRECTION TO THE RECORD: T019 was unblocked earlier tonight with the operator attributing its missing exec bit on runners/orchid-drive to an implementer hand-off. That attribution was WRONG — the same resolver stripped it, and the operator's own journal entry blamed the wrong party while noting the irony that T019 exists to classify exactly such failures. The irony was real but pointed the wrong way: it was not a hand-off orchid failed to recognise, it was damage the operator inflicted and then mis-read as one.

**Operator guidance (2026-08-18T03:14:03Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-28T08:29:53Z):** Track 0 is merged through e8dd137b and the false delivery/attempt block is repaired. A safe rebase was attempted and aborted on semantic failure-feedback conflicts, leaving the branch clean and unchanged. Re-derive previous-attempt feedback against current integration; omit obsolete candidate-local Formula pins per T030.

<!-- orchid:rework-brief candidate=8555a9968ceb8c55badc55137f7089bf8c5ca3e0 superseded -->
**Superseded rework brief (candidate `8555a9968ceb`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-28T14:22:21Z):** operator synchronized T025 onto Track 0; targeted rework, pack, engine adapter, failover, docs, and Part Z driver coverage pass

**Operator guidance (2026-08-29T04:47:53Z):** ledger item: the operator wrapped runners/orchid-drive in ORCHID_INFRA_MAX=20; drive propagated that policy override into candidate verification, making tests/test_drive.sh fail only at 'the bound it hit is infra_max' with expected 3, got 20. The supervisor override is removed. Candidate 2b4adbaae344 is unchanged and clean; re-run verification without an implementer cycle.

**Operator guidance (2026-08-29T05:44:25Z):** ledger item: attempt-7 reviews agree that failure feedback may be stale or vacuous under candidate changes and tight budgets. Bind rework evidence to its producing candidate, never retain a comparison while trimming away the actual failing run, and do not dispatch an implementer with an omitted or one-byte rework payload when feedback is required; pin these cases with RED coverage.

**Operator guidance (2026-08-29T20:04:10Z):** Ledger item: grant two focused convergence rounds for the attempt-8 rework-signature lifecycle, merging-path stop, evidence/audit, and truthful-reroute-journal repairs; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T20:51:10Z):** Ledger item: grant two focused convergence rounds for the attempt-9 unchanged-candidate repair set; retry grants budget without consuming an attempt. The operator has already approved implementation—do not pause for design approval.

**Operator guidance (2026-08-29T22:17:56Z):** Ledger item: repair only the current attempt-10 non-convergence stop plumbing. drive_rework_nonconvergence_stop must never invoke orchid notify directly: mint one operator-decision boundary and let the existing page-on-record path provide de-duplication and declared answers. Capture and branch on task advance blocked exactly as neighbouring arms do; if it refuses, do not claim rework -> blocked or create a boundary asserting the stop succeeded. Add RED/GREEN coverage for one-page behavior, the zero-raw-notify scan, and a refused advance leaving truthful state/output. Correct the adjacent README rework -> blocked edge while touched. Preserve the approved evidence/attribution work and run focused test_drive, test_rework, and notification-channel tests; no candidate Formula pin under T030.

**Operator guidance (2026-08-29T23:11:32Z):** Ledger item: grant T025 two focused rounds to close the successful-verification retirement of captured failure evidence; do not broaden into the four low-severity review notes unless required by the same state-lifecycle fix.

**Operator guidance (2026-08-30T03:14:33Z):** Ledger item: unblock after environment-only implementer loss during the 2026-08-29 host sleep; job j-e55-T025-a12-4af2 died without an envelope, the clean worktree remains exactly at recorded candidate 41854df6c168a08717b06164cd9610185f953269, and attempts were untouched. Resume only the recorded successful-verification retirement fix from the prior guidance.

**Operator guidance (2026-08-30T03:57:23Z):** Ledger item: grant T025 two focused convergence rounds after attempt-12 arbitration for qualified engine-ID to plugin-name resolution in identical-signature failover. Add the mismatched-directory-manifest-ID RED/GREEN case, preserve passing evidence retirement, and use only test_rework, test_pack, and targeted drive coverage; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T04:49:33Z):** Ledger item: grant T025 two focused convergence rounds after attempt-13 arbitration for retry/unblock failure capture before verify-log invalidation. Preserve all passing rework evidence, engine-ID failover, and retirement behavior; use only focused rework, pack, task, and drive coverage; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T06:11:34Z):** Grant two focused rounds for the current stale-engine attribution/reroute finding and verifier fallout.

**Operator guidance (2026-08-30T06:57:45Z):** Grant exactly two rounds after attempt-16 arbitration to repair current reviewer findings: capture candidate-bound merge-gate evidence before retry/unblock deletion, clear or restamp stale implementer_engine_id when .engine is absent, and retire the three bounded low-severity inconsistencies without regressing T034.

**Operator guidance (2026-08-30T08:14:05Z):** Grant exactly two rounds after attempt-17 arbitration for the missing pre-dispatch non-convergence guard and retry/unblock zero-dispatch RED/GREEN coverage; do not widen the already approved implementation.
