---
schema: 1
id: T030
title: Stop the per-candidate formula pin from conflicting on every merge
status: done
archetype: feature
scaffold: false
branch: task/T030
worktree: /Users/bilal/workspace/personal/orchid-orchid-T030
run_id:
depends_on: T013
attempts: 2
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 9fb0dd22f5d542b36915bae8861298eb202f81f4
candidate_sha: 987c7748a01458d928d559e06b83df2f906636b0
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REVIEW EXACT RANGE 9fb0dd22f5d542b36915bae8861298eb202f81f4..987c7748a01458d928d559e06b83df2f906636b0 INCLUDING all operator-handoff commits; Orchid-Handoff: operator means authorized operator work, not exclusion. Review the entire task range. Prior attempt-2 medium and low findings were repaired in 987c774: kernel/default-none wording, drive/handoff examples, troubleshooting guidance, and wrap-safe regression tripwires. Focus on acceptance: release-only Formula placement, default-none generic pin route, sequential sibling-merge regression, stale release-pin failure, executable mode, and correctness/non-brittleness of the repaired guidance checks.
engine: claude
effort: medium
acceptance_criteria: DEADLOCK FOUND IN r-002 (lesson L022), and it would have stalled all 27 remaining tasks. Formula/orchid.rb carries one content-derived checksum line. T004 made the freshness check part of every task's verification chain, so every candidate must re-pin it, so every candidate rewrites the SAME line to a DIFFERENT value. The first task merges; the second and every one after it conflicts in merge's stale-base rebase (libexec/orchid-merge:164), which aborts and advances the task to rework. Rework dispatches the implementer, and the implementer provably cannot resolve it: under the restricted profile (L017) it cannot run git, cannot run scripts/pin-formula.sh and cannot compute a SHA-256, so it produces no commit, the candidate is unchanged, and the same conflict recurs forever. Fix the DESIGN, not the conflict: a derived artifact must not be pinned per candidate. Take the pin out of every task's verification chain and regenerate it on the integration branch after a merge lands (or at release time), so no task branch ever touches Formula/orchid.rb and there is nothing to conflict. Keep the fixpoint property T004 established - the archive checksum must still be correct for what ships, and a stale pin must still fail the release gate - but prove it where the pin actually lives instead of on every candidate. Ship a test that reproduces the deadlock as a RED case: two task branches off one base that both re-pin, merged in sequence, must both land without a conflict and without any operator intervention. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) - do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier; state in the report what needs running. === RELATED EVIDENCE FROM r-001, found while clearing that run's leftover worktrees: scripts/pin-formula.sh is mode 755 on r-001's branch r001/task/T004 but mode 644 on both main and r-002's integration - identical blob 986dff3, mode only. r-001's operator exec-bit hand-off was performed on the branch and then never shipped, because what merged was not the branch tip. It is harmless today only because every caller invokes it as 'bash scripts/pin-formula.sh', but the tree is wrong and any release gate that checks executability would fail on it. Restore the mode as part of this task (it is the same file this task rewrites the role of), and note the general shape for the record: an operator hand-off performed late in a candidate's life can be silently dropped, which is the same failure family as lessons L017 and L021. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_ci_release.sh && /bin/bash tests/test_merge.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-28T01:11:50Z
created: 2026-08-10T02:58:00Z
updated: 2026-08-28T02:49:37Z
refused_envelopes: T030-a1-implementer.json T030-a1-implementer.2.json T030-a1-implementer.3.json
handoff_ack: 987c7748a01458d928d559e06b83df2f906636b0
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T02:42:33Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run whatever covers what you changed and confirm each exits 0 — at minimum the tests/ files for the areas you touched, plus: shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure and blocks this task again.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery, charging the job-delivery ladder. It caught twenty-six tasks including this one, every one of them at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget is untouched by it. The fix is in flight under T040.

BUDGET, PLAINLY: infra_failures is kernel-owned and never reset, so you resume at the cap. One further delivery failure re-blocks this task. That is why step 3 matters — if you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply. Do not answer with an empty round.
