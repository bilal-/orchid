---
schema: 1
id: T020
title: Anchor wallclock_budget_s per attempt instead of per task
status: done
archetype: feature
scaffold: false
branch: task/T020
worktree: /Users/bilal/workspace/personal/orchid-orchid-T020
run_id:
depends_on: T013
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 8141d32bd38e4be00360464c8bafd839d2b59e36
candidate_sha: d4e467872708ee53ca9bd84b4caa65b5b7e5a9e1
risk_tier: low
blocking_severity: high
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: libexec/orchid-task stamps started_at ONLY when it is empty, so wallclock_budget_s is anchored at a task's FIRST dispatch and thereafter measures CALENDAR time — every hour of operator downtime, debugging and overnight idling included. PROTOCOL.md describes started_at as stamped by 'task advance ... implementing', which reads as per-advance and does not match the code. Found in r-001, recorded as a ledger candidate, and it then blocked r-002's T013 after roughly twelve hours of wall time whose actual job work was a fraction of that — on a candidate that had verified clean. Anchor the budget to what it is meant to bound: the current attempt. Re-stamp started_at on each entry into implementing, or keep the task-level anchor and measure the budget from the attempt instead, whichever reads more honestly against PROTOCOL.md's wording — then make PROTOCOL.md and the code agree, since today they do not. Add a focused test that a task idle across a long wall-clock gap is not blocked, while an attempt genuinely exceeding its budget still is. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. CROSS-PROJECT CONFIRMATION AND AN EXTRA REQUIREMENT, from the webBooks orchid run's lesson L006: the same defect stalled a task there, and it recorded a detail this spec was missing — 'orchid task retry' does NOT restamp the anchor either, so an operator's own recovery verb leaves the task to re-block on the very next pass. webBooks calls it an unconvergent loop costing one implementer dispatch per cycle, and it observed budget-exceeded reported for a task whose job was actively RUNNING. So the fix must cover the recovery path, not just the dispatch path: after any operator unblock or retry, the budget must reflect the attempt about to run. Their stated invalidation is the acceptance test for this task — orchid restamps started_at on every advance into an active status, not just the first, OR wallclock_budget_s is redefined to measure time actually spent working rather than wall-clock since first dispatch. Also make 'jobs check' stop reporting budget-exceeded for a task whose job is alive, or say plainly why that is correct. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_task.sh && /bin/bash tests/test_jobs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-10T15:27:30Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-10T18:10:30Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
