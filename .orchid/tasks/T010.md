---
schema: 1
id: T010
title: Carry exact lint findings into the rework brief
status: done
archetype: feature
scaffold: false
branch: task/T010
worktree: /Users/bilal/workspace/personal/orchid-orchid-T010
run_id:
depends_on: T001 T013 T014
attempts: 6
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 69f3f183b9e1e9ce9f32a6d706edd5b6b9565e57
candidate_sha: 896775c65516bb64a21780c72b742ea5c7f56d90
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REWORK BRIEF — ONE LINE. Your worktree inspection is implemented and accepted; do not restructure it. THE DEFECT: lib/handoff.sh:102 treats a FAILED 'git status' as CLEAN, so a tree that cannot be inspected is acknowledged and resumed as though it were verified clean. An inspection that answers 'clean' when it could not look is not a safety check. Make the failure direction DIRTY: if 'git status' does not succeed, refuse, and say that the tree could not be inspected rather than that it was clean. Ship a RED case that makes the status call fail and asserts the ack is refused. Change nothing else — the pre-existing 'grep -q' SIGPIPE fail-open at libexec/orchid-task:260 that the other reviewer noted is NOT yours to fix; the operator has carried it to T016, and you were right to document and avoid it in your own code without widening your scope. MANDATORY OPERATOR HAND-OFFS: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. OPERATOR HAND-OFF COMMITS: any commit trailered 'Orchid-Handoff: operator' is the operator's, not yours.
engine: claude
effort: medium
acceptance_criteria: Lesson L017: the implementer profile denies on the command STRING, not the binary — no bash, no bash -n, no shellcheck, no writing git, and any command containing a literal dollar sign, a semicolon, parenthesis grouping or process substitution is refused. So a lint gate's findings are invisible to the actor asked to fix them. Measured cost in r-001: T005 was told twice to fix two ShellCheck findings in a file it had written; one attempt wrote a documentation paragraph and the next edited eighteen lines, neither touching either offending line, because it could not see them. Make the rework path carry the exact 'file:line: RULE: message' text into the brief automatically wherever a lint or verification failure produced it, AND record lint, pin and chmod fixes as an explicit operator hand-off in PROTOCOL.md rather than a convention nobody enforces. These are NOT alternatives: the constraints require both. Carrying exact locations makes a routed fix satisfiable when the operator does it; recording the hand-off is what stops it being routed to an actor that cannot act on it. Whichever is chosen must remove the possibility of routing an unsatisfiable instruction. Add coverage that a lint failure's exact locations reach the guidance the implementer receives. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. THE HAND-OFF NEEDS A PLACE IN THE PROCEDURE, not just a sentence in a task spec. Today the operator performs the pin, the chmod and any lint fix by habit, between the implementer's envelope reconciling and orchid verify running — nothing in PROTOCOL.md names that point, so a driver or a less careful operator would verify a candidate that was never going to pass and burn an attempt. Define the pause explicitly in THE TICK's implementing arm: after the envelope reconciles and before verification, the operator-owned mechanical steps happen and are journaled. Say what a deterministic driver should do when it reaches that point (stop at a named boundary rather than verify-and-fail), so the same rule holds whether a human or the driver is walking the task. THE BOUNDARY NEEDS A DURABLE ACKNOWLEDGEMENT AND A RESUME RULE, or it becomes an infinite loop: if the tick stops at the hand-off point, something must record that the operator actually performed the pin, the chmod or the lint fix, so a resumed session or a second driver pass does not stop at the same point forever and does not proceed as though it were done. Define where that acknowledgement lives (a journaled entry, a task field, or the boundary record itself), what clears it, and what a resume does when it finds the boundary already satisfied versus still outstanding. Cover it with a focused test that a second pass after an acknowledged hand-off proceeds, and a second pass without one stops again. BIND THE ACKNOWLEDGEMENT TO A COMMITTED candidate_sha, not to a task or a moment: an operator hand-off performed against one candidate must not be read as satisfied for a different one. State what happens when orchid merge's rebase arm moves candidate_sha underneath an acknowledged hand-off — the acknowledgement must be invalidated the same way review evidence is (INV-07), not silently inherited by the rebased tree. RESOLVE THE APPARENT CONTRADICTION EXPLICITLY: this task both routes exact lint locations into implementer guidance and declares lint fixes operator-owned. Both are intended and the spec must say why — the locations travel with the guidance so that WHOEVER acts has them and the record shows what was wrong, while the ACT of running a linter or committing its fix stays with the operator under L017. Write that distinction into PROTOCOL.md so it does not read as a contradiction to the next person. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === UNRESOLVED OPERATOR OBJECTION, RAISED 2026-08-10T21:12Z, WHICH MUST BE EXPLICITLY MET BEFORE THIS TASK MAY BE APPROVED BY ANYONE. (a) The rework brief must not re-inject a stale <id>-merge.log from a superseded candidate; evidence carried into an attempt must belong to the candidate that failed. (b) The candidate must NOT enable handoff_before_verify in this repository's orchid.config. Verified by the operator: the candidate adds 'handoff_before_verify=required' at orchid.config line 15 and it is absent from integration (c1d7550). Ship the key with a documented default and let an operator turn it on through 'orchid config commit --reason'; do not switch a new driver gate on mid-run for every remaining task. (c) kernel.md's 'schema: 1' frontmatter list and the tasks/*.md single-writer table must document handoff_ack and 'orchid task handoff'. TO ANY REVIEWER: do not return approve until all three are met; check orchid.config in the diff explicitly. If they are not met, return request-changes WITH at least one structured finding at medium or above - a prose-only summary with an empty findings[] does not reach the severity gate and reads as no objection at all.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-11T17:15:00Z
created: 2026-08-08T23:20:20Z
updated: 2026-08-11T19:05:20Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T03:00:17Z):** auto-blocked at the cap. Every failure is one of its OWN new tests asserting behaviour its implementation does not perform: the hand-off does not advance candidate_sha to the commit it produced, the acknowledgement is never written at all (comes back empty), neither move is journalled, and an acknowledged hand-off still reads 'outstanding' so the pass can never proceed — an unescapable state, the fourth instance of that class in this run and one that must not ship. Its fixture also drives two illegal transitions. Unblocked with each expected-versus-got pair named; the failures are unusually well specified because the task's own tests are good, which is why this is worth one more round rather than abandonment.

**Operator guidance (2026-08-11T06:00:52Z):** second stranding on six IDENTICAL failures, and the cause is in the fixture rather than the implementation — which is why two informed rounds made no progress. tests/test_drive.sh:1634 creates HANDOFF=$WORK/handoff and commits the operator's mechanical fix there, but never records worktree=$HANDOFF on task H010; the handoff verb derives its tree from that frontmatter field and so reads HEAD from the repo root instead. hhead never matches, the candidate is never advanced, handoff_ack is never written, and every downstream assertion fails with an empty value. The implementation itself journals, advances and acknowledges correctly, and tests/test_task.sh passes clean. Unblocked with the fixture bug named and the downstream failures flagged as probably consequential.
