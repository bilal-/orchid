---
schema: 1
id: T003
title: Stop a soft orchestrator surface from suppressing the operator blocker
status: done
archetype: feature
scaffold: false
branch: task/T003
worktree: /Users/bilal/workspace/personal/orchid-orchid-T003
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 4
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: a7616357d70b0b7c95a3ac057b6bd28397eec358
candidate_sha: a884171e667140e1ca7b38b53a1040eaf6aa0e11
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T003 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: lib/drive.sh's drive_surface_admits returns 0 unconditionally for a soft command surface (line 128, 'soft) return 0 ;;'), so every boundary kind classifies as orchestrator-resolvable under a soft adapter. The driver then skips the orchid notify blocker for it, and the pump wakes a model that cannot settle it — the exact never-told-the-human failure the brokered path exists to prevent, reintroduced on the soft path. Compounding it, only the claude adapter was given a judgment-boundary orchestrate prompt in r-001; plugins/engines/codex/run still carries the pre-v1.1 'execute ONE tick' prompt, and this repo's own role.orchestrator chain admits codex as a fallback. Make a soft surface stop implying every verb is admissible, so a boundary no admitted verb can settle raises an operator blocker; and either give the soft adapters a judgment-boundary prompt or label them as unable to serve that role. Add a focused test that a soft-surface boundary with no settling verb produces a notify blocker rather than a silent model wakeup. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. FOCUSED COVERAGE REQUIRED for the obsolete-prompt path specifically: a test must fail if a soft adapter is woken for a judgment boundary while still carrying a tick-style orchestrate prompt, so the two halves of this fix cannot drift apart — classifying the surface correctly is not enough if the prompt the woken engine receives still tells it to execute a whole tick. === ALSO, THE OTHER HALF OF THE SAME CLAIM (lesson L026), proven on this run at a cost of seven hours. runners/orchid-drive exits 16 as soon as ANY task needs an operator, and a pump that reads that as 'stop and fetch a human' halts the ENTIRE run over a decision affecting one task. That is exactly what happened here: the pump stopped at T013's review conflict at 07:32Z, the operator arbitrated within minutes but did not restart it, and twenty-eight tasks with no relationship to T013 sat idle until 14:42Z. Exit 16 means 'a decision is outstanding somewhere', not 'no further progress is possible', and drive is idempotent so a boundaried task re-reports each pass at no cost. Make the unattended pump report the boundary through the notify channel and CONTINUE dispatching every other task, halting only when it can make no progress at all - and make the exit contract say which of those two things 16 means, because the current name invites the halting reading. Ship a RED case: a run with one boundaried task and one dispatchable task must keep advancing the dispatchable one. This matters beyond convenience: a pump that stops at the first arbitrable disagreement is attended operation wearing an unattended label, which is precisely the kind of claim this run exists to make honest. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/test_pump.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-22T16:23:42Z
created: 2026-08-08T23:20:13Z
updated: 2026-08-22T20:06:03Z
refused_envelopes: T003-a3-implementer.json T003-a3-implementer.2.json T003-a3-implementer.3.json
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

<!-- orchid:rework-brief candidate=bd9accb3ab126559499387c3f3e69f14267100e7 superseded -->
**Superseded rework brief (candidate `bd9accb3ab12`) — 19 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

<!-- orchid:rework-brief candidate=f39a80c72a549ac3e8e5802eb025e7c8cf72287d superseded -->
**Superseded rework brief (candidate `f39a80c72a54`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-18T03:14:00Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-22T01:49:27Z):** SUPERSEDES ALL EARLIER NOTES. You were blocked by the clock, not by your code, and the one blocking finding is already fixed.

WHAT HAPPENED: verify passed clean (zero FAILs), you reached reviewing, and both reviews landed — claude approve, codex request-changes. Then the attempt hit wallclock_budget_s (28800s) while in reviewing and the driver blocked the task. There is no legal route from blocked back to arbitrating, so those two reviews are discarded and this round re-runs. That is orchid losing evidence it had already paid for, not a judgement about your work.

CODEX SOLE OBJECTION IS ALREADY RESOLVED: it said commit 8cf953b re-pins Formula/orchid.rb without the mandatory Orchid-Handoff: operator trailer. That was MY supervisor dropping the trailer, not you. I fixed it at the source and amended the commit, which is now 8a791fe carrying the trailer and a line naming L017. Do not spend the round on it.

DO THIS:
1. Rebase is handled for you; integration moved to a761635 (T004) so expect that base.
2. Re-run and confirm each exits 0 before committing: bash tests/test_drive.sh, bash tests/test_hooks.sh, and shellcheck --norc --shell=bash --severity=warning on every file you edited.
3. If nothing needs changing, say so in your reply and commit any rebase result — do not answer with an empty round.

<!-- orchid:rework-brief candidate=a884171e667140e1ca7b38b53a1040eaf6aa0e11 -->
**Rework brief — exact locations reported by the failing gate (candidate `a884171e6671`):**

These are the gate's own `file:line: RULE: message` lines, copied verbatim.
They are carried here because the actor asked to fix them may not be able to
run the gate that produced them (lesson L017), and an instruction whose
subject the recipient cannot see is not satisfiable. Fix these lines.
Running the linter, or committing its own fix, is operator work — see
PROTOCOL.md, "The operator hand-off".

**From `.orchid/reviews/T003-verify.log`:**

```
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 51: 41197 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 165: 41289 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 388: 46339 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 667: 51192 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 701: 51794 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 724: 51952 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 745: 52082 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 778: 52279 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_jobs.sh: line 809: 52623 Terminated: 15          sleep 100
/Users/bilal/workspace/personal/orchid-orchid-T003/tests/test_stale_root.sh: line 1200:  7211 Killed: 9               HOME="$MACHINE_HOME" ORCHID_ALLOW_STALE_ROOT=1 /bin/bash "$WORK/refresh-crash.sh" "$crashroot" "$REPO_ROOT" > /dev/null 2>&1
```

<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-22T14:38:44Z):** Blocked by the flaky streaming assertion, not your code. All thirteen verify failures are the same one: "streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)" — lesson L020, the timing-sensitive liveness check in test_engine_agy.sh that fails under machine load. It has now stranded five tasks in this run. Your attempts were charged for a flake. Rebase onto integration a761635 is handled for you; before committing run the tests covering what you touched plus shellcheck on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.
