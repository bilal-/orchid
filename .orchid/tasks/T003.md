---
schema: 1
id: T003
title: Add one-command existing-repo setup
status: done
archetype: feature
scaffold: false
branch: task/T003
worktree: /Users/bilal/workspace/personal/orchid-task-T003
run_id:
depends_on: T001 T002
attempts: 5
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 7e46b874a439fb39e9e74d2fa09997bd7559a8aa
candidate_sha: 6f96b8a8613d309d3e137132a30633cf0c3cb453
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 6 REWORK — ONE BLOCKING DEFECT, and it is a SECURITY fix that deliberately extends beyond your own file. Everything else in attempt 5 was cleared by both reviewers: the mutation boundary and refusal ordering, the three-witness planning check under the verb lock, epoch mint-vs-read, never-guess verification, the two-flag trust opt-in, committed modes 100755, INV-01 and INV-05 clean on the new file, and orchid start correctly absent from the driver's admitted-verb list and present in INV-13's forbidden list. Do not restructure any of it, do not re-pin Formula/orchid.rb, do not chmod. (1) BLOCKING, SHELL INJECTION, reproduced by the arbiter with a working payload — libexec/orchid-start:142-166's _start_lock_live builds a shell fragment from a lock's owner.json with jq and then 'eval's it, applying @sh to .hostname and .pid_start but passing .pid through 'tostring' UNQUOTED. An owner.json carrying {"pid":"0; touch /tmp/orchid-injection-proof"} produces 'pid=0; touch /tmp/orchid-injection-proof; host=..; pstart=..' and eval executes the payload; this was run and the file was created. A hostile repository can commit .orchid/runtime/lock/owner.json or .orchid/runtime/verb-lock/owner.json ('git add -f' defeats the .gitignore entry, and a clone carries the file), so any lock-taking verb pointed at that repo runs arbitrary commands as the operator, with NO unattended-trust acknowledgement needed. FIX BOTH CALL SITES, and this is deliberate scope you are authorized to take: your libexec/orchid-start:166, AND lib/common.sh:548 in verb_lock_acquire, which carries the identical idiom, is PRE-EXISTING on the merged base, is already shipped in orchid 1.0.0, and is the place you copied it from. Fixing one and leaving the other would be indefensible. DO NOT simply add @sh to .pid: remove the eval entirely at both sites and read each field into its own variable with separate jq invocations, so no repository-controlled bytes ever reach the shell as code. Preserve the existing behaviour exactly otherwise — the pid/host/pid_start liveness triple, the owner-less grace window, and the dead/foreign break semantics all stay as they are. ADD A TEST that fails on the vulnerable code: craft an owner.json whose .pid carries a shell payload, run the lock-liveness path, and assert the payload did NOT execute (e.g. the sentinel file does not exist) and that the lock is still judged correctly. Put it where it covers BOTH sites. (2) IF CHEAP, sub-medium items the reviewer noted: docs/specs/kernel.md's libexec layout block omits orchid-start (it already omits orchid-drive, orchid-trust, orchid-version and orchid-service, so fixing all of them or none is fine), and the 'verb_lock_guard $wt || orchid_die ...' arm at :848 is dead because verb_lock_acquire dies internally. Do not touch .orchid. Operator authorizes retries until success.
' "$verify_cmd" with no guard on the value, so a --verify containing a newline writes 'verify=<first line>' plus a bare orphan line; _cfg_file_get's cut then resolves the run's verification command to the first line ALONE, the run verifies with a truncated command while this verb's output prints the full value as recorded, and every later orchid start dies on the orphan line — and _start_validate_config runs BEFORE the append, so the corruption is never caught in the run that creates it. Reject a --verify value containing a newline (or any control character) at argument-parsing time, above the mutation boundary, with an actionable message. NOTE THE PRECEDENT: this is the same class of defect that destroyed .orchid/tasks/T002.md earlier in this very run — a multi-line value written into a line-oriented store with no guard — so treat 'line-oriented store, unvalidated value' as a pattern to check for, not a one-off. Add a test. (3) BLOCKING — a project that gitignores orchid.config fails BELOW the mutation boundary: orchid_commit_durable's unguarded 'git add -- orchid.config' exits 1 on an ignored path, after orchid init has run, the worktree exists and the epoch is minted, so the operator gets git's raw error with none of this file's recovery framing and every re-run reproduces it. Detect the ignored path ABOVE the boundary and refuse there with recovery, or use 'git add -f' deliberately and say why. (4) BLOCKING, actionability — the committed-config validation refusal at :238 names no recovery, unlike every other refusal in this file, even though the offending bytes live in a git blob the operator cannot open and edit. Name the fix. IF CHEAP, the reviewer's non-blocking notes: 'help' is matched as an option anywhere in argv so a requirements file named 'help' prints usage; run_status/run_id at :582-583 are dead assignments overwritten at :860-861; and a config line like 'verify = true' with spaces is invisible to config_get but only warns. Do not touch .orchid. Operator authorizes retries until success.
engine: codex
effort: medium
acceptance_criteria: Add orchid start <requirements-file> with explicit verify and worktree options. Run full preflight, validate repo config, never guess verification, require a clean existing repo, never overwrite user files or foreign worktrees, initialize safely, create/reuse only an exact integration worktree, create an epoch only for new initialization, or safely read the existing epoch without resetting fencing. On existing state, refuse if run_status is not planning, if a fresh unreleased lease indicates another session, if a verb/run lock is live, or if the caller cannot prove current-epoch ownership; never resume or steal a session implicitly. Import requirements under the validated epoch and test stale/current fencing. Print epoch, paths, state, and planning handoff. Trust stays off unless separate --ack-unattended plus nonempty --reason invokes the machine-local trust verb. Preserve lower verbs, be idempotent or give exact recovery, and update help, PROTOCOL, quickstart, and tests.
verification_commands: /bin/bash tests/test_start.sh && /bin/bash tests/test_start_fencing.sh && /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_install.sh && /bin/bash tests/test_docs.sh && /bin/bash tests/run.sh
resources: cli,onboarding
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-05T19:15:07Z
created: 2026-08-03T10:37:55Z
updated: 2026-08-06T05:16:31Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
