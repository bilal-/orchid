---
schema: 1
id: T006
title: Refuse to run orchid from a checkout that does not match HEAD
status: done
archetype: feature
scaffold: false
branch: task/T006
worktree: /Users/bilal/workspace/personal/orchid-orchid-T006
run_id:
depends_on: T013 T014
attempts: 7
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: df47066e67f5467fc32664337804016aa1acc4e3
candidate_sha: edd93f4ae1c5e43f9356e3eeb0dfe5014def396e
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REWORK BRIEF. Three serious defects closed in three rounds — the data-loss misdiagnosis, the bare-PID marker, and the index-before-checkout fail-open. Keep all of it. ONE BLOCKER, the same class one window narrower: orchid_kernel_clean checks only BEFORE update-ref, but orchid_refresh_kernel writes tracked paths AFTER — so an editor save landing in that gap is silently overwritten, and the dirty-working-tree requirement you added is violated by your own refresh. Re-check cleanliness immediately before each write, or make the window unobservable; passing a check once is not the requirement, not losing an edit is. Ship a RED case that dirties a tracked path between the check and the write and asserts the refresh refuses rather than overwrites. TWO LOWS: (1) your documented 'git reset' unstage remedy can silently hide a GENUINELY stale kernel — an operator clearing a staged-edit refusal by following your printed advice may unstage past real staleness; make the remedy distinguish the two states or warn; (2) the refresh drift loop cannot handle C-quoted paths from 'git diff --name-only', so a filename containing a space or a non-ASCII byte breaks the walk — use -z and NUL-delimited parsing. MANDATORY OPERATOR HAND-OFFS: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. OPERATOR HAND-OFF COMMITS: any commit trailered 'Orchid-Handoff: operator' is the operator's, not yours.
engine: claude
effort: medium
acceptance_criteria: bin/orchid resolves ORCHID_ROOT from its own location, so every verb, lib, runner and engine adapter executes from the WORKING TREE. orchid merge advances the integration branch with update-ref alone and never touches other checkouts, so a checkout of that branch keeps running PRE-MERGE code indefinitely while every merge appears to succeed. In r-001 this cost the whole run the benefit of its own merged work for a day: T006's review-adapter fix merged and reviewer findings stayed empty for two further rounds because the launcher kept executing the stale adapter (lesson L018). The existing warning is advisory and was ignored precisely because it is advisory. Choose ONE and implement it, recording why the others were rejected: refuse to run from a checkout whose working tree does not match HEAD; resolve ORCHID_ROOT from HEAD; or have orchid merge refresh other checkouts of the branch it advanced. Whichever is chosen must not break the legitimate case of a dirty working tree during development, and must not clobber uncommitted durable .orchid state — the r-001 journal-loss incident is exactly that hazard. Add a focused test. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. FOCUSED COVERAGE REQUIRED for the two cases most likely to be broken by the fix: an ordinary DIRTY working tree during development must still work (a mismatch check that refuses on any uncommitted edit would make the tool unusable), and uncommitted durable .orchid state must survive whatever refresh or refusal is chosen — the r-001 journal-loss incident is exactly that hazard. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_verb_lock.sh && /bin/bash tests/test_merge.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-12T07:28:04Z
created: 2026-08-08T23:20:13Z
updated: 2026-08-12T10:19:46Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T02:56:40Z):** STRANDED ON A MISSING PAIR OF QUOTES. The only failure across the entire verification chain is ShellCheck SC1010 at tests/test_stale_root.sh:457 and :559 — 'assert_eq done "$(...)"', where the bare word done is parsed as the loop keyword rather than a string. Everything else passes. This is the FOURTH time in this run that a gate has charged a task for text that merely resembles syntax: the documentation gate read the prose 'orchid creates' as a verb (T023), INV-13's scan matched 'git worktree add' inside a diagnostic string (T019), ShellCheck parsed backticks in a green_case label as command substitution (T017), and now a bare 'done' as an argument. Three of those four consumed attempts against code that was not defective. Compounding it, this task reworked with the verify log deleted by the same transition that named it (L023), so it was guessing. Unblocking with the exact fix supplied, both line numbers named. Note for the record that unblock does NOT restore any budget — attempts stays at 3 and the next verify failure re-blocks immediately — which is precisely the gap T026 is in flight to close, now demonstrated on a one-character defect.

**Operator guidance (2026-08-11T05:50:48Z):** second stranding, but this time on a GENUINE defect rather than lint: its stale-root check issues 'git -C <repo> symbolic-ref --short -q HEAD' before unattended trust has been acknowledged, violating the trust-before-touch ordering the unattended gate depends on — five assertions catch it, including 'old-Git refusal must precede every target-repository Git query'. An unacknowledged repository is hostile input and running Git in it is already touching it. The quoting fix from the previous round landed correctly, so that budget was not wasted. Unblocked with the exact call and the two most likely call sites named (lib/common.sh:193 and :289), and told explicitly not to relax the tests, which encode the invariant.

**Operator guidance (2026-08-11T09:53:50Z):** THIRD STRANDING, AND THE THIRD ONE CAUSED BY THE OPERATOR RATHER THAN THE CANDIDATE. The sole failure was again 'Formula/orchid.rb checksum is stale for the current tree'. Root cause established from the timeline, not guessed: the operator's pin commit ec054ba landed 2026-08-11T01:13:49, the implementer committed 89ca781 at 04:24:40 — three hours later — and the operator's own supervising loop then SKIPPED re-pinning this worktree on every subsequent pass because its task_job_alive guard refuses to touch a worktree whose job is still running. When the job finally exited, the driver reconciled and verified in a single pass against a pin that had been stale for four hours. The same verify log also shows the L025 mis-binding this run has documented: it recorded 'candidate: ec054ba' while the worktree HEAD was 89ca781, so the evidence named a commit that was not the tree it ran. THE OPERATOR'S GUARD IS NOW FIXED: job liveness gates the REBASE only, not the pin, because the pin commit touches exactly Formula/orchid.rb — a file the implementer profile is forbidden to touch — so re-pinning under a live job can never capture or clobber half-written work. Re-pinned at ebb9d3d, candidate_sha rebound to it, worktree clean. This task's actual work — the trust-before-touch ordering fix, which was a genuine defect and a good catch — passed everything else on its last run. Nothing in the candidate needs changing for this failure.

**Operator guidance (2026-08-11T15:34:46Z):** STRANDED AGAIN ENTIRELY ON THE OPERATOR'S HAND-OFF. The sole failure was 'Formula/orchid.rb checksum is stale for the current tree'; the worktree is clean and nothing in the candidate is defective. This is the fourth and fifth stranding this run caused by the operator's re-pinning workaround (lesson L031). The race is now understood and CANNOT be closed from outside the kernel: the driver reconciles the implementer's envelope and runs verification in the SAME pass, so an implementer commit landing after the loop's last pin is verified against a stale pin before any external process can act. Three generations of guard have narrowed the window - job-liveness, then worktree-cleanliness - and none can eliminate it, because the only safe moment to re-pin is between the job's final commit and the driver's reconcile, and nothing outside the kernel can observe that boundary. Re-pinned at 451950f9f523e91f88a417602f2c0e6c725f7992 and candidate_sha rebound. The real fix is T030, which removes the formula pin from every task's verification chain; until then this recurs and the operator absorbs it.

<!-- orchid:rework-brief candidate=41b1f2d3101d6c4d395a341cf6995ec283169fc1 superseded -->
**Superseded rework brief (candidate `41b1f2d3101d`) — 15 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-11T19:44:32Z):** fifth stranding, this one on two ShellCheck findings that are the linter failing to follow correct code rather than defects: SC1007 at tests/test_stale_root.sh:845 (an empty assignment before a line continuation) and SC2034 at :758 (a variable set as an environment prefix for a subshell that sources its consumer, so the use is invisible to the linter). The operator swept the entire diff and those two are the only instances - an exhaustive list, after T025 stranded twice on siblings of one pattern. Sixth lint stranding of the run (L029). The task's real outstanding work - the bare-PID kernel-refresh marker, the refresh window, and the doctor/status exemption question - is restated in the brief so the next round delivers it rather than only the lint fixes.

<!-- orchid:rework-brief candidate=7277e775b88864f1c5e2f73797108bd300436f29 superseded -->
**Superseded rework brief (candidate `7277e775b888`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T03:48:36Z):** STRANDED ON A VERIFY LOG THAT NO LONGER DESCRIBES THE CANDIDATE. Its log carries ShellCheck SC2178/SC2128 at libexec/orchid-plugins lines 460, 467 and 474 — but the operator checked three things and all three clear the current tree: T006's diff against integration does not touch libexec/orchid-plugins at all; integration's own copy of that file passes ci-local's exact invocation ('shellcheck --norc --shell=bash --severity=warning') with exit 0; and T006's three genuinely changed shell files — lib/common.sh, libexec/orchid-merge, tests/test_stale_root.sh — pass the same invocation with exit 0. The findings came from an earlier candidate and the implementer has since fixed them. NOTHING IN THE CURRENT TREE IS DEFECTIVE. That makes this a new failure mode worth recording: the attempt budget was spent on evidence that had gone stale relative to the work, which is the same family as lesson L025 (evidence bound to a commit that was never the one verified) seen from the operator's side rather than the kernel's — the operator reads a log, believes it describes the candidate, and is wrong. Re-pinned earlier and rebound; unblocking with the current tree verified clean.

<!-- orchid:rework-brief candidate=680b4b054f6432db3564e8f619c8f26f7c7f23f4 -->
**Rework brief — exact locations reported by the failing gate (candidate `680b4b054f64`):**

These are the gate's own `file:line: RULE: message` lines, copied verbatim.
They are carried here because the actor asked to fix them may not be able to
run the gate that produced them (lesson L017), and an instruction whose
subject the recipient cannot see is not satisfiable. Fix these lines.
Running the linter, or committing its own fix, is operator work — see
PROTOCOL.md, "The operator hand-off".

**From `.orchid/reviews/T006-verify.log`:**

```
/Users/bilal/workspace/personal/orchid-orchid-T006/libexec/orchid-plugins:460: SC2178: Variable was used as an array but is now assigned a string.
/Users/bilal/workspace/personal/orchid-orchid-T006/libexec/orchid-plugins:467: SC2178: Variable was used as an array but is now assigned a string.
/Users/bilal/workspace/personal/orchid-orchid-T006/libexec/orchid-plugins:474: SC2178: Variable was used as an array but is now assigned a string.
/Users/bilal/workspace/personal/orchid-orchid-T006/libexec/orchid-plugins:477: SC2128: Expanding an array without an index only gives the first element.
```

<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T05:12:01Z):** STALE VERIFY LOG FOR THE SECOND TIME. Its log reports ShellCheck SC2128/SC2178, but the operator verified three things: T006's changed shell files (lib/common.sh, libexec/orchid-merge) produce ZERO findings under ci-local's exact invocation; integration's full set of 140 shell files also produces zero, exit 0; and T006 does not touch libexec/orchid-plugins, the file the log names. The findings belong to an earlier candidate the implementer has already fixed. This is the second time this task has been stranded by evidence that no longer describes its tree, and the eighth stranding overall for a task whose actual defects have every time been found in REVIEW rather than verification. Recorded as further weight behind the same conclusion: a verify log is only meaningful bound to the candidate it ran against, and an operator reading one without checking that binding will brief an implementer to fix a file it never touched.
