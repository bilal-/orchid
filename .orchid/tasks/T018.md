---
schema: 1
id: T018
title: Refuse to route a step to an actor whose capabilities do not cover it
status: done
archetype: feature
scaffold: false
branch: task/T018
worktree: /Users/bilal/workspace/personal/orchid-orchid-T018
run_id:
depends_on: T010 T013
attempts: 20
infra_failures: 10
session_id:
implementer_engine_id: codex
base_sha: 8096647448aed8e6b7906cca32aff0d8e1954b04
candidate_sha: 104cc03b0de4a78a6197e5ca83a9585465cf480e
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: CURRENT OPERATOR HAND-OFF — supersedes the task's original Formula language: T030 is merged and deliberately moved Formula checksum generation to integration/release time, so no per-candidate Formula/orchid.rb pin is required or permitted. The only outstanding mechanical handoff was executable mode for the new INV-16 test; the operator committed it at 1268815e2478ef02244c56c5784a048a4270159a with trailer Orchid-Handoff: operator. Focused verification is now operator/driver-owned and must be judged from the current verify evidence, not the implementer's older report.
engine: claude
effort: medium
acceptance_criteria: T010 makes the lint hand-off explicit for one case; this generalizes it into a routing rule so the class cannot recur. Plugin manifests already declare capabilities= (shell, workspace_read, workspace_write, git, structured_text). Nothing consults them when deciding whether a STEP may be assigned to an actor, so the tick routinely asks an implementer to run a verifier, re-pin a checksum or chmod a file — all of which the shipped restricted profile refuses, denying on the command STRING rather than the binary (L017). Measured cost: T005 lost two attempt cycles being told twice to fix ShellCheck findings it could not see, and T014 lost three to a pin it could not refresh. Make the routing capability-aware: a step that requires a capability the resolved actor does not declare must not be dispatched to it — it becomes an operator hand-off with a named, journaled boundary instead. Add tests/inv/test_INV-16_capability_routing.sh with a RED case proving the invariant fails when a shell-requiring step is routed to an actor declaring no shell capability. Do not weaken any existing refusal, and do not let an actor self-declare its way past a gate — the capability is a claim by the plugin, not a grant (docs/beta-qualification.md already draws that distinction). MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_launch.sh && /bin/bash tests/test_manifest.sh && /bin/bash tests/inv/test_INV-16_capability_routing.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T20:12:30Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-29T20:30:29Z
refused_envelopes: T018-a6-implementer.json T018-a6-implementer.2.json T018-a6-implementer.3.json
attempt_budget: 22
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

<!-- orchid:rework-brief candidate=26e0e99322eb84130b0a7c153423ebc6fb9b2eb0 superseded -->
**Superseded rework brief (candidate `26e0e99322eb`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-18T03:14:02Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

<!-- orchid:rework-brief candidate=d34a0fa07ebb5226dedc3f3604ccecf876d6d520 superseded -->
**Superseded rework brief (candidate `d34a0fa07ebb`) — 21 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-22T18:42:52Z):** One failure, and it is the known flake. Your verify produced exactly ONE FAIL: "streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)" — lesson L020, the timing-sensitive liveness assertion in test_engine_agy.sh that fails under machine load. It has now stranded six tasks in this run and it is not a defect in your code. Your attempts were charged for it. Rebase onto current integration is handled for you; before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.

<!-- orchid:rework-brief candidate=0573ed568d7a23658d2bfd417b99e803a9507797 superseded -->
**Superseded rework brief (candidate `0573ed568d7a`) — 21 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-23T20:13:28Z):** One failure, and it is a member of the flaky-liveness family. Your single verify FAIL is "heartbeat stub: job log must gain at least one [hb line WHILE the adapter is still running" — a sibling of lesson L020, the same mid-run sampling pattern that fails under machine load. Your code is not implicated and the attempt was charged for a scheduling artifact. Rebase onto current integration is handled for you; before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.

<!-- orchid:rework-brief candidate=c3602227f465c516f3a1ee6661377c633379f57f superseded -->
**Superseded rework brief (candidate `c3602227f465`) — 21 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-25T02:37:47Z):** Both verify failures are the mid-run liveness flake family (lesson L020, six sites across the engine-adapter tests), not your code. The attempt was charged for a scheduling artifact. T019 owns the fix. Rebase is handled for you; before committing run the tests covering what you touched plus shellcheck on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.

**Operator guidance (2026-08-25T14:13:48Z):** Unblocking so the run can move. This task was blocked by the false-block surface this run has been documenting — a mid-run liveness flake (six sites, L020), a wallclock budget that counts time waiting on reviewers (L042), or an infra ladder charged for environment faults (L039). None of those is a judgement about your code. Separately, an engine was excluded for days because its envelopes were never drained rather than because it failed (F35), so review slots queued behind one engine and independence could not be proven. That is fixed: the supervisor now drains before every pass. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0; if nothing needs changing say so in your reply rather than answering with an empty round.

**Operator guidance (2026-08-28T13:39:40Z):** Operator rebased the preserved candidate onto integration e8dd137, resolved the T027/T018 exit-code collision by retaining 18 for the retryable orphan wait and using 19 for capability-routing refusal, and updated the INV-16 fixture for T027 dispatch ownership. Focused verification and warning-level ShellCheck pass at c5d001ee4184.

**Operator guidance (2026-08-29T01:31:04Z):** Grant two convergence rounds for the current candidate's two genuine review findings: capability routing must produce a journaled operator handoff rather than exiting 14 before the check when an implementer lacks structured_text, and the implement operation's kernel-owned requirement row must include shell and git because delivery requires a commit. Ignore the review's Formula sentence: T030 supersedes it and current focused verification passed.

**Operator guidance (2026-08-29T02:04:33Z):** ledger item: a reviewer-slot capability handoff that names only role.reviewer or review.<tier> is a dead-end because the current attempt's pinned review plan ignores live config changes; the operator-facing remedy must also name the supported orchid jobs review-plan <task> --repin step and prove it clears the slot. Current candidate evidence is cab64e304411.

**Operator guidance (2026-08-29T05:10:05Z):** ledger item: attempt-10 normal-chain review confirms resolve_role_available can return exit 14 for a permanent capability/role mismatch before prepare_capability_gate can return exit 19; drive then waits as though only the ledger window were closed. Make permanent capability refusal reach the journaled operator-handoff path and add normal-chain coverage; retain the low dependency/comment corrections.

**Operator guidance (2026-08-29T06:55:54Z):** Suspend-inflated elapsed wall time crossed timeout_minutes even though the heartbeat was current, so jobs check killed the implementer after resume. No attempt was charged. The normal-chain capability repair is preserved cleanly at operator-handoff commit 4307fcf7b3338cfadf9f52f9903e5959d6e2d09f; validate and finish it, retaining the requested end-to-end RED case.

**Operator guidance (2026-08-29T07:40:30Z):** Ledger item: extend attempt 11's chain classifier to the actual orchestrate wake. The pump/tick pre-wake path must distinguish a transient ledger/capsuite wait from an all-entry shell/git shortfall, and the permanent case must create one actionable journaled operator hand-off instead of exit-14 polling forever. Make chain selection operation-aware so an incapable primary cannot shadow a capable proven fallback, and emit only the final capability refusal when converting 14 to 19. Add end-to-end pump/orchestrate RED and GREEN cases. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T14:39:54Z):** Ledger item: grant two convergence rounds after attempt 13's deterministic INV-16 documentation-tripwire failure. The enforced-row extractor reads from 'WHERE EACH ROW IS ENFORCED' through the first bare comment separator; the new orchestrate subsection inserted a bare '#' before the later mechanical/hook sentences, so those priced rows are outside the guarded paragraph. Keep all seven backticked step names inside the extracted enforcement block (or make the test delimit that block explicitly), then rerun test_INV-16 plus launch/manifest. Preserve the integration-based wake-path repair and do not generate a candidate-local Formula pin.

**Operator guidance (2026-08-29T15:15:49Z):** Ledger item: targeted INV-16 reproduction on candidate 4af116ed5d4731f5d2f9b79fb6030b97a1be2ed6 proves the original enforcement-block defect is fixed and reaches 20 RED plus 25 GREEN cases, but seven deterministic fixture assertions remain. Update the three review-plan expectations to T012's pinned schema: revshort includes worktree and test/revshort, live revfull includes worktree, and repinned revfull includes worktree and test/revfull. For the rate-limited orchestrator case, inspect the captured current-pass output: if a new capability handoff is really emitted, fix that product path; if INV-16 is only stale notification text from the earlier permanent-case record, replace the blanket substring check with before/after handoff, blocker, boundary, or outbox evidence while retaining proof that no new handoff occurs. In the surface-prediction section, do not install a second built-in orchestrator.role beside the repository descriptor because INV-10 correctly refuses that duplicate; isolate the lower-requirement descriptor in a non-duplicating role root or exercise an equivalent legal custom-role surface path while preserving coverage that drive_orchestrator_surface reports the actual capable winner and its soft-label twin. Rerun only test_INV-16, then launch and manifest; preserve the wake-path implementation, the T012 rebase union, and no candidate-local Formula pin. Three convergence rounds are granted without consuming an attempt.

**Operator guidance (2026-08-29T16:34:47Z):** ledger item: grant two focused convergence rounds after the attempt-16 compound-step validation bypass; this is substantive gate feedback.

**Operator guidance (2026-08-29T17:22:57Z):** Ledger item: grant one focused round beyond the fully spent 18/18 budget for the current-candidate direct-path operator-handoff finding just arbitrated. T026 guarantees the grant changes only the cap and spends no attempt; limit the round to journaled/boundary coverage for direct planning critique and tick routing refusals without reworking the approved capability matrix.

**Operator guidance (2026-08-29T18:06:56Z):** Ledger item: focused convergence round for candidate 213759fd4284eacf41a1b7455130412aff8d5608. Change only blocker/journal de-duplication so one unresolved capability incident emits once across repeated passes, but after the incident is demonstrably resolved the same later shortfall emits a fresh blocker and journal record. Pin both phases in one direct orchestrator-capability regression; retain existing driver, planning-critique, and tick coverage. No candidate Formula pin under T030.

**Operator guidance (2026-08-29T19:50:02Z):** Ledger item: grant two focused convergence rounds for the attempt-20 missing-question blocker de-dup defect and journal-before-boundary liveness cleanup; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T20:10:11Z):** Ledger item: unblock after an environment-only implementer failure. Attempt-21 Claude returned the provider session-limit condition before doing work; Claude is durably marked failing, so the next normal role-chain dispatch must fail over to healthy Codex. Preserve the current missing-question blocker de-dup and journal-before-boundary guidance; this unblock grants and consumes no attempt budget. Ledger candidate: an environment failure that already disables an engine should not strand every affected task behind a separate operator unblock.
