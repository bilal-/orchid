---
schema: 1
id: T022
title: Refuse to advance on an ok envelope that produced no candidate
status: done
archetype: feature
scaffold: false
branch: task/T022
worktree: /Users/bilal/workspace/personal/orchid-orchid-T022
run_id:
depends_on: T013
attempts: 6
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 416fcc9a9c24a9dd6ca5ab3fc12c175ba36a9ce6
candidate_sha: 7ed2572b7dbe87010bb724ff45a118e58530365f
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REWORK BRIEF — SHORT ROUND. THE OPERATOR'S OBJECTION IS CLEARED: the operator ran tests/test_drive.sh on your candidate 0f29bc4 — rc=0, zero failures, Part L intact. That objection was the operator's mistake to begin with: it demanded a measurement only an operator can take, and you were never able to satisfy it. It is off your plate. YOUR BEHAVIOUR IS APPROVED BY BOTH REVIEWERS — an unchanged-HEAD ok delivery is durably refused without advancing or consuming an attempt. TWO SMALL THINGS REMAIN. (1) drive_delivery_is_noop reads HEAD ONLY, so a dispatch that edited files but never committed them is journalled as 'delivered nothing', and its relaunch then inherits that dirty worktree. A dispatch that wrote real work and failed to commit is not the same as one that did nothing — distinguish them, say which happened, and do not hand a relaunch a tree it did not create. (2) docs/specs/kernel.md's transition table now states an implementing->testing precondition that 'orchid task advance' neither checks nor CAN check, because the driver overwrites candidate_sha before advancing. Either move the check somewhere that can enforce it or reword the table to describe what is actually guaranteed — do not leave the spec claiming a guarantee no verb provides. Also: PROTOCOL.md's implementing arm still needs the repair from last round if you have not done it. MANDATORY OPERATOR HAND-OFFS: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier.
engine: claude
effort: medium
acceptance_criteria: CROSS-PROJECT FINDING from the webBooks orchid run (its lesson L004), independently confirmed in this repository. An implement dispatch can return an ok envelope whose summary is COMMENTARY — findings restated, sources listed — with ZERO edits on disk: worktree HEAD unchanged, tree clean. webBooks observed it four times on one task's rework (j-e6-a3-9c63, j-e7-a3-f982, j-e8-a3-ae77, j-e10-a4-6b62). This run saw the same shape twice on T005, where one attempt added a documentation paragraph and the next edited eighteen lines while touching neither line it was asked to fix. AN OK ENVELOPE IS NOT EVIDENCE THAT WORK HAPPENED. Before advancing implementing -> testing, compare the worktree HEAD against the task's existing candidate_sha: if it has not moved, that is a job-delivery failure and belongs on the escalation ladder, not in verification. Each false advance costs a full verify and review round re-proving an already-arbitrated defect, and it consumes a rework attempt. Implement the check in the tick and in runners/orchid-drive, journal the refusal with both SHAs, and add a focused test that a no-op dispatch escalates instead of advancing. Note the interaction with T019: this failure is a job-delivery fault, so it must not be charged to the candidate's attempt budget either. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === OBSERVED LIVE TWICE ON r-002, use it: an implementer returning status=ok with NO new commit is currently treated as a successful round. runners/orchid-drive:634 reads the worktree HEAD, finds it unchanged, records it as the candidate and advances to testing, where verification re-runs against the identical tree and fails identically — one attempt consumed for nothing. T031 did this at attempt 2 of 3 against a one-line ShellCheck fix it had been given the exact file and line for, leaving it one failure from a block with no supported recovery; T013 did the same earlier. The envelope must not be accepted as ok when it produced no candidate: either refuse it (and say so, naming that no commit was made), or route it somewhere that does not charge an implementation attempt — an implementer that changed nothing has not made an attempt at the work. Ship a RED case: an ok envelope with an unchanged HEAD must not advance the task to testing. === UNRESOLVED OPERATOR OBJECTION, RAISED 2026-08-10T18:53Z, WHICH MUST BE EXPLICITLY MET BEFORE THIS TASK MAY BE APPROVED BY ANYONE. Measured by the operator, not inferred: tests/test_drive.sh exits 0 with ZERO failures on the integration branch, and fails FIVE times on this task's candidate, every failure inside T001's 'Part L' escalation-ladder accounting - including 'nor spend a second rung on the failure it already counted (expected 1, got 2)'. Cause: this task's no-op-delivery refusal charges infra_failures and relaunches the implementer, which is the same ladder T001 merged accounting for, so one event is counted twice. TO ANY REVIEWER: do not return approve until tests/test_drive.sh passes with zero failures on this candidate AND every assertion in T001's Part L is unchanged. If Part L was edited or weakened to make this task's Part M pass, that is a rejection, not a fix - say so with a structured finding at medium or above, because a prose-only summary with an empty findings[] does not reach the severity gate and will be read as no objection at all.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/test_jobs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-12T10:54:22Z
created: 2026-08-10T01:48:06Z
updated: 2026-08-12T18:29:06Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T04:35:34Z):** auto-blocked at the cap on a single ShellCheck warning: SC2034 at tests/test_drive.sh:1466, NDRIVE_RC captured from the driver's exit status and never read. Everything else in the chain passes. Briefed to ASSERT on it rather than delete it — this is the no-op-delivery case and the driver's exit status is the direct evidence of what it decided, so the fix closes a real gap as well as the warning. Fourth task tonight to exhaust its budget on a one-line lint issue, after T006 (a bare 'done' parsed as a keyword), T031 (an unused moved_err) and T017 (backticks in an annotation). None of the four was a defect in the behaviour under test.

**Operator guidance (2026-08-11T17:17:14Z):** one genuine failure, correctly left for the operator by the loop's auto-recovery gate (it declines any task whose verify log carries a non-pin failure). The failure is T022's own test for the refusal-persistence fix the operator required: the third refused envelope, N010-a1-implementer.3.json, is missing from the refused set, so a third ok-with-no-candidate envelope remains selectable and the re-entry the task just closed reopens. Marking is implemented; it does not cover every sibling. Unblocked with that named.

<!-- orchid:rework-brief candidate=bd35c5a99e9146749f0cb2375c75010b1663eda6 superseded -->
**Superseded rework brief (candidate `bd35c5a99e91`) — 17 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T00:05:45Z):** four genuine failures, all in its own gc hold-back feature: the manifest a pending envelope needs is being reaped rather than held, the 'gc-pending <id>' notice is never emitted, the reap-once-drained path does not fire, and a drained manifest is quarantined as .reason-gc-dead instead of reaped. The auto-recovery gate correctly declined this one (four non-pin failures). Unblocked with each assertion named and the drained-versus-pending paths flagged as likely collapsing into the same branch.
