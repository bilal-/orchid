---
schema: 1
id: T028
title: Split depends_on on commas and reject unknown ids
status: done
archetype: feature
scaffold: false
branch: task/T028
worktree: /Users/bilal/workspace/personal/orchid-orchid-T028
run_id:
depends_on: T013
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 6cef02391b1f1cddde6a9d74819fc3a7681654c8
candidate_sha: 74f4b3f5ecd03d45b837a727b76b5218f6c6b55d
risk_tier: low
blocking_severity: high
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F30 (wasiyyat-schedule-c), confirmed live here at lib/schedule.sh:126. 'for d in $deps' splits on WHITESPACE only, so depends_on 'T002,T003' is a single token; fm_get looks for .orchid/tasks/T002,T003.md, finds nothing, never equals done, and the dependency can NEVER be satisfied. 'orchid task set T004 depends_on T002,T003' accepts it silently. What makes it dangerous is the rendered predicate: the unmet ids join back into 'waiting-deps (T002,T003)', byte-identical to a correct two-dependency wait, so it reads right every time — that report's operator stared at it for hours and only caught it statistically (every task that dispatched had exactly one dependency). Split on commas as well as whitespace, AND reject at write time a depends_on id with no corresponding task file, which catches the whole class hours before it can deadlock. Document the separator in the task template, which currently ships depends_on empty with no format hint. Add focused tests for a comma-separated value, an unknown id, and the rendered message. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_schedule.sh && /bin/bash tests/test_task.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-11T08:09:21Z
created: 2026-08-10T01:50:57Z
updated: 2026-08-11T10:58:18Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
