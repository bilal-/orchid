---
schema: 1
id: T016
title: Prove no enforcement gate can be skipped by omission
status: done
archetype: feature
scaffold: false
branch: task/T016
worktree: /Users/bilal/workspace/personal/orchid-orchid-T016
run_id:
depends_on: T007 T013
attempts: 18
infra_failures: 4
session_id:
implementer_engine_id: claude
base_sha: e3db45be8095e3c9e8f73fdef9db279d937c8efd
candidate_sha: a926d3e4f66661571021f6ba1f08771d16454526
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: r-001's single most expensive defect was a gate that existed and that nothing invoked: scripts/ci-local.sh ran in two of eight tasks, seventeen ShellCheck findings accumulated behind a green suite, and the run-level criterion 'the ShellCheck gate passes' was false on the integration branch for the whole run (L016). T007 puts the gate somewhere unavoidable; this task makes that property CHECKABLE so it cannot regress. Add tests/inv/test_INV-15_no_optional_gate.sh asserting that every enforcement gate the repository defines is reachable on a path taken regardless of any per-task opt-in — concretely, that a task whose verification_commands omits the gate is still gated before its ref can advance. Derive the gate list rather than hardcoding it, in the spirit of INV-14's discovery scan, so a gate added tomorrow is covered without anyone remembering to extend a list. Include a RED case proving the invariant fails against a deliberately opt-in gate. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === CARRIED FROM T017's ARBITRATION 2026-08-11T01:05Z, three findings, all yours: (1) THE OMISSION BYPASS, which is this task's literal subject, found by codex-review on T017's candidate — a new tests/inv script can omit tests/helpers.sh entirely and still pass the TEXT linter with unreachable RED/GREEN calls, so it is enrolled on paper and enforced nowhere at runtime. A gate satisfied by text that never executes is the defect this task exists to eliminate; make enrolment prove a case actually RAN, and ship a RED case that adds such a script and asserts it is refused. (2) INV-05's green_case label runs backticked prose as a command substitution, mangling the recorded label — the same defect the operator diagnosed at tests/inv/test_INV-14_engine_neutrality.sh line 127 and briefed a fix for; it survives in a second file, so fix it there and check every remaining green_case and red_case label for the same shape. (3) INV-13's RED and GREEN labels are INVERTED, and its probe exercises code_of rather than the gate's own scan — so that RED case does not demonstrate the gate detecting its own failure, it demonstrates a helper working. Correct the labels and point the probe at the scan. Note the pattern across all three: a proof that is textual, mislabelled, or aimed at the wrong function is indistinguishable from a real one in the record, which is exactly why this task and T017 both exist. === CARRIED FROM T010's ARBITRATION: the pre-existing INV-04 scan at libexec/orchid-task:260 still uses the 'grep -q' SIGPIPE fail-open pattern — grep exits at its first match and SIGPIPEs the producer, and under 'set -o pipefail' the pipeline reports the producer's kill-by-signal status, so a MATCH can be read as a failure and the scan silently passes. r-002's T010 documented and avoided that exact pattern in its own new hand-off re-scan at line 755 while correctly leaving the older instance alone. Fix the pre-existing one, and sweep for the same shape everywhere a gate pipes into 'grep -q': this is an enforcement gate that can be skipped by an accident of process scheduling, which is this task's subject. Ship a RED case that proves the scan still detects a real violation after the fix. === SECOND INSTANCE OF THE SAME CLASS, FOUND ON THIS RUN'S OWN INTEGRATION BRANCH, AND THIS TASK NOW OWNS BOTH THE FIX AND THE INVARIANT (lesson L036). r-001's defect was a gate nothing invoked; this one is a gate that WAS invoked, ran honestly, and was blind. Commit 416fcc9 checked out on branch orchid/integration fails tests/test_unattended_trust.sh with five FAILs and tests/test_hermetic_suite.sh with six; the IDENTICAL commit on any other branch name passes all 67 files with zero failures. I verified the extent: those two files are one defect, the hermetic suite being the wrapper that re-runs the suite and re-reports the same five. THE CAUSE is lib/common.sh:444-465, a stale-ORCHID_ROOT guard added by T006 that answers its first condition from file reads alone and returns early for every root NOT parked on the configured integration_branch — its own comment lists 'an install prefix, a development checkout, a task worktree, every root in an ordinary run' as exiting without a subprocess. Only when the root IS on that branch does it run 'git -C $root diff --cached --name-only HEAD', which is precisely the call the unattended-trust contract forbids before an acknowledgement is found. T006's own design note had said a source-time guard may not spawn git. WHY EVERY GATE PASSED IT, which is the part this task exists to make impossible: T006's verify ran the file and it emitted nothing, and nothing IS what green looks like for it (confirmed — a passing run prints zero lines and exits 0). The MERGE revalidation then re-ran the entire suite on the merged tree and reported CI PASS with zero FAILs. Neither run was skipped, stale or dishonest. Re-running either any number of times would never catch it, because a task worktree and orchid-merge's temp worktree are BY CONSTRUCTION never on the integration branch — the one dimension the guard branches on. A revalidation environment that differs from the deployed environment in the dimension under test converts a real failure into a silent pass. DELIVER TWO THINGS. (1) THE FIX: make the guard satisfy the unattended-trust contract, so the integration branch is green. Do not fix it by narrowing the test or by special-casing the branch name; the guard must not issue a target-repository Git query before an acknowledgement is found, which is what T006's own note anticipated. (2) THE INVARIANT: extend this task's gate-reachability check so it also fails when a gate is reachable but BLIND — at minimum, one test that CONSTRUCTS the integration-branch condition rather than inheriting it from the ambient environment, because the ambient environment of every gate orchid runs is the one where that condition is false. Per lesson L034 pin both edges: a case that must be caught and a case that must not fire, so this does not swing into failing on every checkout. NOTE THE WIDENING HONESTLY in your reply — this task was scoped to omission (a gate nothing calls) and now also covers vacuity (a gate that runs and cannot see). If you judge the two genuinely need separating, say so with your reasoning rather than silently doing half.
verification_commands: /bin/bash tests/inv/test_INV-15_no_optional_gate.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-30T10:43:39Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-30T11:33:32Z
attempt_budget: 20
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-29T17:24:37Z):** Ledger item: grant one focused round beyond the fully spent 3/3 budget for the current-candidate executable merge proof, pre-write pump gate, and invariant-test early-exit findings just arbitrated. T026 guarantees this raises only the cap and consumes no attempt; do not widen beyond those named proofs and mechanical matcher repairs.

**Operator guidance (2026-08-29T18:05:12Z):** Ledger item: focused convergence round for candidate 77f048c0439a944e928bcc39f6f1b5377c53adf8. Fix only: (1) orchid-trust must fire the stale-root gate after its authorization decision and before any durable record write, with no-write/current-root twins; (2) ci_local_late_sections must classify executable comment-stripped code, with a comment-only tests/run.sh fixture that remains static and is caught below the cut; (3) invariant enrollment must require observable runtime RED/GREEN evidence, so an unreachable or conditional helpers source fails. Preserve the passing merge-ref, pump, and early-exit work. Run targeted INV-15 plus stale-root/trust tests; no candidate Formula pin under T030.

**Operator guidance (2026-08-29T19:44:50Z):** Ledger item: grant two focused convergence rounds for the attempt-5 trust-show stale-root bypass and doctor gate ordering; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T19:46:27Z):** Ledger item: focused operator verification of rebased head 146571762ba01fbd9f8763b1865a195389329f3d exposed a second gate-honesty defect at tests/inv/test_INV-15_no_optional_gate.sh:1426: backticked words show and revoke inside the double-quoted not_tested prose execute as command substitutions, print command-not-found, and the invariant still exits 0. Replace command-substitution punctuation with inert quoting and pin that the proof emits no shell errors; this is the same carried INV-05 class, not a style nit. Also remove the prose's trust-show exemption per the current reviewers. This guidance grants no additional budget and consumes no attempt.

**Operator guidance (2026-08-29T20:10:11Z):** Ledger item: unblock after an environment-only implementer failure. Both attempt-6 Claude envelopes contain only the provider session-limit message and no candidate change; Claude is now durably marked failing after four consecutive failures, so the next normal role-chain dispatch must fail over to healthy Codex. Preserve the existing attempt-6 trust-show/backtick guidance; this unblock grants and consumes no attempt budget. Ledger candidate: environment-classified engine failures can still drive a task over infra_max and require manual unblock after the resolver has already excluded that engine.

**Operator guidance (2026-08-29T20:31:26Z):** Ledger item: grant two focused convergence rounds for the attempt-6 trust-lookup/stale-root ordering proof and non-exempt documentation repair; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T21:32:46Z):** Ledger item: grant two focused convergence rounds for the attempt-7 authorization-order and documentation repair; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T22:06:19Z):** Ledger item: repair only the current attempt-8 self-host and entry-universe gaps. Add executable RED/GREEN coverage where ORCHID_REPO and ORCHID_ROOT are the same stale integration checkout with no acknowledgement, proving doctor performs no target-repository Git before denial while still never emitting an unchecked diagnosis; do not satisfy this with another line-order scan. Make scripts/beta-qualify.sh explicitly fire the armed stale-root gate before parsing/running qualification work, and derive INV-15's entry set across scripts and shipped engine adapters as well as bin/libexec/runners so the invariant cannot share the implementation's blind spot. Preserve direct trust behavior and keep verification targeted to INV-15, unattended trust, stale-root, and beta-qualification coverage; no candidate Formula pin under T030.

**Operator guidance (2026-08-29T22:48:38Z):** Ledger item: INV-15 targeted proof failed because GIT_TRACE writes to stderr and the production self-host Git probes intentionally redirect stderr, so the test cannot observe the correctly refused probes. Repair only the proof instrumentation: use a trace-file or controlled git executable side effect that survives stderr suppression, retain explicit ordering evidence that stale-root refusal occurs before repository inspection, and do not weaken production stderr silence or broaden the suite. Re-run syntax, diff hygiene, ShellCheck, INV-15, and test_unattended_trust.

**Operator guidance (2026-08-30T03:14:28Z):** Ledger item: unblock after environment-only reviewer loss during the 2026-08-29 host sleep; job j-e55-T016-a10-f35f died without an envelope, while attempts and candidate 59d67826edf2389c591ffbfc93501513a37064a2 were untouched. The surviving current codex review identifies a real self-host trust-show gap: libexec/orchid-trust runs orchid_root_stale_gate before unattended_trust_show, so when ORCHID_ROOT equals the unacknowledged target it spends target Git before the denial. Extend the executable INV-15 proof to self-hosted trust show, arrange the machine-local denial before any target query without allowing a stale report, and preserve the repaired trace-file instrumentation.

**Operator guidance (2026-08-30T03:56:38Z):** Ledger item: grant T016 two focused convergence rounds after attempt-10 arbitration for the top-level enrollment-trap proof, self-host documentation correction, and trust/service help-path gate consistency. Preserve the verified trust-show lookup-before-target-Git ordering; run INV-15 and only directly affected trust, service, stale-root, and unattended-trust tests; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T04:47:26Z):** Ledger item: grant T016 two focused convergence rounds after attempt-11 arbitration for self-host revoke ordering, trust-arm coverage, pump/tick early-exit gate reach, and the remaining self-host documentation falsehood. Preserve all passing INV-15 work and use only directly affected tests; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T05:36:56Z):** Grant two focused rounds for the current paired-review findings and verifier fallout.

**Operator guidance (2026-08-30T06:21:03Z):** Grant two focused rounds for the current install-gate reachability and pre-ack stale-root findings and verifier fallout.

**Operator guidance (2026-08-30T07:05:14Z):** Grant exactly two rounds after attempt-14 arbitration to make INV-15's family inventory genuinely exhaustive, prove plain status decision-before-fire behavior, and align the two overbroad documentation claims.

**Operator guidance (2026-08-30T08:11:46Z):** Grant exactly two rounds after attempt-15 arbitration for the universal enforcement-enrollment contract, fifth-family RED/GREEN proof, and contradictory not_tested cleanup; preserve all already verified route-specific work.

**Operator guidance (2026-08-30T08:58:58Z):** Ledger item: grant exactly two focused convergence rounds after attempt-16 arbitration for executable reachability classification in ci_local_late_sections and its unreachable-call RED/live-call GREEN proof. Preserve every already approved INV-15 route and use only the task-declared INV-15 verifier; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T09:51:38Z):** Ledger item: grant exactly two focused convergence rounds after attempt-17 arbitration to make enrolled-proof evidence originate from a recorder call that actually ran, not from a replaceable helpers EXIT trap or a reached source line. Preserve all approved INV-15 work and use only the task-declared INV-15 verifier; no candidate Formula pin under T030.

**Operator guidance (2026-08-30T10:43:12Z):** Ledger item: grant exactly two focused convergence rounds after attempt-18 arbitration, limited to the two current-candidate findings. (1) For every off-path lib/common.sh loader that must fire orchid_root_stale_gate explicitly, prove the firing call is actually reached rather than merely present in text; add RED fixtures for a call hidden in an uninvoked function and below an unconditional exit, plus reachable twins. (2) Mechanically eliminate the remaining top-level tests/*.sh producer-to-early-exit grep -q fail-open assertions and extend INV-15's derived sweep so those assertion gates cannot recur; preserve intentional full-consumption or status-discarding cases and name exclusions precisely. Preserve all previously approved kernel, receipt, ordering, and constructed-environment work. Run only T016's declared INV-15 verifier; no candidate Formula pin under T030.
