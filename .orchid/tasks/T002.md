---
schema: 1
id: T002
title: Add deterministic routine tick driver
status: pending
archetype: feature
scaffold: false
branch: task/T002
worktree:
run_id:
depends_on: T001
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id:
base_sha:
candidate_sha:
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: codex
effort: medium
acceptance_criteria: Add a deterministic one-pass driver covering lease refresh, reconcile-check-gc ordering, safe dispatch, implementer reconciliation, verification, reviewer launch/reconciliation, unanimous structured approval, one serialized merge, status generation, and final lease refresh. Decisions use structured fields and exit codes only. Every durable or cross-process .orchid mutation, including boundary set/clear, goes through a named verb with single-writer ownership; status generation calls orchid status --html. Add orchid run boundary set|clear|show with schema and dedicated exit code, and orchid task arbitrate as the sole explicit judgment-result verb. Non-overlapping truth table: missing/malformed/non-ok/stale/incomplete evidence -> boundary; when every required review is valid/current, unanimous scope-complete approvals -> deterministic approve through task arbitrate; any request-changes verdict, any finding at or above blocking_severity, mixed verdicts, or non-scope-complete review -> boundary with no transition. Planning, blocked tasks, required-hook failure, and operator-only decisions are boundaries. Add a judgment-only argument-validating broker after driver/arbitrate exist; admit exact reads and task arbitrate, journal/lesson, notify forms; reject trust/service/config/plugin/init/start/launcher/vendor/arbitrary shell. Claude allowlists only broker; soft adapters are labeled and acknowledgement-gated. Pump invokes an LLM only on exact judgment exit plus verb-read boundary. Driver must honor configured on_verify_fail, before_arbitration, before_merge, and on_blocker hooks with required/optional semantics, and drive archetypes from declared transitions/outcome rather than feature-specific branching; add focused review, test, and custom-archetype cases. Worktree dispatch is idempotent and crash-safe: reuse only a registered worktree whose path, branch, git-common-dir, and task agree; adopt an exact orphan created before field recording; refuse foreign/mismatched paths; never create a duplicate. Add focused no-name-branching coverage that dynamically scans bin, lib, libexec, and runners against all discovered engine identifiers, plus resolver/manifest/plugin tests. Update PROTOCOL.md, help, architecture, and invariants.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/test_drive_hooks_archetypes.sh && /bin/bash tests/test_drive_worktrees.sh && /bin/bash tests/test_task_arbitrate.sh && /bin/bash tests/test_run_boundary.sh && /bin/bash tests/test_orchestrator_command.sh && /bin/bash tests/inv/test_INV-13_driver_verb_only.sh && /bin/bash tests/inv/test_INV-14_engine_neutrality.sh && /bin/bash tests/test_manifest.sh && /bin/bash tests/test_config_resolver.sh && /bin/bash tests/test_launch.sh && /bin/bash tests/test_plugins_test.sh && /bin/bash tests/test_pump.sh && /bin/bash tests/test_e2e_lifecycle.sh && /bin/bash tests/test_e2e_concurrency.sh && /bin/bash tests/inv/test_INV-01_no_spawn_in_tier1.sh && /bin/bash tests/inv/test_INV-02_epoch_fencing.sh && /bin/bash tests/inv/test_INV-07_rebase_reverify.sh && /bin/bash tests/inv/test_INV-11_verify_evidence.sh && /bin/bash tests/test_docs.sh && /bin/bash tests/run.sh
resources: kernel,runners,protocol
exclusive: false
wallclock_budget_s: 28800
started_at:
created: 2026-08-03T10:37:54Z
updated: 2026-08-03T11:08:41Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
