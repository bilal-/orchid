---
schema: 1
id: T002
title: Add deterministic routine tick driver
status: done
archetype: feature
scaffold: false
branch: task/T002
worktree: /Users/bilal/workspace/personal/orchid-task-T002
run_id:
depends_on: T001
attempts: 7
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 260192ef1328c0c9df11f4880b17ec2c206a47cf
candidate_sha: b15b68094ea592f651f68707418443059e2715cd
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 8 REWORK — four fixes, all confirmed against the code by the arbiter. Everything praised in attempt 7 stands: the scoped-first truth table, slot-keyed routing, INV-13/INV-14, drive_worktree_plan, and the honest-labeling disclosures. Do NOT revisit them, do not re-pin Formula/orchid.rb (operator hand-off), and keep the suite green. (1) BLOCKING and it is the ARBITER'S OWN PRIOR MISTAKE, correcting it now — attempt 6's guidance told you 'a non-ok or malformed envelope FOR THE CURRENT CANDIDATE must still fail closed to a boundary'. That was WRONG and contradicts the kernel. libexec/orchid-task:301-313, the reviewing->arbitrating gate, counts only envelopes whose candidate_sha equals the task's current candidate_sha AND whose status is ok, and SILENTLY SKIPS everything else, saying so in its own comment: 'Only status==ok envelopes count; anything else is silently skipped, same as an sha mismatch.' Make lib/drive.sh's evidence arm mirror that EXACTLY: within the current-candidate set, skip any envelope that fails envelope_validate or whose status is not ok — do not boundary on it — and let sufficiency be purely the COUNT of valid ok current envelopes versus review_required_count. The evidence arm then fires only on an insufficient count. Otherwise the ordinary recovery path (a slot errors, reconcile lands status:error bound to the current candidate, the relaunch lands ok) advances through the kernel gate to arbitrating with a complete unanimous set and is then permanently refused deterministic approval over the dead envelope, with no verb able to remove it. Add a focused test for exactly that sequence. See lesson L007. (2) BLOCKING — boundary RESOLVABILITY must be computed from the task's CURRENT STATUS and the verbs the resolved adapter's command surface actually admits, never from the boundary kind alone. Two confirmed instances: run-complete sets drive_boundary_wakes_orchestrator to 0, but runners/orchid-orchestrator-command admits only 'run boundary show|clear' and never 'run accept', so a FINISHED run wakes a model every staleness window forever and, because the notify is suppressed whenever wakes_orchestrator is true, never raises the blocker telling the human to run 'orchid run accept --evidence'; and the three set_boundary review-evidence calls in the reviewing walk fire while the task is still status reviewing, where 'orchid task arbitrate' refuses outright (libexec/orchid-task, exit 3), yet drive_boundary_priority ranks them 1 (arbitrable) so they outrank genuine operator-only boundaries. Fix the classification, and make any boundary that no admitted verb can settle fall back to the 'orchid notify' blocker path so a human is actually told. Add focused tests for the run-complete and reviewing cases. (3) BLOCKING crash-safety — drive_job_outstanding (runners/orchid-drive:438-441) adopts ANY manifest on disk as a live job, including a pid-0 manifest that 'jobs prepare' minted but orchid-launch never spawned. The escalation sweep skips pid-0 (:221), ordinary 'jobs gc' skips pid-0 by design, and the driver never calls 'gc --reap-prepared' — so dispatch prints 'adopting the job an earlier pass already spawned', advances the task, and it then sits in implementing forever with no live job, no infra-fail and no boundary. This defeats the LAUNCH-FIRST-ADVANCE-ONLY-ON-A-SPAWN invariant your own comment at :417-430 states. Treat a pid-0 manifest as NOT outstanding (reap it via the bounded 'gc --reap-prepared' path or relaunch), and add a focused test. (4) BLOCKING liveness, empirically confirmed on this very repo — the driver refreshes the lease only at runners/orchid-drive:187 and :969, but runs 'orchid verify' SYNCHRONOUSLY at :524 with no heartbeat, while pump_stale_s defaults to 900s. T002's own verification chain runs the whole suite and takes about 15 minutes on this machine, so a headless drive here would have its lease read as stale mid-pass, a second drive would start and fence a new epoch, and the first would die on epoch_require under set -e — no pass could ever complete on such a repo. Refresh the lease around the long synchronous verify (a heartbeat during it, or at minimum immediately before and after), and add a focused test that a verify longer than pump_stale_s does not let a second drive start. Do not touch .orchid. Operator authorizes retries until success.
engine: codex
effort: medium
acceptance_criteria: Add a deterministic one-pass driver covering lease refresh, reconcile-check-gc ordering, safe dispatch, implementer reconciliation, verification, reviewer launch/reconciliation, unanimous structured approval, one serialized merge, status generation, and final lease refresh. Decisions use structured fields and exit codes only. Every durable or cross-process .orchid mutation, including boundary set/clear, goes through a named verb with single-writer ownership; status generation calls orchid status --html. Add orchid run boundary set|clear|show with schema and dedicated exit code, and orchid task arbitrate as the sole explicit judgment-result verb. Non-overlapping truth table: missing/malformed/non-ok/stale/incomplete evidence -> boundary; when every required review is valid/current, unanimous scope-complete approvals -> deterministic approve through task arbitrate; any request-changes verdict, any finding at or above blocking_severity, mixed verdicts, or non-scope-complete review -> boundary with no transition. Planning, blocked tasks, required-hook failure, and operator-only decisions are boundaries. Add a judgment-only argument-validating broker after driver/arbitrate exist; admit exact reads and task arbitrate, journal/lesson, notify forms; reject trust/service/config/plugin/init/start/launcher/vendor/arbitrary shell. Claude allowlists only broker; soft adapters are labeled and acknowledgement-gated. Pump invokes an LLM only on exact judgment exit plus verb-read boundary. Driver must honor configured on_verify_fail, before_arbitration, before_merge, and on_blocker hooks with required/optional semantics, and drive archetypes from declared transitions/outcome rather than feature-specific branching; add focused review, test, and custom-archetype cases. Worktree dispatch is idempotent and crash-safe: reuse only a registered worktree whose path, branch, git-common-dir, and task agree; adopt an exact orphan created before field recording; refuse foreign/mismatched paths; never create a duplicate. Add focused no-name-branching coverage that dynamically scans bin, lib, libexec, and runners against all discovered engine identifiers, plus resolver/manifest/plugin tests. Update PROTOCOL.md, help, architecture, and invariants.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/test_drive_hooks_archetypes.sh && /bin/bash tests/test_drive_worktrees.sh && /bin/bash tests/test_task_arbitrate.sh && /bin/bash tests/test_run_boundary.sh && /bin/bash tests/test_orchestrator_command.sh && /bin/bash tests/inv/test_INV-13_driver_verb_only.sh && /bin/bash tests/inv/test_INV-14_engine_neutrality.sh && /bin/bash tests/test_manifest.sh && /bin/bash tests/test_config_resolver.sh && /bin/bash tests/test_launch.sh && /bin/bash tests/test_plugins_test.sh && /bin/bash tests/test_pump.sh && /bin/bash tests/test_e2e_lifecycle.sh && /bin/bash tests/test_e2e_concurrency.sh && /bin/bash tests/inv/test_INV-01_no_spawn_in_tier1.sh && /bin/bash tests/inv/test_INV-02_epoch_fencing.sh && /bin/bash tests/inv/test_INV-07_rebase_reverify.sh && /bin/bash tests/inv/test_INV-11_verify_evidence.sh && /bin/bash tests/test_docs.sh && /bin/bash tests/run.sh
resources: kernel,runners,protocol
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-05T12:35:55Z
created: 2026-08-03T10:37:54Z
updated: 2026-08-05T19:14:18Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
