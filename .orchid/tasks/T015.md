---
schema: 1
id: T015
title: Reconcile cross-cutting docs and prove run-level acceptance
status: done
archetype: feature
scaffold: false
branch: task/T015
worktree: /Users/bilal/workspace/personal/orchid-orchid-T015
run_id:
depends_on: T001 T002 T003 T004 T005 T006 T007 T008 T009 T010 T011 T012 T013 T014 T016 T017 T018 T019 T020 T021 T022 T023 T024 T025 T026 T027 T028 T029 T030 T031 T032 T033 T034 T035 T036 T037 T039 T040
attempts: 7
infra_failures: 0
session_id:
implementer_engine_id: codex
base_sha: 51f5fa3c59cfe2b18d756ab99318b0730a5629b7
candidate_sha: d38e8ed8a16903b949613de8aa405b9b4cfe8447
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: CURRENT REVIEW GUIDANCE: candidate d38e8ed8a16903b949613de8aa405b9b4cfe8447 includes mandatory operator-owned evidence commits carrying Orchid-Handoff: operator, focused scanner corrections, and the final legacy tests/test_install.sh verb-lint fix. T030 removed candidate-local Formula pinning; Formula/orchid.rb remains untouched. No executable or chmod is involved. Targeted operator checks passed: test_install, test_docs, bash syntax, and warning ShellCheck. Review the complete candidate and its production RED/GREEN evidence without misattributing the trailered operator commits.
engine: codex
effort: medium
acceptance_criteria: The plan critique found that cross-cutting documentation and final run-level acceptance had no owning task, so they would have been done ad hoc by whoever noticed — which is how r-001 ended up with a stale help string and an unenforced gate. This task owns them. Reconcile every documentation surface the run changed so code and prose agree: README, PROTOCOL.md, docs/specs/*, docs/engines/*, docs/troubleshooting.md, docs/beta-qualification.md and every --help text a changed verb prints. Confirm each r-002 decision recorded in docs/specs/ states BOTH the choice and the rejected alternative (T011, T012). Retire or update any lesson this run supersedes rather than leaving two lessons that contradict each other, and add lessons for anything r-002 itself found. Then prove run-level acceptance as far as a candidate CAN: this task cannot verify the merged tree, because its own candidate has to merge first — the merged-tree check is therefore the operator step immediately after this task lands, and the evidence file must say so rather than claiming a tree it never saw. What this candidate proves locally: scripts/ci-local.sh green, and — new for this run, since r-001's evidence was misleading on exactly this point — the LOCAL equivalent of what CI runs, including the PATH-restricted no-vendor-CLI proof T013 delivers, since this run may not contact a remote. GitHub Actions going green is an OPERATOR step outside this run, exactly like the PR: the run produces the fixes and the local proof, the operator pushes and observes. Record in the evidence file that remote CI was NOT observed by this run and name the command the operator should watch, rather than implying it passed. Write the evidence file, stating plainly what was and was not proven, and keep the shipped version on 1.0.0-beta.x. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. STATE THE RELEASE POSTURE EXPLICITLY in the evidence, as r-001's did: no third-party beta run has occurred, nothing was published, pushed, tagged or sent to a remote by this run, and the shipped version remains 1.0.0-beta.x. Omitting these reads as a claim; r-002 must not weaken the honesty r-001 established. AUDIT THE BOOTSTRAP PROCEDURE: read back the journal entries the operator was required to write for every task dispatched before T001, T006 and T010 merged, and report any dispatch that has none. A procedure with no record is indistinguishable from one nobody followed, which is exactly how r-001's release gate went unenforced for eight tasks. === ALSO: the documentation gate in scripts/ci-local.sh reads ordinary prose as a verb invocation. It extracts every 'orchid <word>' in PROTOCOL.md and demands a matching libexec/orchid-<word>, so the sentence fragment 'orchid creates ...' failed r-002 T023 with 'PROTOCOL.md names orchid creates but libexec/orchid-creates does not exist'. The gate is worth keeping — it catches genuinely undocumented verbs — but it must distinguish a documented invocation from English. Anchor it to how verbs are actually written in PROTOCOL.md (a code span or backticks) rather than any bare word after 'orchid', and add a RED case proving it still catches a real undocumented verb plus a case proving prose no longer trips it. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === SECOND INSTANCE OF THE SAME GATE DEFECT, found on r-002 T019: tests/inv/test_INV-13_driver_verb_only.sh strips comments and then greps the remaining text for forbidden tokens including 'worktree[[:space:]]+(add|remove)', so it flags a printf of the human-readable diagnostic 'git worktree add cannot reproduce it' as though lib/drive.sh had performed the operation. The words are inside a STRING LITERAL. This is the identical shape as the ci-local.sh documentation gate reading the prose 'orchid creates' as a verb invocation, and it cost T019 a full attempt against code that mutates nothing. Both gates are worth keeping - they catch real violations - but a textual gate that cannot distinguish an operation from a string describing one will keep charging attempts for correct code, and the natural workaround (reword the diagnostic) makes error messages worse to satisfy a linter. Fix both together: strip or ignore string literals as well as comments, or key the checks on something structural. Ship a RED case proving each gate still catches a genuine violation, plus a case proving prose mentioning the token no longer trips it. === DIAGRAMS ARE A DOCUMENTATION SURFACE AND THIS TASK OWNS THEM TOO. README.md and docs/architecture.md carry mermaid diagrams that encode the task state machine, the tick sequence and the evidence rules - exactly the things this run changes. Off the top: T022 adds a refusal path out of implementing for an ok envelope that produced no candidate; T031 makes verification refuse a tree that is not the recorded candidate and changes when evidence is considered bound; T032 makes an operator objection persist so deterministic approval cannot fire over it; T003 changes what the pump does at a judgment boundary; T036 changes when a run reaches a terminal state. A diagram that still shows the old edges is a confident, readable lie, and it is the first thing a new reader trusts. Walk every diagram against the merged behaviour, redraw what moved, and add edges the run introduced rather than leaving them implicit in prose. If a diagram cannot be made accurate without becoming unreadable, say what it deliberately omits in a caption instead of quietly leaving it wrong. === FOURTH INSTANCE, AND IT STRANDED A TASK: r-002 T006 exhausted its attempt budget and was auto-blocked on ShellCheck SC1010 at tests/test_stale_root.sh:457 and :559 — 'assert_eq done "$(...)"', where the bare word 'done' passed as an argument is parsed as the loop keyword. Every other check in its chain passed. The full set of four is now: the ci-local.sh documentation gate reading the prose 'orchid creates' as a verb invocation (T023); INV-13's scan matching 'git worktree add' inside a diagnostic string (T019); ShellCheck parsing backticks in a green_case annotation as command substitution (T017); and this one. THREE OF THE FOUR CONSUMED ATTEMPTS AGAINST CODE THAT WAS NOT DEFECTIVE, and the fourth cost a task its entire budget. This is no longer a papercut to note in passing — it is a systematic tax on every task, and the natural operator workaround (reword the prose, quote the word, avoid the token) degrades diagnostics and test readability to satisfy a linter. Fix the two gates orchid owns by ignoring string literals as well as comments, or by keying on something structural rather than textual. Where the gate is ShellCheck itself and cannot be changed, say so plainly in the contributing docs with the exact idioms that trip it, so an implementer is warned before it costs a round rather than after. === PROTOCOL.md's TICK PROCEDURE IS NOW WRONG IN TWO PLACES, FOR THE SAME STRUCTURAL REASON, and this task must repair both and say why it happened. Reviewers found T022 had mangled the 'implementing' arm - the list numbered 3, 4, 2, 1, with the superseded 'advance straight to testing' step still ahead of the new ones and item 2 truncated mid-sentence - and, two hours later, found T019 had done the same to the 'testing-FAIL' arm, keeping the superseded pre-classification advance, duplicating its closing paragraph, and splicing its new block in as a mid-sentence fragment. NEITHER IMPLEMENTER DID ANYTHING LOCALLY UNREASONABLE: each inserted its own step into a numbered procedure it was editing in isolation, and neither could see the other's edit or the result of both. This is a cost of running four tasks in parallel over one prose file that describes a single ordered process, and no amount of per-task review catches it, because every individual edit reads correctly. Repair both arms into one correctly ordered procedure that matches what the driver actually does after all of this run's merges, read the whole file start to finish rather than diffing it, and record the general point in the retrospective's terms: a numbered procedure edited concurrently by independent actors degrades in a way a diff review cannot see, and PROTOCOL.md is the file every future implementer is handed. === RUN-LEVEL ACCEPTANCE MUST INCLUDE ONE SUITE RUN ON THE INTEGRATION BRANCH ITSELF, and this task owns adding it. Proven on 2026-08-12 (lesson L036): commit 416fcc9 checked out on branch orchid/integration fails tests/test_unattended_trust.sh with five FAILs; the IDENTICAL commit checked out on a branch called anything else passes with rc=0. The cause is a kernel guard in lib/common.sh:444-465 that returns early for every root not parked on the configured integration_branch and only then shells out to git — so a task worktree and orchid-merge's temp worktree, which are by construction never on that branch, cannot observe it. T006's own verify AND the merge revalidation both ran the whole suite honestly and both reported CI PASS with zero FAILs. Re-running either would never have caught it. The general rule to write down: any kernel path conditioned on the checked-out branch, on being the install root, or on any property a temp worktree cannot have, needs a test that CONSTRUCTS that condition rather than inheriting it from the ambient environment, because the ambient environment of every gate orchid runs is the one where the condition is false. State this in the docs and add the integration-branch run to the run's acceptance checklist, so 'the full suite is green' names WHERE it was green.
verification_commands: /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-30T21:24:58Z
created: 2026-08-09T15:20:34Z
updated: 2026-08-31T01:01:07Z
attempt_budget: 8
handoff_ack: d38e8ed8a16903b949613de8aa405b9b4cfe8447
rework_rounds: 1
rework_signature: 0c2ab66960bd44989eb152d72ac156902ff8fb02d27ed6def38d87910bb1e2ed
rework_signature_repeats: 0
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-30T18:41:09Z):** Complete the implementation now; do not ask for design approval. Preserve the three partial files in candidate d0690c85, then satisfy every omitted T015 acceptance surface: README, PROTOCOL and both broken tick arms, architecture diagrams, specs and engine/help docs, troubleshooting and beta qualification, T011/T012 rejected alternatives, bootstrap journal audit, lesson reconciliation, and an honest candidate-vs-post-merge acceptance evidence file. Explicitly record no remote CI, third-party beta, push, tag, publish or release and retain 1.0.0-beta.x. Because T030 removed per-candidate formula pinning, do not touch Formula/orchid.rb; name integration/release pinning as operator-owned. Add no chmod handoff unless a new executable exists. Do not run verification per this task's handoff; edit and commit the complete result and report checks as not run.

**Operator guidance (2026-08-30T19:07:04Z):** THIS SUPERSEDES EARLIER GUIDANCE. Fix only the targeted preflight blockers on candidate cb78ab6d and commit the result. First, keep boundary.json and other data-token scans on code_of so quoted literal paths remain visible. Second, operations_of must not let a policy library hide forbidden work in a shell code argument: explicitly reject bash -c and sh -c or preserve those command strings, and add RED probes aimed at the same production scans for a quoted boundary path and shell-c worktree mutation. Third, make the two new help assertions in tests/test_docs.sh match their shipped help: keep the required drive phrase contiguous and name the PATH-restricted no-vendor-CLI proof verbatim. Preserve the broad documentation and evidence work. Do not run verification under this handoff and do not touch Formula/orchid.rb or file modes.

**Operator guidance (2026-08-30T19:17:03Z):** THIS SUPERSEDES EARLIER GUIDANCE. Preserve candidate a921c8a2. Make the INV-13 policy purity scan reject shell code for bash or sh when c is bundled with other short flags, when options precede -c, and when a command-position shell variable is followed by a code option; add production-path RED probes for bash -lc, sh -ec, /bin/bash --noprofile -c and "$BASH" -c so string elision cannot hide a forbidden mutation. Then point the existing run-complete help assertion at drive_help_one_line so the complete phrase is tested across wrapping. Keep the other passing RED and GREEN cases unchanged. Do not run verification, touch Formula/orchid.rb or change modes.

**Operator guidance (2026-08-30T19:39:09Z):** THIS SUPERSEDES EARLIER GUIDANCE FOR THE NEXT ROUND. Preserve candidate b6747a9c. Make only two documentation-honesty corrections: (1) docs/beta-qualification.md must say completed rows are an operator acceptance policy because orchid run accept checks run status and evidence-file existence but does not parse row content; add a focused docs assertion for that enforced/advisory distinction. (2) docs/contributing.md must say the <engine> token becomes redirection only when it is inside backtick command substitution nested in a double-quoted annotation, while a plain <engine> inside an ordinary double-quoted argument is literal and safe. Preserve the already-correct PROTOCOL walk, the audit evidence, all gate fixes, Formula/orchid.rb, and modes. Do not run verification under the implementer handoff; commit only these narrow changes.

**Operator guidance (2026-08-30T19:47:30Z):** THIS SUPERSEDES EARLIER GUIDANCE FOR THE NEXT ROUND. Preserve candidate a55ff896. Fix only the escaped-quote cross-pairing bypass in tests/inv/test_INV-13_driver_verb_only.sh. operations_of currently treats every quote byte as a delimiter, so two separately valid shell strings each containing one escaped quote can pair their outer quotes across live code and elide a genuine git worktree add. Honor the documented fail-closed rule: if escaped quoting is ambiguous, emit the original line for scanning, or implement equivalent escape-aware parsing that cannot hide the operation. Add a RED probe through the same production policy_impurity function using two escaped-quote strings around a real worktree add; ensure the existing inert-diagnostic GREEN and all six shell-code variants remain. Do not touch other docs, Formula/orchid.rb, or modes, and do not run verification under the implementer handoff.

**Operator guidance (2026-08-30T19:56:18Z):** THIS SUPERSEDES EARLIER GUIDANCE FOR THE NEXT ROUND. Preserve candidate c5b83b2. Fix only POLICY_SHELL_CODE and its shell_code_probe so real bash/sh code invocations remain rejected when option arguments precede the c option. Exact currently missed rows: bash -o pipefail -c, bash -O extglob -c, bash +O extglob -c, bash --rcfile /tmp/rc -c, bash --init-file /tmp/rc -c, and sh -o errexit -c, each with a quoted git worktree mutation. Do not let matching cross semicolon, pipe, ampersand, or parenthesis command separators. A conservative fail-closed match after a literal bash/sh or command-position $BASH is acceptable; inert diagnostic strings must still pass because operations_of elides them. Add exact assertions through production policy_impurity, preserve all five current RED cases/two GREEN cases, the docs corrections, Formula policy, and modes. Do not run verification under the implementer handoff.

**Operator guidance (2026-08-30T21:18:07Z):** THIS SUPERSEDES EARLIER GUIDANCE. Preserve candidate 4e0565c7. Fix only the legacy PROTOCOL verb-existence lint in tests/test_install.sh: it still uses grep -oE 'orchid [A-Za-z_-]+' over all prose and caused formal CI to fail on the ordinary phrases 'orchid code', 'orchid itself', and 'orchid runs'. Make it recognize only structurally marked invocations (backtick code spans and command lines inside fenced blocks), consistent with the already-correct doc_verb_names policy in tests/test_docs.sh. Add focused production-path RED evidence for a marked nonexistent verb and GREEN evidence for bare prose, including those exact three phrases. Preserve all other candidate work, Formula/orchid.rb, and file modes. Do not run verification under the implementer handoff; commit only the narrow correction.
