---
schema: 1
id: T013
title: Make the suite hermetic so CI passes without vendor CLIs
status: done
archetype: feature
scaffold: false
branch: task/T013
worktree: /Users/bilal/workspace/personal/orchid-orchid-T013
run_id:
depends_on:
attempts: 3
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 2e1f689aac023e59eb479a2f1eee1aaac7ffb964
candidate_sha: 88a8001f8a111a48dc98e9a7bf6e02b1e519ba78
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: MAKE NO CHANGES. Your previous round is CORRECT and the operator has verified it by hand: the full chain (tests/test_hermetic_suite.sh, tests/test_plugins_test.sh, tests/test_capsuite.sh, scripts/ci-local.sh) exits 0 with zero failures at commit 88a8001, and the proof now certifies the suite with codex claude agy hermes openclaw unresolvable, in a disposable HOME, under a concurrent writer of the ambient home. Your NOT-TESTED note about a second Orchid mutating this checkout mid-run is accepted as an honest, operator-owned boundary. The ONLY reason the last verification failed was the operator's own hand-off: your report commit landed after the formula pin, making Formula/orchid.rb stale. The operator has re-pinned it. This task is at its attempt cap and there is NO supported way to grant it another, so any edit you make risks stranding the entire run. Do not edit, refactor, rename, add tests or tidy anything. Reply with a short report confirming you changed nothing and naming the verification the operator should run. MANDATORY OPERATOR HAND-OFFS remain: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier.
engine: claude
effort: medium
acceptance_criteria: CI HAS NEVER BEEN GREEN. The workflow T004 added was never exercised because nothing was pushed until 2026-08-06; its acceptance criterion checked that the config was VALID, not that it RAN. On its first three runs it failed on ubuntu-latest AND macos-latest. Root cause on both platforms: the suite is not hermetic. tests/test_plugins_test.sh asserts binaries_present for codex, claude and agy, which are installed on the author's machine and absent on a runner, so capsuite checks report passed=false and the plugins-test assertions fail. Local scripts/ci-local.sh passes only because those CLIs happen to exist locally. Make the deterministic suite pass with NO vendor CLI installed: skip or stub the capability-suite checks that genuinely require a real binary, and record them honestly as not-tested rather than as passes, matching the vocabulary beta-qualify already uses. Do not weaken a check into always-true. The PROOF RUN IS THE OPERATOR'S STEP, not yours: you cannot execute anything (L017). Your deliverable is the change plus a committed, self-contained way to run it — a test or script that PATH-restricts codex, claude and agy to unresolvable and asserts the suite still passes — so the operator can run exactly that and CI runs it thereafter. Name in your report the command the operator should run. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. The PATH-restricted proof must be a NAMED, independently runnable file — create tests/test_hermetic_suite.sh, which this task's verification_commands already invokes — so the no-vendor-CLI guarantee is executed by the suite and by CI thereafter, not merely described in a report. RECURSION GUARD REQUIRED: tests/run.sh globs tests/test_*.sh, so a harness named tests/test_hermetic_suite.sh that itself invokes the full suite would be re-entered by the run it launches, forever. Guard it explicitly — an env marker the inner run sets and the harness checks before re-invoking, or a location the glob does not reach — and add an assertion that the guard actually prevents re-entry, so a future refactor cannot silently restore the loop.
verification_commands: /bin/bash tests/test_hermetic_suite.sh && /bin/bash tests/test_plugins_test.sh && /bin/bash tests/test_capsuite.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-10T01:01:03Z
created: 2026-08-08T23:25:26Z
updated: 2026-08-10T15:25:56Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-10T05:02:48Z):** the verify failure that exhausted the budget was SPURIOUS, proven by re-running the identical candidate 4bb8d03 by hand: PASS 03:44, FAIL 04:45, PASS 05:20, with no change to the tree and no recursion-guard skip in the passing run. The failing run reported ~12 notify-channel assertions that do not depend on the diff, and it ran while the drive loop was polling this repository; the clean run had nothing else touching it. Recorded as lesson L024. Not one of this task's three attempts was a genuine implementation failure: one went to the formula-pin merge deadlock (L022), one to a no-op rebase that invalidated a passing verify, and this one to flakiness — exactly the class T019 says must not be charged. Reworking with a narrow brief: fix the isolation the proof does not yet cover, and make ORCHID_HERMETIC_PROOF strict so a vacuous pass cannot masquerade as this flake.

**Operator guidance (2026-08-10T06:36:35Z):** the candidate is GREEN: the operator re-pinned Formula/orchid.rb at the implementer's final commit (88a8001) and hand-ran the full verification chain to exit 0 with zero failures. The block came from two operator/environment causes and no implementation defect: the report commit landed after the pin, staling it, and the evidence was mis-bound because the driver captured candidate_sha at 05:04:31Z while the job kept committing until 05:23:10Z (lesson L025, scheduled as T031). The rework round exists only because no transition leaves blocked except through rework; the implementer is briefed to change nothing. Attempts were consumed entirely by the formula-pin deadlock (L022), a no-op rebase, and suite flakiness (L024) — the class T019 says must not be charged.
