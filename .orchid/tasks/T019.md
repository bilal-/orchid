---
schema: 1
id: T019
title: Stop charging environment and operator failures against the attempt budget
status: done
archetype: feature
scaffold: false
branch: task/T019
worktree: /Users/bilal/workspace/personal/orchid-orchid-T019
run_id:
depends_on: T013
attempts: 26
infra_failures: 12
session_id:
implementer_engine_id: claude
base_sha: b170bcff7762165867f9352806e59ab5d943082a
candidate_sha: 5a04b1eace9bf5a6718a4ab5840d4b519c128d6b
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: FINAL ATTEMPT-27 CANDIDATE 5a04b1eace9bf5a6718a4ab5840d4b519c128d6b. Review only 76a53f74d8d09615477284e8c39338f9bf215044..5a04b1eace9bf5a6718a4ab5840d4b519c128d6b. This accepts the Codex attempt-26 finding: verifier evidence now captures base_sha before candidate-controlled commands, validation binds it to task state, and flaky authority uses the captured base. The hostile regression proves an unbound base_sha rewrite trusts a candidate-authored FLAKE register while the bound classifier charges. Do not reopen prior approved scope or broaden into low-severity observations.
engine: claude
effort: medium
acceptance_criteria: The attempt budget currently measures the harness's bad days, not the candidate's quality. In r-002's bootstrap wave alone: T014 burned three attempts on a stale Formula/orchid.rb it could not re-pin, T013 was stranded by a reaped manifest, and both were sent to rework by a scheduling-dependent streaming assertion (L020) that passes in isolation. None of those failures were in the code under test, yet each consumed a rework attempt and drove tasks to blocked. Classify a verification failure before charging it: a failure attributable to a known operator hand-off (stale pin, missing exec bit), to a harness or environment fault, or to a test the repository already records as flaky must not consume an attempt, and must say so in the journal. A failure in the candidate must. Where classification is uncertain, charge the attempt and say why — fail toward the strict reading, never silently forgive. Add focused tests for both arms. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. NAMED CASE from the webBooks orchid run's lesson L003, which is the same class and must be covered: a freshly created task worktree does not carry gitignored build state that 'git worktree add' cannot reproduce — there, mobile/node_modules exists in the integration checkout only as a gitignored symlink into a sibling checkout, so the first orchid verify in every new worktree failed on missing dependencies and burned a rework attempt on an environment gap rather than a candidate defect. Classify that as environmental and do not charge it. Consider also whether dispatch should reproduce such state, or at minimum detect and report it, rather than letting each project rediscover it by losing an attempt. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === THE FLAKE THAT PROVES THIS TASK NECESSARY, AND WHICH THIS TASK MUST NOW ALSO NEUTRALISE (lesson L020). One assertion in tests/test_engine_agy.sh — "streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)" — is timing-sensitive and fails whenever the machine is loaded. By 2026-08-22 it had stranded SIX tasks in this run: T002, T003, T005, T018 and two earlier. In every case the candidate was otherwise clean, the attempt budget was charged for a scheduling artifact, and the task hit the hardcoded cap of three and blocked. Recovering each one costs an operator intervention plus a full verify cycle of roughly half an hour to rediscover that the assertion flakes. It is, measured over this run, the single largest consumer of attempt budget — larger than any real defect. WHAT TO DELIVER, in addition to the classifier work already specified above. (1) Make the assertion not flake: it is asserting LIVENESS (the adapter streams while running), so sample for growth with a bounded wait rather than testing a single midpoint instant, or drive the stub deterministically so the midpoint is not a race. Do not delete the assertion — the property it checks is real and is the stall-detector own evidence. (2) If it cannot be made deterministic in this task, QUARANTINE it explicitly: mark it as a known-flaky case that reports without failing the suite, name it in the file with the reason, and add it to whatever list makes that visible, so it stops charging attempts while remaining visible as an unresolved problem. Silence is not acceptable; an unreliable gate must announce that it is unreliable. (3) EITHER WAY, the classifier half of this task must recognise this failure signature as a non-candidate failure, so that if it flakes again it charges infra rather than attempts. That is the general rule this task exists for and this is its most expensive live instance. PIN BOTH EDGES per lesson L034: a case proving a genuine streaming stall is still caught, and a case proving a loaded-machine timing artifact no longer charges the attempt budget. === CORRECTION TO THE L020 SCOPE ABOVE: THE FLAKE IS A FAMILY OF FOUR, NOT ONE ASSERTION, AND FIXING ONE LEAVES THREE. Found 2026-08-23 when T018 blocked on a sibling nobody had named. The same mid-run liveness pattern — sample the job log at a midpoint and assert it has moved WHILE the adapter is still running — appears in THREE engine-adapter test files: tests/test_engine_agy.sh carries BOTH shapes (the grown-while assertion around line 224 and the heartbeat-count one), tests/test_engine_claude.sh carries the grown-while shape, and tests/test_engine_codex.sh carries the heartbeat-count shape at line 445. All four fail the same way for the same reason: they sample at one instant and a loaded machine has not scheduled the writer yet. Between them they have now stranded EIGHT tasks in this run. Treat this as one design fault with four sites, not four bugs: whatever you do for the agy grown-while case must be applied to all four, and your RED and GREEN pair must cover the shape rather than a single file. If you quarantine rather than de-flake, quarantine all four and name them together with one reason, so a reader learns the pattern instead of finding three more later.
verification_commands: /bin/bash tests/test_task.sh && /bin/bash tests/test_drive.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-27T20:55:07Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-27T23:08:08Z
refused_envelopes: T019-a4-implementer.14.json T019-a4-implementer.15.json T019-a4-implementer.16.json
handoff_ack: 5a04b1eace9bf5a6718a4ab5840d4b519c128d6b
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T02:59:02Z):** auto-blocked at the cap, but two of its failures were the operator's own hand-off timing (a stale formula pin, since re-pinned; candidate_sha now equals worktree HEAD) rather than defects in the candidate. The genuine failure is a real and pointed one: the verify_class classifier recognises the stale-pin hand-off but classifies the EXEC-BIT hand-off as 'candidate' instead of 'handoff', so the task built to stop operator hand-offs being charged to the attempt budget charges one of the two named hand-offs to the implementer. Unblocked with that named precisely and the operator-caused noise excluded. Recording again that unblock restores no budget — attempts stays 3 and the next verify failure re-blocks — which is the T026 gap, now hit by a second task tonight.

**Operator guidance (2026-08-11T06:01:33Z):** second stranding but with clear progress: three failures became two, and the named exec-bit hand-off is now classified correctly. What remains is the RAW shape — the shell's own refusal to execute a mode-644 file, which is what a repository with no such gate actually sees — still classified as 'candidate' rather than 'handoff', and the reason not naming the file needing chmod. Both are the same fix from either end: detect the raw shape and carry its filename into the reason. Unblocked with that stated precisely.

**Operator guidance (2026-08-11T12:18:45Z):** stranded on a mix: the stale-pin failure was the operator's hand-off racing the implementer's final commit (worktree was clean, so the loop's cleanliness guard behaved correctly - this is the inherent L009 same-pass race, not the guard), now re-pinned and rebound to 1a1e490. The genuine failures are mechanical and the task is following the operator's scope steer correctly: it corroborates a path from the diagnostic rather than matching prose, but its tokenizer keeps the trailing ':' or ',' that punctuation wraps around a path, so the path never opens, corroborates nothing, and the classifier charges - the tests state the failure direction explicitly. Plus built-in recognition of T014's stale-package-pin case requires per-repo config it should not need. Unblocked with all four named.

**Operator guidance (2026-08-11T15:34:46Z):** STRANDED AGAIN ENTIRELY ON THE OPERATOR'S HAND-OFF. The sole failure was 'Formula/orchid.rb checksum is stale for the current tree'; the worktree is clean and nothing in the candidate is defective. This is the fourth and fifth stranding this run caused by the operator's re-pinning workaround (lesson L031). The race is now understood and CANNOT be closed from outside the kernel: the driver reconciles the implementer's envelope and runs verification in the SAME pass, so an implementer commit landing after the loop's last pin is verified against a stale pin before any external process can act. Three generations of guard have narrowed the window - job-liveness, then worktree-cleanliness - and none can eliminate it, because the only safe moment to re-pin is between the job's final commit and the driver's reconcile, and nothing outside the kernel can observe that boundary. Re-pinned at b027590f80368c120069f7cdf46abe52f328aef1 and candidate_sha rebound. The real fix is T030, which removes the formula pin from every task's verification chain; until then this recurs and the operator absorbs it.

**Operator guidance (2026-08-11T22:14:56Z):** STRANDED BY A MISSING EXEC BIT — the operator's hand-off, and the irony is worth recording: THIS IS THE TASK WHOSE PURPOSE IS TO CLASSIFY EXACTLY THIS FAILURE AS A HAND-OFF RATHER THAN A DEFECT. runners/orchid-drive shipped mode 644, so every drive invocation in its own suite returned 126 Permission denied and 116 assertions cascaded from that single cause: 'runners/orchid-drive must exist and be executable', 'T001 must reach done under repeated deterministic passes alone (last rc=126 ... Permission denied)', and so on. Nothing in the candidate was defective. The operator has chmod +x'd it, swept the rest of the diff for any other mode-644 file carrying a shebang (none), re-pinned and rebound. WHY THE OPERATOR'S OWN LOOP MISSED IT: its exec-bit hand-off step inspects only 'git diff --name-only HEAD~1', so a mode that was lost in an earlier commit of the same attempt is invisible to it. That is a real gap in the operator's scaffolding, now the sixth defect found in it this run, and it is the same shape as the others — a guard that covers almost the right thing. It also stands as direct evidence for T019's own thesis: an unexecutable driver is not an implementation defect, it is a hand-off only the operator can perform, and charging it to the attempt budget is exactly the injustice this task exists to remove.

**Operator guidance (2026-08-12T02:38:13Z):** 39 genuine failures, but they cascade from one: the classification pass exits rc=2 rather than completing or reaching a boundary. Everything else in the round is downstream of that abort. The stale pin was the operator's (re-pinned, rebound); the 'config_get: command not found' line is a PRE-EXISTING backtick-in-annotation defect on the integration branch, verified identical at df47066, and belongs to T016 rather than to this task. Unblocked with the abort named as the thing to find first and the full assertion list restated.

<!-- orchid:rework-brief candidate=a92fed18ce5861f8a6407c86a3151baf14b37567 superseded -->
**Superseded rework brief (candidate `a92fed18ce58`) — 16 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T04:29:39Z):** the rc=2 classification abort from last round is FIXED and all 39 cascading failures with it — this round has ZERO failed assertions. It stranded on exactly two ShellCheck findings, both genuine: SC2194 at lib/drive.sh:1714, where a case pattern of regex metacharacters is written as one constant word so the branch can only match that literal and is probably dead; and SC1087 at tests/test_drive.sh:3653, where '$WMARK[' reads as an array expansion and needs bracing. The operator swept all three of its changed shell files with ci-local's exact invocation: those two are the only findings. Unblocked with both named and the likely logic bug in the first flagged as more than a lint issue.

<!-- orchid:rework-brief candidate=c905823c37ece8cf21048d97707adc5a2505e544 superseded -->
**Superseded rework brief (candidate `c905823c37ec`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T10:56:55Z):** ONE WORD, AND IT IS THE ONLY THING WRONG. This round's verify produced three FAIL lines that all trace to a single cause, and the scope-cut deliverable itself is clean.

THE DEFECT. PROTOCOL.md line 989 reads 'wait for what it samples: orchid charges it, because it cannot prove flakiness and will not guess at it.' That is prose about the product, but it is spelled with a lowercase 'orchid' immediately followed by a word, which is exactly the shape tests/test_install.sh's verb-existence lint scans for. That lint is deliberately broad — its own comment at line 282 says it matches 'every orchid <word> mention, backtick-wrapped or not' — so it reads 'orchid charges' as a reference to a verb and demands libexec/orchid-charges, which does not and should not exist. The lint is right; the prose is wrong.

THE FIX. In PROTOCOL.md, change 'orchid charges it' to 'Orchid charges it' — capital O, because here it names the product and not the command. Change nothing else. The lint's regex is case-sensitive, so the capital is what clears it.

WHY THE OTHER TWO FAILS ALSO GO. The 'deterministic suite does not pass ... in an isolated HOME' failure is not a second problem: the hermetic run's preserved output shows its only failure is this same missing-orchid-charges line. One fix clears all three.

I VERIFIED THIS IS THE ONLY INSTANCE. Every 'orchid <word>' mention in your PROTOCOL.md resolves to an executable libexec verb except this one, so there is no second occurrence waiting to burn the next round. Do not go looking for others and do not reword surrounding sentences.

BUDGET. Your attempts field stays at 3, which means this is your last verify: the driver blocks again on any verify FAIL. It is a one-word edit and the rest of the candidate already passed, so that is enough — but it is why the instruction is to change that word and nothing more.

**Operator guidance (2026-08-18T03:14:02Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-25T02:37:47Z):** Unblocking so the run can move: this task hit the infra ladder cap, and the ladder has been charged repeatedly for environment faults rather than defects — which is precisely what this task exists to fix. Your outstanding work is unchanged: the classifier inversion (the L003 named case pinned as CHARGED), the six-site liveness flake family, and classifying harness and known-flaky failures rather than only the two operator hand-offs. Before committing run bash tests/test_drive.sh, bash tests/test_engine_agy.sh, bash tests/test_engine_codex.sh, bash tests/test_engine_claude.sh and shellcheck on every file you edited, and confirm each exits 0.

**Operator guidance (2026-08-25T14:13:49Z):** Unblocking so the run can move. This task was blocked by the false-block surface this run has been documenting — a mid-run liveness flake (six sites, L020), a wallclock budget that counts time waiting on reviewers (L042), or an infra ladder charged for environment faults (L039). None of those is a judgement about your code. Separately, an engine was excluded for days because its envelopes were never drained rather than because it failed (F35), so review slots queued behind one engine and independence could not be proven. That is fixed: the supervisor now drains before every pass. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0; if nothing needs changing say so in your reply rather than answering with an empty round.
