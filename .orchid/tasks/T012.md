---
schema: 1
id: T012
title: Decide whether review policy requires a worktree-capable slot
status: done
archetype: feature
scaffold: false
branch: task/T012
worktree: /Users/bilal/workspace/personal/orchid-orchid-T012
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 9
infra_failures: 6
session_id:
implementer_engine_id: claude
base_sha: 469bdcb99264332d8f857ddd5467adfd8290168a
candidate_sha: d632bc05020905f7dfd3be8d088e172b2d006357
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: Lesson L010: engine independence and review DEPTH are different axes, with direct evidence from r-001. On T003 the engine-independent slot (agy) APPROVED a candidate whose central acceptance criterion was unmet, with a one-sentence unsupported rationale and a null findings array, while the session-independent slot found the defect and cited the file and line; the arbiter confirmed it in the code and rejected. agy did that four times across the run. It is an INLINE, diff-only reviewer by design — no tools, no worktree — so it judges from diff text alone and cannot open the file a change must stay consistent with. Its independence is real and worth keeping; its depth is bounded. DECIDE whether review.<tier> should require at least one WORKTREE-CAPABLE reviewer when a task's criteria involve interaction with existing kernel behaviour, and implement or explicitly reject it, recording the reasoning and the rejected alternative in docs/specs/. Do not drop agy: on a diff it can actually inspect it is the only genuine engine independence available when codex is unavailable, and independence guards a failure mode depth cannot. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_review.sh && /bin/bash tests/inv/test_INV-14_engine_neutrality.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T14:19:34Z
created: 2026-08-08T23:20:21Z
updated: 2026-08-29T14:59:13Z
refused_envelopes: T012-a2-implementer.json T012-a2-implementer.2.json T012-a2-implementer.3.json
attempt_budget: 11
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T03:14:02Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

<!-- orchid:rework-brief candidate=4898f38da4d31e8e61e34b65acaa4bbcd3a0aff3 superseded -->
**Superseded rework brief (candidate `4898f38da4d3`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-24T21:52:22Z):** Blocked by the clock, not your code: verify passed with ZERO failures and the block is wallclock budget exceeded while in reviewing (lesson L042). The budget counts time spent waiting on REVIEWERS, which is not work you control, and recovering costs the review evidence because blocked to arbitrating is an illegal transition. Relevant context: two of the four engines had been permanently excluded from this run until yesterday, so review slots were queueing behind a single engine, which is very likely why the clock ran out. Rebase onto current integration is handled for you; before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.

**Operator guidance (2026-08-25T14:13:48Z):** Unblocking so the run can move. This task was blocked by the false-block surface this run has been documenting — a mid-run liveness flake (six sites, L020), a wallclock budget that counts time waiting on reviewers (L042), or an infra ladder charged for environment faults (L039). None of those is a judgement about your code. Separately, an engine was excluded for days because its envelopes were never drained rather than because it failed (F35), so review slots queued behind one engine and independence could not be proven. That is fixed: the supervisor now drains before every pass. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0; if nothing needs changing say so in your reply rather than answering with an empty round.

<!-- orchid:rework-brief candidate=51a49c6dd0ea785ab43d89d4eda496d49d2173ad superseded -->
**Superseded rework brief (candidate `51a49c6dd0ea`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-28T08:29:53Z):** Track 0 is merged through e8dd137b and the prior false blocks are repaired. A safe rebase was attempted and aborted on real PROTOCOL/docs architecture conflicts, leaving the branch clean and unchanged. Re-derive the worktree-capable review policy onto current integration and omit obsolete candidate-local Formula pins per T030.

**Operator guidance (2026-08-28T14:05:39Z):** Operator replayed the preserved review-depth candidate onto integration e8dd137, joined its fourth depth column with T039's attempt-pinned review plans, and added legacy three-column pin migration. Focused test_review, INV-14, and test_review_routing pass at 1ada3ec36ee3; Bash syntax, diff hygiene, and warning-level ShellCheck pass.

**Operator guidance (2026-08-29T01:32:24Z):** Grant two convergence rounds to bind review-depth attribution to the pinned round rather than a mutable live manifest, while keeping routing, slot credit, and malformed-pin handling fail-closed and consistent.

**Operator guidance (2026-08-29T02:02:52Z):** ledger item: a review plan that freezes only bare route name and depth still depends on the live plugin registry to map a filed envelope's qualified engine id; persist the qualified id in the attempt pin and use it for evidence attribution so uninstall or rebind cannot withdraw completed review depth. Current candidate evidence is ba01cf32463a.

**Operator guidance (2026-08-29T04:27:03Z):** Environment recovery, not a candidate judgment: host suspend killed four unrelated live jobs together. Your interrupted edits are preserved in operator-handoff commit 77cd9f8125c1; inspect that commit, complete the pinned qualified-engine-id migration and third-party uninstall/rebind coverage, then commit your final correction. Do not discard the preserved work.

**Operator guidance (2026-08-29T07:05:56Z):** Ledger item: repair the confirmed attempt-8 pinned-evidence defects. --adopt-evidence must use the selected current envelope's qualified engine id even when its short name equals the old row after a plugin rebind, and it must not re-derive a retained row's pinned depth from live manifests. Add focused regressions for same-short-name qualified-id rebinding, depth-manifest mutation, and the empty-engine validator case noted by review. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T07:31:57Z):** Ledger item: finish the attempt-9 evidence gate. drive_review_decision must never use live routing to prove depth: require review_plan_pinned for this attempt/current candidate, and return an actionable review-evidence boundary when the pin is missing, unreadable, empty, or candidate-stale. Keep review_plan's live fallback only for pre-dispatch planning callers. Add a regression deleting/rebinding the pin after reviews but before deterministic arbitration. Also close review_plan_row_valid's empty-fifth/sixth-column ambiguity so an extra field cannot masquerade as the qid. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T14:05:00Z):** Ledger item: infra failure #6 was an environment fault after host suspend/resume, not a candidate delivery failure: reviewer j-e55-T012-a10-3fa5 was quarantined gc-dead before the supervisor's post-resume heartbeat grace could run. Resume to rework using the surviving current, candidate-bound Claude review: when slot 1 is already worktree-capable, slot 2 must not widen its depth pass to the implementer's engine and unnecessarily discard available engine independence. Prefer the normal distinct independent route once the round already has depth; add a focused routing regression.
