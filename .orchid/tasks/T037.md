---
schema: 1
id: T037
title: Stop orchid run state leaking into a product repository's main
status: done
archetype: feature
scaffold: false
branch: task/T037
worktree: /Users/bilal/workspace/personal/orchid-orchid-T037
run_id:
depends_on:
attempts: 13
infra_failures: 4
session_id:
implementer_engine_id: claude
base_sha: cb37800c9fb18239e60d52e3f24778d8d4e5c529
candidate_sha: f5b18b5b9ea9fcaa7f848386ea946c3305a28224
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING (webBooks run) - ORCHID'S OWN RUN STATE SHIPPED INTO A PRODUCT REPOSITORY'S MAIN BRANCH. 14 .orchid/ files are tracked in webBooks main right now: roadmap, journal, blockers, plugins.lock, review envelopes. They rode the merge chain integration branch -> feature branch -> main, and passed review because the MR was large and the paths look like tooling. CONFIRMED STRUCTURAL IN THIS TREE, NOT A ONE-OFF MISTAKE: .orchid/ is tracked on orchid's OWN integration branch (257 files) and on main (234 files); only .orchid/runtime/ is gitignored. Committing run state is the DESIGN, so in any product repo that state rides the merge chain by default. THE REPORTER'S PROPOSED FIX IS NECESSARY BUT NOT SUFFICIENT, and this is the important part. The report proposes orchid staging with 'git add -f', because the operator had to un-ignore .orchid/ to unstick 'plan apply' (which stages with a plain git add and exits 1 on an ignored path) - the F21 fix - and un-ignoring is what made the files merge-able. 'git add -f' does solve THAT: it lets plan apply stage an ignored path without the operator changing .gitignore. But it does NOT solve the leak, because a force-added file is still TRACKED, and a tracked file still rides integration -> feature -> main exactly as these 14 did. Two separable problems, and both need answering: (a) plan apply must be able to stage run state whether or not the operator ignores it - 'git add -f' is the right shape here; (b) run state committed on the integration branch must not reach the product's main - by keeping it on a branch that never merges, by stripping it at the point the integration branch is merged out, or by an explicit guard that refuses or warns. NOTE THE EXISTING DELIBERATE STANCE, which the fix must not silently reverse: libexec/orchid-start:338 argues 'git add -f is deliberately NOT the answer' for orchid.config and fails with an actionable message instead, on the grounds that force-committing a file the operator excluded overrides their intent. Decide consciously whether run state differs from orchid.config here and write down why, rather than applying -f everywhere. Ship RED cases: plan apply must succeed with .orchid/ ignored; and a merge that would carry run state into a branch outside the run must be refused or loudly flagged. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) - do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only; the operator does perform those steps and the resulting commits WILL be in the candidate, trailered 'Orchid-Handoff: operator'. Do not report them as violations.
verification_commands: /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-30T15:38:18Z
created: 2026-08-10T21:47:21Z
updated: 2026-08-30T16:00:02Z
refused_envelopes: T037-a1-implementer.json T037-a1-implementer.2.json T037-a1-implementer.3.json
rework_rounds: 2
rework_signature: 835d7ae515bafcbae03adb2bfa3980e9ba9c76d1411d5f90ba238b802181f60e
rework_signature_repeats: 0
attempt_budget: 14
objection_seq: 4
unresolved_objection_by: 
unresolved_objection: 
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T03:14:04Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-28T14:27:20Z):** operator synchronized T037 onto Track 0; targeted start, merge, launch/pre-push, docs, syntax, diff, and ShellCheck coverage pass

**Operator guidance (2026-08-30T03:15:07Z):** Ledger item: unblock after environment-only implementer loss during the 2026-08-29 host sleep; job j-e55-T037-a1-44e8 died without an envelope and attempts were untouched. The worktree is clean at operator-handoff commit 32ad584b00d4e6d6256ee0610c957a9c096c9112 on task/T037, while candidate_sha still names the pre-rebase history dd9f4c410dc70cda1f9dcab48b31364472733080; preserve the rebased work and let the normal implement-envelope transition bind the current branch HEAD rather than discarding or hand-splicing state.

**Operator guidance (2026-08-30T10:09:01Z):** Ledger item: grant exactly two focused convergence rounds after attempt-2 arbitration for a reachable existing-repository pre-push hook upgrade and non-vacuous live-task/integration exemption fixtures. Preserve the approved leak containment behavior and use only T037's task-declared test_start and test_merge verification.

**Operator guidance (2026-08-30T10:50:54Z):** Ledger item: grant exactly two focused convergence rounds after attempt-3 arbitration. (1) Recognize an Orchid-owned pre-push hook only by the exact canonical anchored header/position needed to upgrade legacy hooks; a user hook that merely mentions the phrase orchid pre-push guard anywhere else must remain byte-for-byte untouched. (2) Apply run-state containment only to refs/heads/* after the existing managed-branch checks; tag and other non-branch ref pushes must retain normal Git behavior even when their commit carries .orchid. Add RED/GREEN fixtures for both twins. Preserve ignored-state staging, external-branch containment, existing-repository upgrade reachability, and only test_start.sh plus test_merge.sh verification.

**Operator guidance (2026-08-30T11:15:29Z):** Ledger item: grant exactly two focused convergence rounds after attempt-4 arbitration. (1) Resolve the effective pre-push hook path exactly as Git does, including absolute and relative core.hooksPath, shared/worktree layouts, spaces, and unset/default behavior; install, inspect, and report the same path Git will execute. Add a RED fixture proving a configured hooks path is protected and the unused .git/hooks path is untouched. (2) Add a supported explicit idempotent hook-refresh route for an already-initialized repository whose run is past planning; do not hide mutation inside a normal start invocation that otherwise refuses. The route must preserve operator-authored hooks and document the exact upgrade command. Preserve anchored ownership and branch-only containment; run only test_start.sh plus test_merge.sh.

**Operator guidance (2026-08-30T11:49:51Z):** Ledger item: grant exactly two focused convergence rounds after attempt-5 arbitration. Treat relative core.hooksPath according to Git's actual per-worktree rule. Do not claim repository-wide protection from one resolved path and do not rewrite the operator's config. Either (a) install/preserve the guard for every existing worktree and every future Orchid-created worktree with a doctor check for uncovered worktrees, or (b) if that lifecycle cannot be guaranteed in this bounded round, classify relative hooksPath as unsupported for the repository-wide guard: explicit refresh exits nonzero, init/start/doctor emit an actionable warning naming the per-worktree risk and absolute/unset recovery, and no output says installed/current. Default and absolute hooksPath retain normal behavior. Separately, a byte-current but non-executable owned hook must restore +x and report repair; chmod failure is not current/success. Add RED/GREEN twins and run only test_start.sh plus test_merge.sh.

**Operator guidance (2026-08-30T12:44:44Z):** Ledger item — THIS SUPERSEDES ALL EARLIER OPERATOR NOTES FOR THE NEXT ROUND. The stale-pack attempt 7 did not receive the current reviews. Do only these two items. (1) Treat Gerrit refs/for/<branch> and refs/for/refs/heads/<branch>, including push-option suffixes, as branch-bound review uploads in the run-state push leg and fail closed when the local tip carries run state; add a blocked upload and clean-upload twin. (2) Support an absolute core.hooksPath only when its resolved hook directory is contained by this repositorys Git common directory. For an external or shared absolute path, init/start/doctor warn actionably, explicit refresh exits nonzero, no hook is written or overwritten or called current, operator config is preserved, and recovery names a repo-contained absolute path or unset. Adjust the positive absolute fixture and add a two-repository shared-path RED case. Run only /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, then commit.

**Operator guidance (2026-08-30T13:38:38Z):** Ledger item — THIS SUPERSEDES ALL EARLIER OPERATOR NOTES FOR THE NEXT ROUND. Candidate c0df278 passed test_start and test_merge; its current attempt-8 reviews found exactly two connected containment gaps. (1) Make both containment guards history-aware. The local merge advisory and pre-push branch or Gerrit gate must detect durable .orchid state in any commit whose history is newly made reachable, even when a later commit deletes .orchid and the branch tip is clean. Use the appropriate remote or target baseline where available. Add non-vacuous add-then-delete RED cases for an ordinary branch push, a Gerrit upload, and the local outside-run branch advisory, plus truly clean-history GREEN twins. Preserve tag and note behavior, managed-branch refusal, all Gerrit ref spellings and suffixes, and the existing deliberate-remote exemption semantics. (2) Exempt the local merge advisory only when the target repository and ORCHID_ROOT resolve to the same physical checkout, which proves this invocation is self-hosted. Never infer this from the repository name or from .orchid already existing on a product branch. Add a real self-host GREEN case and an otherwise identical product-repository RED case. The forge-only discovery limitation is ledgered low and does not gate this round. Run only /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, then commit the bounded fix.

**Operator guidance (2026-08-30T14:13:44Z):** Ledger item — THIS SUPERSEDES EARLIER T037 NOTES FOR THE NEXT ROUND. Candidate 460ebc43 has one targeted verification blocker. Remove the all-remote --remotes=<name> history baseline. A deliberate ORCHID_ALLOW_PUSH=1 publication may exempt later pushes only to that same destination ref; it must not make contaminated history eligible for a different product ref merely because the remote already stores the objects elsewhere. For ordinary pushes, bind history comparison to the remote SHA of the exact destination ref. For refs/for/<target> Gerrit uploads, use only the appropriate target tracking ref when locally available and otherwise fail closed; never use unrelated remote refs. Add a non-vacuous RED/GREEN sequence: add then delete .orchid, override-push to scratch, fetch, then prove an unoverridden push of the identical clean-tip history to new main is refused while a later push to scratch remains allowed; retain clean-history and Gerrit twins. Preserve history-aware local-branch discovery and the physical self-host exemption. Run only /bin/bash tests/test_start.sh and /bin/bash tests/test_merge.sh, then commit the bounded fix.

**Operator guidance (2026-08-30T14:46:45Z):** Ledger item — THIS SUPERSEDES EARLIER T037 NOTES FOR THE NEXT ROUND. Candidate 94ae5a7712e9d49b108d60712d9740a9393d8a17 passed test_start and test_merge; both current attempt-10 reviews are bound to it. Do exactly two blocking corrections. (1) In the pre-push destination-ref history check, allow a cleanup-only suffix when the destination remote ref already carries .orchid and every newly reachable commit tree is clean; do not equate a deletion commit merely touching .orchid with a new leak. Still refuse add-then-delete histories whenever any newly reachable commit tree carries durable .orchid, including merge/Gerrit cases, and add a non-vacuous cleanup RED/GREEN regression. (2) Correct docs/specs/kernel.md so its normative baseline is only the exact destination ref's remote SHA, or the Gerrit target tracking ref when available; remove the obsolete plus-all-remote-tracking-refs claim contradicted by the scratch-to-main RED case. Preserve all other containment, hook lifecycle, ignored-state staging, and physical self-host behavior. Run only /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, then commit.

**Operator guidance (2026-08-30T15:10:17Z):** Ledger item — THIS SUPERSEDES EARLIER T037 NOTES FOR THE OPERATOR CONVERGENCE ROUND. Candidate 26b6deb41951be15c7bfd84550233cdda8b15e3e passed test_start and test_merge. Make exactly two bounded corrections. (1) Remove the path-limited candidate prefilter from history_carries_run_state: inspect every commit newly reachable from the exact destination baseline and refuse if any such commit tree carries durable .orchid, including an ordinary no-.orchid-change commit that inherits it before a later deletion. Preserve the immediate deletion-only suffix pass and all existing add-then-delete, merge, Gerrit, destination-binding, tag/note, and managed-branch behavior. Add a non-vacuous inherited-state-then-delete RED twin beside the cleanup GREEN. (2) Correct docs/troubleshooting.md so it does not claim the local merge warning fires for Orchid's physical self-host checkout, which is deliberately exempt. Operator will implement, run targeted RED/GREEN plus /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, and commit with Orchid-Handoff: operator.

**Operator guidance (2026-08-30T15:23:52Z):** Ledger item — THIS SUPERSEDES EARLIER T037 NOTES FOR THE FINAL OPERATOR OPTIMIZATION. Candidate 1f5d693a54ca6cf9211486b5329c2dcad2bb7d02 passed targeted cleanup/inherited/recontamination checks and test_start plus test_merge; both current attempt-12 reviews confirm the a11 semantic objection is fixed. Preserve the exact all-newly-reachable-tree semantics, but replace the per-commit git ls-tree fork loop with a bounded-process batch query (one git rev-list producing <tree>:.orchid specs and one git cat-file --batch-check consumer). Treat every resolved object type as carried run state and only explicit missing results as absent; consume the whole stream without grep -q/SIGPIPE ambiguity. Add a non-vacuous clean new-branch fixture that counts git ls-tree invocations and proves the hook uses only the single tip check, not one fork per historical commit. Run focused three-way semantics plus /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, and commit with Orchid-Handoff: operator.

**Operator guidance (2026-08-30T15:37:05Z):** Ledger item — THIS SUPERSEDES EARLIER T037 NOTES FOR THE FINAL PORTABILITY HAND-OFF. Candidate c7e1fb445d4f594b2da4e3fc5defefcac8c0b00a passed focused semantics, the ls-tree fork-count regression, and test_start plus test_merge; both attempt-13 reviews confirm the a12 performance objection is fixed. Remove the Git-2.33-only --no-commit-header dependency without restoring per-commit forks: use portable rev-list --format output with the same single cat-file batch, treat rev-list's ordinary commit-header probes as explicit missing results and the following <tree>:.orchid result as authoritative, preserve fail-closed command failure and all semantic twins. Extend the PATH shim to reject --no-commit-header so the clean new-branch batch test is a non-vacuous older-Git compatibility RED case. Run focused semantics plus /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh, then commit with Orchid-Handoff: operator.
