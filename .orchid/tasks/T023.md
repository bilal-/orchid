---
schema: 1
id: T023
title: Give a worktree a prepare step and a portable repo-root path
status: done
archetype: feature
scaffold: false
branch: task/T023
worktree: /Users/bilal/workspace/personal/orchid-orchid-T023
run_id:
depends_on: T013
attempts: 9
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 2658972632012c3332a34e17dce9fb295d7bf5ff
candidate_sha: 845c5803bfe3adfbbb1ee521c418ad8c24854e4c
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T023 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDINGS F24 and F25 (wasiyyat-schedule-c), and webBooks hit the same class independently (its L003). git worktree add materializes only TRACKED files, so any repo whose suite needs gitignored paths fails verification for reasons unrelated to the task: composer vendor/ (Could not open input file: vendor/bin/phpunit), two multi-GB data directories (8 failures across two integration classes), tests/E2E/node_modules (would have broken all 65 Playwright specs). The first task failed verify on this alone though its code was already correct — 2136 tests green once vendor/ existed — and without diagnosis it would have failed all 9 tasks identically and looked like nine bad implementations. The available workaround, folding a bootstrap into verify=, conflates two concerns: verify runs every pass so one-time setup is re-checked constantly, and the bootstrap must be idempotent, guarded, and inlined into the single config line whose failure otherwise means 'the task's code is wrong'. ADD a separate prepare= command run once per worktree after creation and before first verify, whose failure counts as infra_failures rather than attempts. F25: it must ALSO cover the merge worktree — merge validation runs verification_commands in a temp worktree under TMPDIR, where a sibling-relative bootstrap resolves to nothing; that task was approved unanimously twice and then failed merge validation twice for a reason invisible in any review. EXPORT the main repo path (e.g. ORCHID_REPO_ROOT) into the prepare/verify environment so a bootstrap can be written portably instead of hardcoding an absolute path into committed config. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === UNRESOLVED OPERATOR OBJECTION, RAISED 2026-08-10T21:20Z, WHICH MUST BE EXPLICITLY MET BEFORE THIS TASK MAY BE APPROVED BY ANYONE. The prepare command must not inherit the driver's stdin. Confirmed in code: lib/common.sh:518 runs 'bash -c "$cmd"' with no redirection while runners/orchid-drive walks its work through herestring-fed loops (448/459, 850/867, 993/1011), so a prepare command that reads stdin eats the remaining lines of the list being iterated and the walk ends early with no error. Redirect stdin from /dev/null for the prepare child, and do the same at libexec/orchid-verify:40, whose comment already claims the two run alike. Ship a RED case: a prepare command that reads stdin must NOT truncate the driver's walk. TO ANY REVIEWER: do not return approve until that redirection exists and is covered by a test; if it is missing, return request-changes with a structured finding at medium or above, because a prose-only summary with an empty findings[] does not reach the severity gate.
verification_commands: /bin/bash tests/test_start.sh && /bin/bash tests/test_merge.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T18:09:06Z
created: 2026-08-10T01:50:56Z
updated: 2026-08-29T18:34:07Z
refused_envelopes: T023-a5-implementer.4.json T023-a5-implementer.5.json T023-a5-implementer.6.json
attempt_budget: 11
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T05:12:37Z):** auto-blocked at the cap by the SAME documentation gate that cost it an attempt earlier: scripts/ci-local.sh extracts every 'orchid <word>' from PROTOCOL.md and demands a matching libexec verb, and PROTOCOL.md:799 contains the English phrase 'no orchid verb ever runs a verification suite there'. It is prose about verbs, read as a verb named 'verb'. This is the FIFTH instance of a gate charging a task for text that merely resembles syntax — after 'orchid creates' (this same task), 'git worktree add' inside a diagnostic string (T019), backticks in an annotation (T017) and a bare 'done' as an argument (T006) — and the second time this specific task has been stranded by this specific gate. Unblocked with the exact line and a reword, and explicitly told NOT to edit the gate: that repair belongs to T015, which already carries it with a required RED case.

**Operator guidance (2026-08-11T17:21:55Z):** eight genuine failures, correctly left for the operator by the loop's auto-recovery gate. All eight are the single gap briefed last round: ORCHID_REPO_ROOT is exported to the prepare child only, not to orchid-verify's invocation nor to merge's revalidation in the temp worktree. The tests for it are written and the export is not there. The temp-worktree case is the one that matters most - a TMPDIR checkout has no relative path back to the repository, so a suite needing the dispatching repo can only find it through that variable. Unblocked with both call sites named.

**Operator guidance (2026-08-12T00:05:22Z):** STRANDED BY THE OPERATOR'S STALE PIN PLUS THE SAME FLAKY TIMING ASSERTION PROVEN FLAKY ON T021 NINETY MINUTES AGO. Its four non-pin failures are: the stale Formula/orchid.rb checksum (the operator's hand-off, now re-pinned and rebound), and two instances of 'streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)'. The operator re-ran tests/test_engine_agy.sh against this same candidate: rc=0, zero failures — the identical assertion the operator re-ran twice for T021 with the identical result. It depends on a stub adapter flushing to its log by a wall-clock midpoint and fails under machine load, which is lesson L020's scheduling-dependent class. NOTHING IN THIS CANDIDATE IS IMPLICATED. That makes three strandings this run caused by this single flaky assertion (T021 once, T023 twice within one log), on top of six from the operator's pin race and six from textual gates. RECORDED FOR T038: the run's attempt budget has now been consumed more often by the harness and the operator than by implementer error, and this assertion in particular should either be made load-independent or excluded from the budget, because it will keep doing this to whichever task happens to run while the machine is busy.

<!-- orchid:rework-brief candidate=3562a10b080ef0e75fc899b8916c99f8aa1e00b3 superseded -->
**Superseded rework brief (candidate `3562a10b080e`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T05:12:01Z):** two real failures, one rule: its new fixtures use plain 'cd' into a scratch root where the suite requires cd_scratch (the L014 helper that refuses an unregistered directory). The other log entries are the operator's stale pin and the known flaky streaming-liveness assertion (L020), re-run to zero failures three times tonight. Unblocked with the rule named and an instruction to sweep the diff for other bare cd calls.

**Operator guidance (2026-08-18T03:14:02Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-25T14:13:49Z):** Unblocking so the run can move. This task was blocked by the false-block surface this run has been documenting — a mid-run liveness flake (six sites, L020), a wallclock budget that counts time waiting on reviewers (L042), or an infra ladder charged for environment faults (L039). None of those is a judgement about your code. Separately, an engine was excluded for days because its envelopes were never drained rather than because it failed (F35), so review slots queued behind one engine and independence could not be proven. That is fixed: the supervisor now drains before every pass. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0; if nothing needs changing say so in your reply rather than answering with an empty round.

<!-- orchid:rework-brief candidate=e495458dc1ac531cc7c8189365128d3db020aa9a superseded -->
**Superseded rework brief (candidate `e495458dc1ac`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-28T13:42:38Z):** Operator rebased the preserved scratch-worktree candidate onto integration e8dd137, joined the T030 merge regressions without test-id collisions, and retained the current launch-failure behavior. Focused test_start and test_merge pass at 470ae772e1c4; Bash syntax, diff hygiene, and warning-level ShellCheck pass.

**Operator guidance (2026-08-29T04:47:52Z):** ledger item: clean targeted verification on candidate b9bd4e21e9ae reproduced the candidate failure without the operator's ORCHID_INFRA_MAX override. tests/test_merge.sh leaves T004 and T108 active in merging; the later walk_to_merging calls for T109, T110, and T011 are therefore refused at concurrency-cap 2/2, cascading into ten assertions. Release those fixture slots through legal task transitions, or reorder the terminal active cases, then run test_start.sh and test_merge.sh.

**Operator guidance (2026-08-29T14:19:23Z):** Ledger item: grant two convergence rounds to update the now-landed worktree_prepare specification and prove the existing test_docs tripwire passes, in addition to T023's targeted start/merge tests.

**Operator guidance (2026-08-29T15:38:39Z):** ledger item: grant two focused convergence rounds for the current-review base_sha stamping race; attempt 9 produced substantive reviewer feedback.

**Operator guidance (2026-08-29T18:08:39Z):** Ledger item: this guidance is deliberately delivered through task retry because the previous waived arbitration reason did not enter the implementer capsule. Fix the duplicate merge_status blocked) arms now: one reachable arm must distinguish durable merge_gate failure evidence from worktree_prepare infra exhaustion; the former raises operator-decision and names the existing merge log, the latter raises blocked-task with unblock|retry|reverify and must not name a merge log. Replace/augment the textual source-count guard with executable fixtures that drive both causes to blocked and assert the correct boundary kind/reason. Preserve the approved base-stamp, stdin, ORCHID_REPO_ROOT, and prepare behavior. Run test_drive plus targeted start/merge tests; no Formula pin under T030.
