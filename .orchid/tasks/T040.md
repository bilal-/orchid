---
schema: 1
id: T040
title: Never discard work a job already completed
status: done
archetype: feature
scaffold: false
branch: task/T040
worktree: /Users/bilal/workspace/personal/orchid-orchid-T040
run_id:
# depends_on: comma- or whitespace-separated ids of tasks that must reach
# `done` before this one dispatches (e.g. `T001,T002`); each must already exist
depends_on:
attempts: 3
infra_failures: 10
session_id:
implementer_engine_id:
base_sha: 2a6a6c8eba296eaf4fd1af4618c5e503d2657950
candidate_sha: 83c03ed1bfa86bb701c9723709a4176a02bd632f
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F35 (wasiyyat general-forms, 2026-08-11), reported as CRITICAL and the worst finding of that run, because ORCHID THREW AWAY WORK IT HAD ALREADY DONE. Critique attempt a4 ran to completion, produced EIGHT complete findings, wrote every one of them to its job log, and then exited WITHOUT writing an envelope. 'orchid jobs reconcile' had nothing to land, so from the outside the attempt simply never happened. The findings sat in .orchid/runtime/logs/j-e0-plan-a4-b068.log the whole time and the operator recovered them with grep and applied them by hand; without that they were lost and an expensive critique would have been re-run to regenerate findings orchid already had on disk. A SECOND ATTEMPT DIED DIFFERENTLY AND JUST AS SILENTLY: a3 kept sending heartbeats while CPU stayed flat - 0.85s at 05:18:12Z and 1.07s at 05:23:42Z, roughly 0.2 seconds of CPU across five minutes of wallclock - then exited with no envelope. 'jobs check' has a stalled status and it never fired, because the process was ALIVE: liveness is checked, progress is not. TWO OF FOUR ATTEMPTS PRODUCED NOTHING REACHABLE THROUGH THE VERBS. REQUIRED: (1) SALVAGE PARTIAL OUTPUT - if a job exits without an envelope but its log holds parseable results, reconcile must land them as a DEGRADED envelope rather than discarding them; the engine already paid for that work, and a degraded envelope an operator can see beats a silent loss. (2) Treat 'exited, no envelope' as a first-class failure with its own status, never as a job that silently never happened. (3) Make 'jobs check' use a CPU DELTA, not just liveness, to decide stalled - a process burning 0.2s of CPU in five minutes is not working, and its heartbeats are lying. (4) Journal the exit code and the last N log lines whenever an envelope is missing, so an operator sees something without knowing to grep runtime logs. Ship RED cases for each: a job that logs results and exits without an envelope must yield a degraded envelope and a visible failure, and a heartbeating process with no CPU progress must be marked stalled. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) - do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only; the operator does perform those steps and the resulting commits WILL be in the candidate, trailered 'Orchid-Handoff: operator'. Do not report them as violations. === THIS TASK NOW ALSO OWNS THE DEFECT THAT STOPPED THE RUN (lesson L039), AND IT IS THE SAME PRINCIPLE ONE LAYER DOWN. Your existing scope is that work a job already completed must not be discarded. On 2026-08-12 the driver began discarding the FACT that work was completed, and within four hours twenty-six of this runs forty tasks were blocked, ten of them without ever spending an attempt. The run has not advanced since. THE DEFECT is in lib/drive.sh drive_delivery_is_noop and drive_delivery_floor, merged with T022. An ok implement envelope whose worktree HEAD still equals the delivery floor is judged to have delivered nothing, where the floor is candidate_sha when the task has one and base_sha otherwise. The stated premise is that the round was dispatched precisely to change that candidate, so a HEAD still sitting on it delivered nothing. That premise assumes every re-dispatch requires a change, and it cannot tell a round that produced NO CANDIDATE from a round that correctly produced NO NEW COMMIT on top of a candidate that already exists and is already complete. THE EVIDENCE IT IS A FALSE POSITIVE: every blocked worktree is CLEAN and sits three to five commits AHEAD of the integration branch, so a candidate demonstrably exists; HEAD equals candidate_sha but differs from base_sha, and the rule compares only against the floor, so it never asks the question that separates the two cases. The T040 implementer log states the honest case exactly: I found nothing to change and made no edits, manufacturing a diff to look productive would be worse than reporting the work as delivered. That answer is currently charged as an infra failure, the implementer is relaunched, it truthfully repeats itself, and at infra_max the task is blocked. THE TRIGGER IS A REBASE, which is why this is reachable in ordinary operation rather than only by a lazy engine: rebasing rewrites a tasks commits, the driver re-stamps the new HEAD as candidate_sha, the next round dispatches, and the implementer finds its own already-delivered work in place. Every run with concurrency above one rebases in-flight tasks onto a moved integration branch, so every such run walks into this. WHAT TO DELIVER. Separate the two cases at the comparison. HEAD equal to base_sha means nothing was ever produced and T022 refusal is correct and must survive unchanged. HEAD equal to candidate_sha but AHEAD of base_sha means a candidate exists and this round added nothing to it: that is a routing question, not a delivery failure, and it must not spend the infra ladder. Advance it or raise a boundary, and say in the journal which and why. DO NOT weaken T022 protection to achieve this: an engine returning ok with pure commentary over a worktree that never moved must still be refused, and there is a test Part in tests/test_drive.sh that proves it which must keep passing. PIN BOTH EDGES, per lesson L034, which this defect is a textbook case of: T022 shipped one RED case pinning the too-permissive edge and left the too-strict edge unpinned, and the unpinned edge cost the run twenty-six tasks in four hours. Ship a RED case for a genuine no-op that must still be refused AND a RED case for an honest ok over an already-complete candidate that must NOT be charged. OPERATOR NOTE ON YOUR OWN BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3; the operator has raised infra_max to 6 in the run config to give you room, and will restore it to 3 once this lands. Your attempts field is untouched at 0.
verification_commands: /bin/bash tests/test_jobs.sh && /bin/bash tests/test_launch.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-18T02:24:38Z
created: 2026-08-11T18:40:36Z
updated: 2026-08-18T18:01:53Z
refused_envelopes: T040-a1-implementer.json T040-a1-implementer.2.json T040-a1-implementer.3.json T040-a1-implementer.4.json T040-a2-implementer.json T040-a2-implementer.2.json T040-a3-implementer.json T040-a3-implementer.2.json T040-a3-implementer.3.json T040-a4-implementer.json
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-13T16:32:00Z):** UNBLOCKING YOU TO FIX THE DEFECT THAT BLOCKED THE RUN. You were not at fault and neither was your code: you reported honestly that the work was already delivered and that manufacturing a diff to look productive would be worse, and the driver charged that answer as an infra failure three times and blocked you. Twenty-six tasks are blocked the same way and the run has not advanced in nineteen hours.

Your acceptance criteria now carry the full diagnosis (lesson L039) and it is the same principle you already own, one layer down: work a job already completed must not be discarded, and the driver is currently discarding the FACT that it was completed. Read the criteria before starting; the essential change is that HEAD equal to base_sha (nothing ever produced) and HEAD equal to candidate_sha but ahead of base_sha (a candidate exists, this round added nothing) are different situations and only the first is a delivery failure.

Two things to hold on to. Do not weaken T022 protection to get there — an engine returning ok with pure commentary over a worktree that never moved must still be refused, and the test Part proving it must keep passing. And pin BOTH edges with RED cases; this defect exists precisely because only one edge was pinned, which is lesson L034 and it has now cost a whole run.

You will need to COMMIT your change. That is not a formality here: an uncommitted fix leaves HEAD on candidate_sha, which is the very condition that blocked you, so a round that delivers nothing cannot get this fix out. Your attempts field is 0 and untouched; infra_failures resumes at 3 because it is kernel-owned and never reset, so the operator has raised infra_max to 6 in the run config to give you room and will restore it to 3 once this lands.

**Operator guidance (2026-08-14T01:16:04Z):** RECOVERING A COMPLETE CANDIDATE THAT THE DEFECT IT FIXES HAS TRAPPED. You committed the fix (1a4fadc, distinguish an already-delivered candidate from a no-op delivery) and the F35 salvage work (c0d43c9). Your worktree is four commits ahead of integration, clean, and candidate_sha ebc1530 matches HEAD. Nothing is missing. You were re-blocked at infra_failures 4 by the very rule this candidate corrects: a later round found the work already done, reported ok honestly, HEAD had not moved, and the driver charged that as a failed delivery. The operator is advancing this candidate to verification directly rather than asking you for another round you correctly have nothing to put in.

<!-- orchid:rework-brief candidate=ebc153031e6ffdf5ba8d2d9424d9d7e363b6ba4e superseded -->
**Superseded rework brief (candidate `ebc153031e6f`) — 30 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-14T16:47:25Z):** ONE ASSERTION, AND YOUR PRODUCT CODE IS RIGHT. I ran your candidate 1f2f3f6 directly. ShellCheck at severity=warning is clean across lib/drive.sh, runners/orchid-drive, runners/orchid-launch and libexec/orchid-task. tests/test_drive.sh has exactly ONE failure and it is in the test harness, not in the behaviour.

THE FAILURE: assert_match "a candidate exists and this round added nothing to it" against DDRIVE_OUT, labelled "the pass names the case it routed rather than reporting a delivery it did not see" at tests/test_drive.sh:2949. The very next assertion, the same string against the journal at :2952, PASSES — which is the clue that tells you where the fault is.

THE CAUSE: run_ddrive at :2905 does DDRIVE_OUT="$(...)" — it REASSIGNS on every pass. The loop below it runs passes until attempts reaches 2, and the unchanged note is emitted on an earlier pass than the last one, so by the time the assertion runs DDRIVE_OUT holds only the final pass output and the note is gone from it. The journal assertion passes precisely because a journal ACCUMULATES and that variable does not. Your product code emitted the note correctly on the pass that routed the case.

THE FIX IS IN THE TEST: accumulate the drive output across passes and assert against the accumulation, keeping the per-pass variable for the checks that genuinely mean "the last pass". Do not weaken the assertion to make it pass, and do not move it to the journal — the point it is making is that the PASS says which case it routed, and that point is worth keeping. Change nothing in runners/orchid-drive or lib/drive.sh; I have read the unchanged arm and it is correct, including that it charges no rung, does not mark the envelope refused, and leaves bounding to the attempt budget.

YOU MUST COMMIT THE CHANGE. Your infra_failures is at 5 against a cap of 3, so a round that commits nothing re-blocks you immediately — the trap this task exists to remove, which has now caught this task three times. There is real work here, so committing it clears you.

Everything else in the candidate stands: the delivery verdict split, the F35 salvage work, and the new cases in test_jobs.sh and test_launch.sh.

**Operator guidance (2026-08-15T04:08:22Z):** CANDIDATE IS GREEN ON THE CHECKS THAT FAILED. I ran f471912 directly: ShellCheck clean across lib/drive.sh, runners/orchid-drive, runners/orchid-launch and libexec/orchid-task, and tests/test_drive.sh exits 0 with zero failures. Your accumulation fix (9df7178) is correct and the assertion it repairs now holds without being weakened. Nothing further is being asked of you.

**Operator guidance (2026-08-16T02:40:33Z):** THIS SUPERSEDES EVERY EARLIER OPERATOR NOTE ON THIS TASK. If you find a previous message from me saying the candidate is green and that nothing further is being asked of you, it is STALE — I wrote it on 2026-08-15 before the review round, and the review round found three real defects. That earlier note is what the last implementer read, which is why it filed "candidate complete, no changes needed", committed nothing, and left this task blocked and the whole run halted. Do not repeat that. There is work to do and it is listed below.

WHAT IS ALREADY ACCEPTED AND MUST NOT CHANGE: the delivery-verdict split in lib/drive.sh (1a4fadc), the F35 salvage work, and the harness accumulation fix (9df7178). I verified those myself — ShellCheck clean, tests/test_drive.sh zero failures. Leave lib/drive.sh and the drive_delivery functions alone.

THE THREE CHANGES REQUIRED, all in the job-lifecycle half:

1. BLOCKING — libexec/orchid-jobs, hb_cpu_flat kills healthy jobs. It ends in "if (CPU[seen] - CPU[anchor] >= floor + 0) exit 1", so a NEGATIVE delta fails that test, falls through, returns stalled, and the caller at line 432 answers stalled with kill_stuck. A CPU counter goes backwards on pid reuse, which is ordinary. Guard the negative case explicitly and treat it as UNKNOWN, never as no-progress. Unknown must not kill.

2. BLOCKING — the signal itself. F35, the report this task is built from, RETRACTED the CPU signal: a legitimate implement job sat at about 9 seconds of CPU across 40 minutes, looked identical to a dead one, was working the whole time, and had 24 modified files in its worktree to prove it. Its conclusion was that CPU is a bad progress signal for an agent whose wallclock is dominated by waiting on API responses, that git status in the worktree separates the two cases cleanly, and that CPU is corroboration at best. Either corroborate a flat-CPU verdict with filesystem progress in the task worktree before killing, or default cpu_stall_min_s to 0 so the arm is opt-in. State which you chose and why.

3. BLOCKING — CPU-stall and timeout kills terminate the launcher wrapper before it records status, so reconciliation journals "Exit code: unrecorded" instead of the mandated exit code. That is the diagnostic this task exists to preserve, missing in exactly the case it was added for. Record the status before or despite the kill.

CARRIED, fix only if small: the salvage pass skips gc suspect-field validation when tailing the manifest log path and building the reviews write path.

YOU MUST COMMIT. infra_failures is at 7 against a cap of 3, so a round that commits nothing blocks this task instantly and the run — twenty-six other tasks — stays halted. That has now happened five times. If you genuinely believe a listed item needs no change, make the other changes, COMMIT, and say so in your reply; do not answer with an empty round.

**Operator guidance (2026-08-16T14:13:50Z):** SUPERSEDES ALL EARLIER NOTES.

DO THIS:
1. Run: bash tests/test_jobs.sh
   It has 15 failures. Fix them.
2. Before you commit, run all three and confirm each exits 0:
   bash tests/test_jobs.sh
   bash tests/test_launch.sh
   bash tests/test_drive.sh
3. Commit. A round that commits nothing blocks this task and keeps 26 others halted.

THE FAULT: your exit-status recorder (26076bc) broke the salvage reader. Every one of the 15 failures is the same symptom -- salvage produces an empty envelope: status expected no_envelope got empty, degraded expected true got empty, exit code expected 7 got empty, findings not salvaged, nothing journaled. The recorder and the salvage reader disagree about where or when the status is written. They passed together before 26076bc, so compare against 9df7178 to see what the contract was.

MY ERROR, NOT YOURS, ON ONE POINT: I told you to "record the status before or despite the kill" and gave you a choice about the CPU signal. I should have told you to re-run tests/test_jobs.sh after touching the recorder, because the salvage path reads what it writes. That instruction is now item 2 above and it is not optional.

ALREADY ACCEPTED, DO NOT TOUCH: lib/drive.sh delivery-verdict split, the harness accumulation fix, the negative-delta guard, cpu_stall_min_s defaulting to 0. I verified those: ShellCheck clean, test_drive.sh and test_launch.sh both exit 0.

**Operator guidance (2026-08-16T14:32:35Z):** Candidate verified green by the operator at 83c03ed: ShellCheck clean, and tests/test_drive.sh, tests/test_launch.sh and tests/test_jobs.sh each exit 0 with zero failures. The 15 salvage failures are fixed (eacdace). Nothing further is asked of you on this candidate.

**Operator guidance (2026-08-18T02:24:38Z):** Operator is advancing this candidate to verification and merge. No implementer round is being requested and none should run; the operator moves the task through implementing directly.
