---
schema: 1
id: T008
title: Stop a capability refusal counting as an engine fault
status: done
archetype: feature
scaffold: false
branch: task/T008
worktree: /Users/bilal/workspace/personal/orchid-orchid-T008
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 1
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 58d8f8dac7fed6e0510a2039ce448e4a78abfa35
candidate_sha: 3cee1b0e513c8d3ab833a742239f31587eba2a6a
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T008 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: lib/ledger.sh's ledger_mark increments consecutive_failures for any non-ok envelope status and flips the engine to failing at engine_fail_threshold, with no distinction for a CAPABILITY REFUSAL. When agy declines a diff over agy_max_bytes it is doing exactly what plugins/engines/agy/run is designed to do and says so clearly, yet it counts toward disqualification — so an engine that reviews small diffs perfectly would eventually be skipped for everything, including the work it is good at. Observed live in r-001: agy refused a 100777-byte diff by 777 bytes and the ledger recorded consecutive_failures 1 with last_status failed. Distinguish a capability refusal from an engine fault, end to end: the adapter must be able to say which it is, and the ledger must not count the former. Add a focused test that a capability refusal leaves consecutive_failures unchanged while a genuine fault increments it. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. The focused test must assert BOTH fields, not just the counter: a capability refusal must leave consecutive_failures unchanged AND must not record last_status as failed, since resolve_role_available and the status ledger both read that field and a refusal mislabelled as a fault still misleads an operator reading orchid status. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === PROVEN LIVE ON r-002, WITH THE FULL CASCADE MEASURED - use this as the concrete case. agy's three 'failures' were not failures. Its job log reads: 'orchid/agy: diff.patch is 101108 bytes (> agy_max_bytes=100000); route to a worktree-capable reviewer'. That is a CAPABILITY REFUSAL, correctly reported, naming the limit, the actual size and the remedy - and the diff exceeded the limit by 1.1 percent. The ledger recorded it as an engine fault three times and set agy to status failing. THE CASCADE THAT FOLLOWED, every step observed: (1) agy marked failing, so the reviewer pool for the whole run dropped to one healthy session-independent engine; (2) 'orchid jobs review-plan' is computed live from engine health, so slot 1 of T024's medium tier was reassigned from agy to codex-review AFTER agy had already filed a valid, candidate-bound review; (3) that review stopped counting for the slot it was dispatched for, and the driver refused to reconcile with 'slot(s) 1 have no review of their own - the tier's engine independence is unproven'; (4) T024 was left in reviewing with NO legal exit - reviewing -> arbitrating is the only forward edge and it is gated, there is no reviewing -> rework edge, and 'orchid task arbitrate' refuses because the task is not arbitrating. A correct refusal by a well-behaved engine stranded a task and degraded the review independence of every medium and high task in the run. So this defect is not cosmetic ledger hygiene: it silently reduces how many independent engines a run believes it has, and the damage surfaces somewhere else entirely, as a boundary that blames the task. REQUIRED: a capability refusal must be distinguishable from an engine fault at the point the ledger is written, must not increment consecutive_failures, and must be visible as a refusal in the run record (dogfood finding F12 already noted that capability fallback is silent). The sibling task covering the stranded-plan half is tracked separately; fix this half at the ledger.
verification_commands: /bin/bash tests/test_ledger.sh && /bin/bash tests/test_failover.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-20T12:17:56Z
created: 2026-08-08T23:20:20Z
updated: 2026-08-20T14:44:20Z
refused_envelopes: T008-a2-implementer.json T008-a2-implementer.2.json T008-a2-implementer.3.json
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

<!-- orchid:rework-brief candidate=605571203f40dc73cd3be1e04d4c6f0ee886d848 superseded -->
**Superseded rework brief (candidate `605571203f40`) — 19 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-18T03:14:01Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.
