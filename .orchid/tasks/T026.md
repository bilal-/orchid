---
schema: 1
id: T026
title: Give the operator a supported reverify and a real retry budget
status: done
archetype: feature
scaffold: false
branch: task/T026
worktree: /Users/bilal/workspace/personal/orchid-orchid-T026
run_id:
depends_on: T013
attempts: 7
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 1b781c3af66f6a2d1b4a1ea626b32acba5a8ee7d
candidate_sha: 34692d639bbb81125828ee61586d91a450c1d5d0
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: SUPERSEDES ALL EARLIER OPERATOR NOTES. Re-derive T026 from current integration 1b781c3. Prior work is preserved at r002-preserve/T026 for reference only. Implement the full acceptance criteria and the verified rejection fixes: testing_entry_blocker must fail closed when git log cannot answer; lib/handoff.sh must state that reverify withdraws the handoff acknowledgement. Run bash tests/test_task.sh, bash tests/test_drive.sh, and ShellCheck on every edited shell file before committing. Do not re-pin Formula/orchid.rb or change executable modes; those are operator handoffs.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F28 (wasiyyat-schedule-c), and this operator hit the identical wall on r-002's T013 and T014. 'orchid task retry' restores STATUS but grants no attempt budget: after a precise operator diagnosis naming file, lines, exact substitution and an explicit anti-instruction, the task moved blocked to rework to implementing, produced a byte-identical fourth failure, and immediately re-blocked with attempts exhausted. TWO DISTINCT PROBLEMS. (1) You cannot tell whether --reason reaches the implementer at all: the diagnosis was actionable and the next attempt repeated the mistake, so either the reason is journal-only or it was delivered and ignored — indistinguishable from outside, which is itself the defect. Make reason delivery visible in task show or in retry's own output. (2) There is no supported 'the operator fixed it, just verify' path. That report's operator ended up editing the test in the task worktree and committing on the task branch by hand, then retrying with 'the tree is already green' — outside the verbs, with no name in the protocol. This operator did exactly the same thing twice tonight. ADD a first-class reverify edge meaning 'the tree is green, re-run verification without another implementation pass', and let retry grant an attempt or take --attempts N. In that run four engine invocations went to a two-word test change the operator had diagnosed correctly after the first failure. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. === PROVEN ON THIS RUN, use it as the concrete case: T013 exhausted its budget without a single genuine implementation failure. Two of its three attempts were spent on the formula-pin merge deadlock (lesson L022) and on a no-op rebase that invalidated a passing verify and forced an identical re-run — environment and operator causes, exactly what T019 says must not be charged. Recovery was then IMPOSSIBLE THROUGH ANY VERB: the budget is a hardcoded 'attempts >= 3' in runners/orchid-drive:690 with no configuration, and 'attempts' is in libexec/orchid-task's kernel-owned deny-list so 'task set' refuses it. 'task unblock' and 'task retry' both move blocked/rework -> rework WITHOUT resetting attempts, so the task re-blocks on its very next verify failure. The operator's only remaining move is to hand-edit frontmatter, which this run's own constraints forbid. Give the operator a supported way to grant a task a fresh budget with a recorded reason (journal-first, like infra-fail), and make the cap configurable rather than a literal in the driver. ALSO make reverify supported: after a spurious failure the operator's only way to re-run verification is to push the task backwards through implementing, which discards the candidate's evidence and costs another attempt. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_task.sh && /bin/bash tests/test_drive.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-25T22:39:20Z
created: 2026-08-10T01:50:57Z
updated: 2026-08-26T00:16:14Z
refused_envelopes: T026-a7-implementer.json T026-a7-implementer.2.json T026-a7-implementer.3.json
handoff_ack: 34692d639bbb81125828ee61586d91a450c1d5d0
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T02:42:33Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run whatever covers what you changed and confirm each exits 0 — at minimum the tests/ files for the areas you touched, plus: shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure and blocks this task again.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery, charging the job-delivery ladder. It caught twenty-six tasks including this one, every one of them at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget is untouched by it. The fix is in flight under T040.

BUDGET, PLAINLY: infra_failures is kernel-owned and never reset, so you resume at the cap. One further delivery failure re-blocks this task. That is why step 3 matters — if you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply. Do not answer with an empty round.
