---
schema: 1
id: T009
title: Give hermes an inbound probe and keep doctor honest about it
status: pending
archetype: feature
scaffold: false
branch: task/T009
worktree:
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id:
base_sha:
candidate_sha:
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: claude
effort: medium
acceptance_criteria: T006 added a manifest-declared inbound_probe so a notify plugin that can report its own return-leg liveness does, and plugins/notify/openclaw/plugin.conf declares one. plugins/notify/hermes/plugin.conf does NOT, even though 'hermes gateway status' reports exactly that fact and hermes is the channel r-001 actually delivered on — and the channel whose gateway being down silently swallowed a real operator answer (lesson L011). Give the hermes notify plugin an inbound probe backed by its own CLI, declared the same way openclaw's is. Keep doctor's honesty rules intact: inbound ok only from a plugin's own positive probe, 'cannot determine' reported as such and never as health, and the whole notify block advisory so it can never flip doctor's exit code. Add coverage in tests/test_notify_hermes_channel.sh. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. FOCUSED COVERAGE REQUIRED for the NEGATIVE case as well: doctor must report 'cannot determine' — never health — for a configured notify plugin that declares no inbound_probe, and that assertion must exist alongside the positive hermes case, so the honesty rule is enforced rather than merely intended.
verification_commands: /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_notify_hermes_channel.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at:
created: 2026-08-08T23:20:20Z
updated: 2026-08-09T15:53:22Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
