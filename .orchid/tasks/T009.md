---
schema: 1
id: T009
title: Give hermes an inbound probe and keep doctor honest about it
status: done
archetype: feature
scaffold: false
branch: task/T009
worktree: /Users/bilal/workspace/personal/orchid-orchid-T009
run_id:
depends_on: T001 T006 T010 T013 T014
attempts: 23
infra_failures: 8
session_id:
implementer_engine_id: claude
base_sha: fec39e48994580b21c42439a9bb2f5d8a5911043
candidate_sha: bfa53eb3c1153e084daefa6fc58181dc51119afe
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: OPERATOR HAND-OFF COMMITS: any commit whose message carries the trailer 'Orchid-Handoff: operator' was made by the OPERATOR, not the implementer, under this task's mandatory hand-off clause (lesson L017: the implementer profile cannot execute anything, so it cannot compute a checksum or set a mode bit). Re-pinning Formula/orchid.rb and setting an exec bit are therefore CORRECT operator actions, never implementer violations — do not file them as findings. Review only the implementer's own commits.
engine: claude
effort: medium
acceptance_criteria: T006 added a manifest-declared inbound_probe so a notify plugin that can report its own return-leg liveness does, and plugins/notify/openclaw/plugin.conf declares one. plugins/notify/hermes/plugin.conf does NOT, even though 'hermes gateway status' reports exactly that fact and hermes is the channel r-001 actually delivered on — and the channel whose gateway being down silently swallowed a real operator answer (lesson L011). Give the hermes notify plugin an inbound probe backed by its own CLI, declared the same way openclaw's is. Keep doctor's honesty rules intact: inbound ok only from a plugin's own positive probe, 'cannot determine' reported as such and never as health, and the whole notify block advisory so it can never flip doctor's exit code. Add coverage in tests/test_notify_hermes_channel.sh. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them. FOCUSED COVERAGE REQUIRED for the NEGATIVE case as well: doctor must report 'cannot determine' — never health — for a configured notify plugin that declares no inbound_probe, and that assertion must exist alongside the positive hermes case, so the honesty rule is enforced rather than merely intended. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits. === ALSO OWNED BY THIS TASK NOW: A JUDGMENT PAGE MUST SAY WHAT IS BEING DECIDED AND WHAT MAY BE ANSWERED. Found 2026-08-21 after the operator connected the notification chain for the first time in this run. The message orchid actually sends is one printf at libexec/orchid-notify:115 and reads: "<qid>: judgment boundary [blocked-task] needs an operator: task is blocked — only an operator resolves it — reply: ORCHID_REPO=... orchid answer <qid> <choice> --nonce <n>". An opaque id first, the boundary kind second, a shell command third. No task id, no task title, no round number, no statement of the actual defect, and no options. THE DEEPER FAULT IS THAT <choice> CANNOT BE ENUMERATED, because orchid does not model choices at all: libexec/orchid-answer accepts <choice> as FREE TEXT and validates it against nothing, so a typo is accepted silently and a page cannot list what it will accept. That is lesson L028 in a different medium — a refusal that does not name the action which clears it — and it is why twenty-seven boundaries in this run were answerable in principle and unanswerable in practice. DELIVER TWO THINGS. (1) MODEL THE CHOICES: let orchid notify carry a valid set (for example --choice approve --choice request-changes --choice defer), record it with the question, and have orchid answer REFUSE a value outside that set, naming the valid ones in the refusal. Free text stays permitted only when a question declares no choice set, so nothing existing breaks. (2) GIVE THE PAGE A BODY: task id AND title, the round or attempt number, one line stating what is actually being decided, and the permitted answers — then the reply command. The qid and nonce must survive verbatim because they are the security model; this is about what surrounds them. PIN BOTH EDGES per lesson L034: a RED case where an answer outside the declared set is refused and the refusal names the valid choices, and a GREEN case where a question with no declared set still accepts free text as it does today. NOT IN SCOPE HERE, deliberately: Telegram inline keyboards. The Bot API supports tap-to-answer via reply_markup with an inline_keyboard, and a tap returns a callback_query that maps cleanly onto orchid answer <qid> <choice> --nonce <n>. But plugins/engines/hermes/run does not speak the Bot API directly — it contains no sendMessage, no reply_markup — so buttons are new surface and belong to r-003 Track B. This task makes them POSSIBLE by giving a question a declared choice set, which is the thing a button has to carry.
verification_commands: /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_notify_hermes_channel.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T20:15:12Z
created: 2026-08-08T23:20:20Z
updated: 2026-08-29T21:24:22Z
refused_envelopes: T009-a2-implementer.json T009-a2-implementer.2.json T009-a2-implementer.3.json
attempt_budget: 25
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T03:14:01Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-22T14:38:44Z):** Blocked by the clock, not your code: verify passed with ZERO failures and the block is "reviewing -> blocked: wallclock budget exceeded" — the budget counts time spent WAITING ON REVIEWERS, which is not work you control (lesson L042). Note your acceptance criteria were extended on 2026-08-21 to cover notification legibility: model a declared choice set on a question, refuse an out-of-set answer while naming the valid ones, and give the page a body (task id and title, round, one line on the decision, permitted answers). Read the criteria before starting. Before committing run the tests covering what you touched plus shellcheck on every file you edited, and commit.

<!-- orchid:rework-brief candidate=40ac15d2f9a2f22ff3d81026bf84d836a8585b2c superseded -->
**Superseded rework brief (candidate `40ac15d2f9a2`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-25T03:19:00Z):** Unblocking: slot 1 now has its independent review. The codex-review envelope existed all along — the engine did the work and wrote VERDICT: request-changes to its job log, but the envelope was never drained into reviews/ (dogfood finding F35). One jobs reconcile recovered it intact. Its finding stands and is yours to address: the Hermes probe misreads service-managed output as undetermined, and orchid-drive still invokes notify without choices, so real judgment pages still go out without a declared choice set — which is the core of your acceptance criteria. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0.

**Operator guidance (2026-08-25T14:13:48Z):** Unblocking so the run can move. This task was blocked by the false-block surface this run has been documenting — a mid-run liveness flake (six sites, L020), a wallclock budget that counts time waiting on reviewers (L042), or an infra ladder charged for environment faults (L039). None of those is a judgement about your code. Separately, an engine was excluded for days because its envelopes were never drained rather than because it failed (F35), so review slots queued behind one engine and independence could not be proven. That is fixed: the supervisor now drains before every pass. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0; if nothing needs changing say so in your reply rather than answering with an empty round.

<!-- orchid:rework-brief candidate=3c9f5c3244032f5ad5eeb018dddd49cdaa661693 superseded -->
**Superseded rework brief (candidate `3c9f5c324403`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

<!-- orchid:rework-brief candidate=71134ec4e375fd22ddd0311e986eb2e4d02ab5c0 superseded -->
**Superseded rework brief (candidate `71134ec4e375`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-28T08:29:52Z):** Track 0 is merged through e8dd137b and the prior liveness/budget/delivery blocks are repaired. A safe rebase was attempted and aborted on a real runners/orchid-drive conflict, leaving the branch clean and unchanged. Port the current T009 work and outstanding review guidance onto current integration; drop obsolete candidate-local Formula pins per T030.

**Operator guidance (2026-08-28T13:57:28Z):** Operator replayed the preserved notification/answer candidate onto integration e8dd137, dropped obsolete Formula pins, and joined T009's declared blocked-task choices with T019 classification and T026 per-task attempt budgets. Focused doctor, Hermes channel, answer, and status tests pass at 4a52c5d53437; Bash syntax and warning-level ShellCheck pass aside from orchid-answer's unchanged integration SC2034.

**Operator guidance (2026-08-29T01:32:24Z):** Grant two convergence rounds to recognize Hermes v0.19.0's exact healthy launchd output while preserving negative/unknown honesty and the declared-choice parser guarantees.

**Operator guidance (2026-08-29T01:58:40Z):** ledger item: the Hermes inbound probe conflates generic CLI invocation failure with a proven dead gateway, and judgment pages expose the completed-attempt counter instead of the current attempt; preserve explicit down versus unknown classification and render attempts plus one with focused RED/GREEN coverage. Current candidate evidence is cd491b618c2e.

**Operator guidance (2026-08-29T02:15:59Z):** Focused reproduction for candidate c6e6816a72b2: tests/test_init_doctor.sh exits 0; tests/test_notify_hermes_channel.sh exits 1 only in section 8 because page_orchid invokes task create and notify against PAGE_REPO with ORCHID_EPOCH unset (current fixture epoch 0), so fencing refuses and the attempt-line assertions cascade. Make page_orchid read the fixture repo's current epoch through the sourced epoch helper on every verb; keep the attempt: 1 and attempt: 5 assertions. No broad suite rerun is needed before these two focused commands pass. ledger item: the candidate's new epoch-unaware fixture, not Hermes classification, caused this round's verifier failure.

**Operator guidance (2026-08-29T05:10:05Z):** ledger item: attempt-10 installed-CLI reproduction confirms the Hermes probe returns undetermined when the configured WhatsApp row says not paired even while the gateway row says not running. Fix the evidence ranking/negative vocabulary and pin this exact two-line output; also remove the overly broad bare 'refused' failed-query token noted by the approving reviewer.

**Operator guidance (2026-08-29T06:52:24Z):** Host sleep resumed inside an already-running drive pass, so the wrapper's pre-drive stale-heartbeat grace could not run before jobs check killed the live job. No attempt was charged. The Hermes repair and new RED coverage are preserved cleanly at operator-handoff commit a465fba7ac8e2c825cefbf6dbeb81d6d63dd81a6; validate and finish that tree.

**Operator guidance (2026-08-29T07:13:00Z):** Ledger item: complete only the confirmed attempt-11 judgment-page repair. A blocked-task page must state the task's concrete blocking cause, not merely 'task is blocked', and its declared answers must include every supported recovery named by the kernel, including reverify alongside unblock/retry/defer. Add focused coverage for both the rendered cause and answer enforcement. Do not re-pin Formula in the candidate: T030 moved that operation to integration/release. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T07:37:30Z):** Ledger item: close the attempt-12 choice representability gap without reopening the completed page work. A --choice value accepted by orchid notify must be passable as orchid answer's <choice>; reject leading-dash and other out-of-grammar values at mint time using the same conservative alphanumeric-first word grammar the broker already enforces (or provide and document an equivalent unambiguous terminator). Add focused RED coverage for notify --choice -foo and GREEN coverage for hyphenated alphanumeric choices. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T13:59:19Z):** Ledger item: grant two convergence rounds for the confirmed Hermes 'not responding' plus live-PID false-positive and focused regression; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T14:26:14Z):** Ledger item: grant two convergence rounds to make review-evidence question choices state-correct and prove both reviewing and arbitrating page/answer edges.

**Operator guidance (2026-08-29T14:57:48Z):** Ledger item: grant two convergence rounds for the confirmed duplicate-qid exhausted-attempt defect so the current repair does not immediately re-freeze at its prior attempt cap; this grants budget without consuming an attempt.

**Operator guidance (2026-08-29T15:36:23Z):** ledger item: grant two focused convergence rounds for the current-review false-down precedence defect; attempt 16 was substantive reviewer feedback, not a delivery failure.

**Operator guidance (2026-08-29T16:04:43Z):** ledger item: grant two focused convergence rounds for the unanimous attempt-17 duplicate-qid finding; this is substantive reviewer feedback on the remaining producer arm.

**Operator guidance (2026-08-29T16:28:26Z):** ledger item: grant two focused convergence rounds after the attempt-18 competing-poller liveness finding; this is substantive parser feedback on a concrete fatal status row.

**Operator guidance (2026-08-29T17:21:08Z):** Ledger item: grant one focused round beyond the fully spent 20/20 budget for the three current-candidate attempt-20 findings just arbitrated. T026 guarantees this operator grant raises only the cap and consumes no attempt itself; the round is limited to the two explicit false-health transcript families and distinct-stop notification starvation regressions.

**Operator guidance (2026-08-29T18:12:27Z):** Ledger item: focused convergence round for rebased candidate 280315dd7fbcfcf1a0349502f3e2ae7f0a15ffa7. Fix only per-stop page de-dup: (1) a matching durable boundary must not suppress a fresh page when the prior question is expired/unanswerable; (2) active-question matching must include task identity as well as page text, so two tasks with byte-identical reasons each get one qid. Pin repeated passes of one live question to exactly one page, then expiry to one fresh page, and two same-reason tasks to two pages. Preserve all accepted Hermes/choice behavior and the operator-rebased adapter contract; no Formula pin under T030.

**Operator guidance (2026-08-29T19:12:39Z):** Ledger item: grant two focused convergence rounds for the attempt-22 Hermes evidence-ranking defects; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T20:14:56Z):** Ledger item: grant two focused convergence rounds for the attempt-23 gate_failed choice-set and real-path regression repair; retry grants budget without consuming an attempt.
