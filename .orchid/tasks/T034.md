---
schema: 1
id: T034
title: Refuse a newline in a task value instead of destroying the task file
status: done
archetype: feature
scaffold: false
branch: task/T034
worktree: /Users/bilal/workspace/personal/orchid-orchid-T034
run_id:
depends_on:
attempts: 4
infra_failures: 4
session_id:
implementer_engine_id: claude
base_sha: 0f5b667cbe291577d02eb5c8056a6fa76e3b8b1a
candidate_sha: 0c84da4289e6e43e76fe57a5957d85b96c99ee4f
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FINDING F34 (wasiyyat-schedule-c), and THIS OPERATOR DESTROYED A TASK FILE THE SAME WAY EARLIER IN r-002. 'orchid task set <id> <key> "<value containing a newline>"' prints 'awk: newline in string ... at source line 1' three times, EXITS 0, and leaves the task file at ZERO BYTES - id, title, status, archetype, branch, every field gone. Not a rejected write: a destroyed file. On r-002 this took out .orchid/tasks/T002.md while the operator was setting a multi-paragraph hook_guidance, and it was only recovered because a copy of the frontmatter happened to survive in a review pack at .orchid/runtime/packs/. IT THEN FAILS QUIETLY IN BOTH DIRECTIONS: subsequent 'task set' calls against the now-empty file also report success, so nothing signals that the task no longer exists; and 'task show <id>' EXITS 0 AND PRINTS NOTHING rather than reporting a malformed or empty task. Both dogfood operators found it only by noticing a grep came back empty. REQUIRED: reject a value containing a newline BEFORE writing anything, naming the constraint (single-line frontmatter is a reasonable rule; destroying the file when it is violated is not); write through a temp file and atomic rename so a failed rewrite can never leave a truncated task; make 'task show' exit non-zero on an empty or unparseable task file; and add a doctor or lint check for zero-byte or frontmatter-less task files, since a run whose task file vanished mid-flight otherwise presents as a task that simply stopped existing. CONTEXT WORTH KEEPING: this happens while writing long acceptance_criteria or guidance - exactly where an operator pastes prose containing newlines, and exactly the field where losing the content hurts most. Ship RED cases: a newline-bearing value must be refused with a non-zero exit and the task file must be byte-identical afterwards; and 'task show' on an empty task file must exit non-zero. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) - do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only; the operator does perform those steps and the resulting commits WILL be in the candidate, trailered 'Orchid-Handoff: operator'. Do not report them as violations.
verification_commands: /bin/bash tests/test_task.sh && /bin/bash tests/test_init_doctor.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-30T05:24:16Z
created: 2026-08-10T19:12:34Z
updated: 2026-08-30T06:19:10Z
refused_envelopes: T034-a1-implementer.json T034-a1-implementer.2.json T034-a1-implementer.3.json
attempt_budget: 5
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T03:14:03Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-28T13:53:09Z):** Operator rebased the atomic frontmatter-write candidate onto integration e8dd137 and preserved both T026 retry-brief refreshes and T034 appended guidance through the validated writer. Focused test_task and test_init_doctor pass at b7b5806585cb; Bash syntax, diff hygiene, and warning-level ShellCheck pass.

**Operator guidance (2026-08-29T22:36:46Z):** Ledger item: close only the attempt-1 task-create literal-title gap. A title containing '&' or backslash-n must be written byte-for-byte without sed replacement expansion, must remain one valid frontmatter value, and task show/doctor must read the resulting file successfully. Use the same fail-closed atomic/frontmatter discipline the candidate added for rewrites; add RED/GREEN twins that run on BSD and GNU sed rather than relying on one implementation's escape behavior. Correct the adjacent create-path refusal wording and stale test comments while touched. Run focused test_task, test_frontmatter, and test_init_doctor; no candidate Formula pin under T030.

**Operator guidance (2026-08-29T23:13:31Z):** Ledger item: grant T034 two focused rounds for the protocol producer, structural malformed-frontmatter refusal, verb-only remedy, and damaged-list visibility; keep verification to test_task, test_frontmatter, test_init_doctor, and the narrow documented hook workflow case.

**Operator guidance (2026-08-30T03:14:54Z):** Ledger item: unblock after environment-only implementer loss during the 2026-08-29 host sleep; job j-e55-T034-a3-1336 died without an envelope, the clean worktree remains exactly at recorded candidate 421d04e154162cb2dd80dac3534e947a72bf9722, and attempts were untouched. Resume only the recorded protocol producer, malformed-reader, verb-only remedy, and damaged-list visibility fixes.

**Operator guidance (2026-08-30T04:27:58Z):** Ledger item: grant T034 two focused convergence rounds after attempt-3 arbitration for malformed-write refusal, upstream-producer atomicity, and verb-valid recovery guidance. Preserve the candidate existing frontmatter and damaged-task fixes; use only focused frontmatter, task, doctor, and producer-failure tests; no candidate Formula pin under T030.

<!-- orchid:rework-brief candidate=5569e8234bc963b3e35b6bd842fc83c104094c94 -->
**Rework brief — exact locations reported by the failing gate (candidate `5569e8234bc9`):**

These are the gate's own `file:line: RULE: message` lines, copied verbatim.
They are carried here because the actor asked to fix them may not be able to
run the gate that produced them (lesson L017), and an instruction whose
subject the recipient cannot see is not satisfiable. Fix these lines.
Running the linter, or committing its own fix, is operator work — see
PROTOCOL.md, "The operator hand-off".

**From `.orchid/reviews/T034-merge.log`:**

```
/var/folders/78/nq9t6wjn0px4x18prnmd7t240000gn/T/orchid-merge.Kg3HWc/tests/test_frontmatter.sh:357: SC2034: silent_err appears unused. Verify use (or export if used externally).
```

<!-- orchid:rework-brief-end -->
