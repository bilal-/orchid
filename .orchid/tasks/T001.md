---
schema: 1
id: T001
title: Guard drive_implementing against duplicate concurrent implementers
status: pending
archetype: feature
scaffold: false
branch: task/T001
worktree:
run_id:
depends_on: T013 T014
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id:
base_sha:
candidate_sha:
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: claude
effort: medium
acceptance_criteria: runners/orchid-drive's drive_implementing must not escalate while a live job for the same task/operation is still outstanding. Its sibling arms already guard with drive_job_outstanding (hook at :428, dispatch at :571, review at :762); the implementing arm calls drive_escalate directly after drive_implement_failed, and drive_implement_failed stays true for the WHOLE duration of a relaunched job because reconcile files siblings and never removes the dead envelope. Consequence: infra-fail 1of3 relaunch, 2of3 a SECOND implementer spawns into the same worktree on the same branch, 3of3 auto-blocks while two or three are still writing to that checkout. Add the guard. Separately decide, and record the reasoning, whether libexec/orchid-jobs prepare should also refuse a duplicate manifest for the same task and operation, or whether the driver-side guard is the right single place. Add a focused test proving a second pass over a failed-implement task does not spawn a second implementer and does not increment infra_failures without a genuine repeat failure. MANDATORY OPERATOR HAND-OFFS for this task, not optional: the implementer profile cannot execute anything (lesson L017), so it must NOT re-pin Formula/orchid.rb, must NOT chmod any file, and must NOT be asked to run a verifier, shellcheck or bash. It must instead state plainly in its report that the formula pin is stale, that verification was not run, and — ONLY IF this task actually adds a new executable — that its mode needs setting. Do not report a chmod hand-off when no executable was added. The operator performs those three and records them.
verification_commands: /bin/bash tests/test_drive.sh && /bin/bash tests/inv/test_INV-13_driver_verb_only.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at:
created: 2026-08-08T23:20:12Z
updated: 2026-08-09T15:53:22Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
