---
schema: 1
id: T001
title: Harden unattended trust boundary
status: pending
archetype: feature
scaffold: false
branch: task/T001
worktree:
run_id:
depends_on:
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id:
base_sha:
candidate_sha:
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance:
engine: codex
effort: medium
acceptance_criteria: Add unattended trust as operator-authored machine-local state outside the repository, default-denying runners/orchid-pump before any outbox drain or other effect, runners/orchid-tick, and service installation until acknowledged. Provide orchid trust unattended <repo> --reason <reason>, show, and revoke. Bind each record to Git common-directory filesystem identity (device and inode), repository root commit, and trust-policy version: linked worktrees share trust; a same-filesystem move preserving identity remains trusted; a fresh clone, copy, replacement/recreated .git, root-history replacement, or policy-version change is untrusted. Never derive trust from tracked content, origin, or repo config. Surface provenance and gate in doctor and status --explain. Preserve interactive/manual, planning, and read-only commands. Do not wire a broker yet; T002 owns it. Document target-repo prompt injection and distinguish environment hygiene, vendor sandboxing, prompt policy, brokerage availability, and OS containment. Replace overstrong claims and add focused identity, exemption, pump/outbox, tick, and service refusal tests.
verification_commands: find . -type f '(' -name '*.sh' -o -perm -111 ')' -not -path './.git/*' -print0 | xargs -0 /bin/bash -n && /bin/bash tests/test_unattended_trust.sh && /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_service.sh && /bin/bash tests/test_tick.sh && /bin/bash tests/test_pump.sh && /bin/bash tests/test_status.sh && /bin/bash tests/run.sh
resources: security,adapters
exclusive: false
wallclock_budget_s: 28800
started_at:
created: 2026-08-03T10:37:54Z
updated: 2026-08-03T11:08:41Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
