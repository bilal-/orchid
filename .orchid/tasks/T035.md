---
schema: 1
id: T035
title: Give the operator a process table for the run
status: done
archetype: feature
scaffold: false
branch: task/T035
worktree: /Users/bilal/workspace/personal/orchid-orchid-T035
run_id:
depends_on:
attempts: 0
infra_failures: 3
session_id:
implementer_engine_id: claude
base_sha: 71c2f7cc68f0a07ea78370da0a42cfc4c39a7680
candidate_sha: 9c168403fbb741951733c677e64ce70e3983569a
risk_tier: low
blocking_severity: high
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: RE-DERIVED CANDIDATE: this task's worktree was reset to integration 58d8f8d by the operator on 2026-08-20. Its previous work is preserved at tag r002-preserve/T035 and is NOT lost. Review the CURRENT diff against 58d8f8d only; do not treat the absence of earlier commits as regression, and do not compare against the preserved tag unless the implementer cites it.
engine: claude
effort: medium
acceptance_criteria: DOGFOOD FEATURE REQUEST (wasiyyat-schedule-c), filed as an enhancement not a defect, and CORROBORATED INDEPENDENTLY BY r-002 - both runs lost multi-hour stretches to exactly what it describes. WHAT status SHOWS TODAY while two jobs exist for one task is a task id and a state word: 'T004 dead' / 'T004 running'. You cannot tell which job is which, its role, engine, attempt, start time, elapsed, or whether the dead one is a corpse already handled or a fresh failure needing escalation. r-002 hit the same wall: a bare 'T031 budget-exceeded' and 'T031 dead' told the operator nothing about which budget, how long it had run, or whether to intervene, and the answer needed jq over .orchid/runtime/jobs/*.json. EVERY FIELD IS ALREADY PERSISTED in the job manifest - attempt, base_sha, candidate_sha, engine, hook_point, job_id, log, operation, output, pgid, pid, role, started_at, task - so this is a RENDERING gap, not a data-modelling one, which is what makes it cheap. REQUIRED: 'orchid jobs ls' plus the same table under 'orchid status --jobs', one row per outstanding job with JOB, TASK, ROLE, OP, ATT, ENGINE, PID, STATE, ELAPSED and BUDGET columns. TWO PROPERTIES MATTER MORE THAN THE COLUMNS. (1) LIVENESS MUST BE COMPUTED, NEVER READ: a manifest with pid 0, or whose pid fails kill -0, must render as never-started or dead and NEVER as prepared. This is dogfood finding F29 surfacing in the UI layer - 73 never-launched jobs rendered as 73 identical 'prepared' lines, and the only way to find they were corpses was cat-ing manifests and noticing pid 0, started_at 0. (2) ELAPSED AGAINST wallclock_budget_s: distinguishing slow from hung currently means reading started_at out of a manifest and doing the arithmetic by hand; the budget is already tracked well enough for 'jobs check' to report budget-exceeded, so showing the percentage continuously is nearly free and is exactly what tells an operator whether to wait or intervene. WORTH HAVING ALONGSIDE: who launched it (a drive pass vs an operator's hand-run runners/orchid-launch vs the pump - during that run the operator could not distinguish a job drive had just started from one they had started themselves, and the two want different responses); the log path on the row so the next command is copy-paste rather than a find under .orchid/runtime/logs/; a --watch mode, since the natural use is polling; and a history mode 'orchid jobs ls --all' covering reconciled jobs so 'what did this task run, in what order, and how long did each take' is answerable after the fact instead of reconstructed from journal.md by hand. WHY IT EARNS ITS PLACE: 'orchid service' already installs a real pump, so semi-attended runs are a first-class mode, but there is no operator-facing view of what that pump is doing. In the wasiyyat run two separate multi-hour stalls - F29's silent retry, and a driver that had exited unnoticed - would both have been obvious at a glance and instead took manual manifest archaeology. r-002 lost SEVEN HOURS to the second of those exactly: the supervising loop exited at a judgment boundary at 07:32Z and nothing moved until 14:42Z, because there was no view that would have shown the pump was no longer running (lesson L026). Both operators independently wrote a supervising loop around orchid drive partly because there was nothing to watch. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) - do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only; the operator does perform those steps and the resulting commits WILL be in the candidate, trailered 'Orchid-Handoff: operator'. Do not report them as violations. === THIRD RUN IN A ROW THIS COST HOURS (dogfood F36, wasiyyat general-forms 2026-08-11), and this time it caused a false report to the operator. The session told its operator the critique was 'actively working' and quoted its recent findings; the job had been DEAD FOR TWELVE AND A HALF HOURS. The findings quoted were real - they were simply stale, and nothing indicated that. The operator had to say twice that nothing was happening. EVERYTHING NEEDED TO NOTICE ALREADY EXISTED and none of it surfaced: the job's pid was gone, .orchid/reviews/ had no a4 envelope, and the log's mtime was twelve hours old. 'orchid status' showed a run in planning with no hint that its only in-flight job had died. So the requirement is sharper than a nicer table: a job whose pid is gone and whose envelope never landed must appear in status as FAILED immediately, age must be shown alongside liveness, and a staleness warning ('job X last wrote 12h ago') is worth more than any column. Note the failure mode this closes is not 'the operator lacked data' but 'the operator, and an assistant reading the same logs, both concluded the run was healthy from CONTENT while the TIMESTAMP said otherwise' - so age must be impossible to miss, not merely available.
verification_commands: /bin/bash tests/test_jobs.sh && /bin/bash tests/test_init_doctor.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-28T06:55:15Z
created: 2026-08-10T19:31:43Z
updated: 2026-08-28T08:22:25Z
refused_envelopes: T035-a1-implementer.json T035-a1-implementer.2.json T035-a1-implementer.3.json
handoff_ack: 9c168403fbb741951733c677e64ce70e3983569a
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-18T02:42:34Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run whatever covers what you changed and confirm each exits 0 — at minimum the tests/ files for the areas you touched, plus: shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure and blocks this task again.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery, charging the job-delivery ladder. It caught twenty-six tasks including this one, every one of them at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget is untouched by it. The fix is in flight under T040.

BUDGET, PLAINLY: infra_failures is kernel-owned and never reset, so you resume at the cap. One further delivery failure re-blocks this task. That is why step 3 matters — if you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply. Do not answer with an empty round.
