---
schema: 1
id: T021
title: Cross-check the previous run's ledger against the plan before applying it
status: done
archetype: feature
scaffold: false
branch: task/T021
worktree: /Users/bilal/workspace/personal/orchid-orchid-T021
run_id:
depends_on: T013
attempts: 12
infra_failures: 12
session_id:
implementer_engine_id: claude
base_sha: 63947e81da6f7d12c55cf456eccf3d3e5503cace
candidate_sha: d942a1f6634020f50fb75414ec1ca4cb88b43778
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: REWORK BRIEF — unblocked. ALL 113 FAILURES COME FROM ONE APOSTROPHE. lib/plancheck.sh:223 reports 'syntax error near unexpected token text,' and every assertion in the round is downstream: 18 cases expecting exit 3 and 5 expecting exit 0 all get 2, because the library does not parse and the crosscheck cannot run at all. THE CAUSE, located exactly: your awk program opens with a single quote at line 174 ('awk -v rid="$rid" '). Line 222 is a comment inside that program reading 'a copy and the caller's string is untouched' — and the apostrophe in "caller's" CLOSES the awk quote. Everything from line 223 on is then parsed by bash, which chokes on the awk function signature 'function markers(text, k, n, p, needle) {'. FIX: remove or reword the apostrophe — 'the string belonging to the caller is untouched' — and then SWEEP THE WHOLE AWK BLOCK (lines 174 to its closing quote) for any other apostrophe in a comment or string, because the same character will do the same thing anywhere in that program. Nothing else in your candidate is implicated: no stale pin, no flake, and the operator found no other failure that is not downstream of this. Your ledger cross-check logic has never actually run, so treat the round after this as the first real test of it — the assertions about per-finding coverage, forged deferrals, frontmatter keys not counting as coverage, and the 'no previous run' report are all still unproven. MANDATORY OPERATOR HAND-OFFS: you cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier.
engine: claude
effort: medium
acceptance_criteria: r-002's own requirements omitted a defect r-001 had already found and recorded — the once-only started_at anchor — and it blocked a task hours into the run. The operator had eighteen active lessons and the previous run's full journal available while scoping, and still missed one. That is the L016 pattern applied to knowledge rather than to gates: the information existed and nothing forced its use. Add a planning-time cross-check: before orchid plan apply commits a roadmap, report which ledger items and active lessons from the previous run are covered by NO task in the new plan, and require the operator to either cover them or record an explicit decision to defer each one. Coverage may be approximate — a keyword or lesson-id association is enough to raise the question; the point is that no carried-forward finding can leave planning UNCONSIDERED. Do not make it a hard refusal for items an operator has deliberately deferred, and do not let it silently pass when the previous run left nothing, which is itself worth stating. Add focused tests for a covered item, an uncovered one, and an explicitly deferred one. MANDATORY OPERATOR HAND-OFFS: the implementer profile cannot execute anything (L017) — do not re-pin Formula/orchid.rb, do not chmod, do not run a verifier. State in your report that the pin is stale and verification was not run; the operator does both. TO THE REVIEWER, ABOUT THAT HAND-OFF CLAUSE: it binds the IMPLEMENTER only. The operator does perform those steps, and the resulting commits WILL be present in the candidate you review — a re-pin of Formula/orchid.rb and, where a new executable was added, a mode change. They carry the trailer 'Orchid-Handoff: operator' in their commit message. They are the hand-off being followed, not broken. Do NOT report them as violations of this clause or as implementer defects; judge only the implementer's own commits.
verification_commands: /bin/bash tests/test_plan.sh && /bin/bash tests/test_lessons.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-29T16:56:16Z
created: 2026-08-10T01:28:10Z
updated: 2026-08-29T17:27:14Z
refused_envelopes: T021-a5-implementer.json T021-a5-implementer.2.json T021-a5-implementer.3.json
attempt_budget: 13
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)

**Operator guidance (2026-08-11T07:33:54Z):** AUTO-BLOCKED ENTIRELY ON THE OPERATOR'S OWN HAND-OFF. The sole failure in the chain was 'Formula/orchid.rb checksum is stale for the current tree' — the operator's re-pin landed before the implementer's final commit, so verification ran against a stale pin. Nothing in the candidate was defective. The operator has re-pinned at HEAD (9db942b) and rebound candidate_sha to it. This is the exact injustice T019 exists to remove, inflicted by the operator's own workaround for the L009 hand-off gap, on a task that had already spent one attempt on a lint false positive and two reworking blind after L023 deleted its logs. Not one of this task's three attempts was consumed by a defect in its own behaviour.

**Operator guidance (2026-08-11T16:38:51Z):** sixth stranding this run caused solely by the operator's formula re-pin racing the driver's same-pass reconcile-and-verify (lesson L031). Worktree clean, pin was the only failure, nothing in the candidate defective. Re-pinned and rebound. The operator is now automating this exact recovery in the supervising loop, gated on the verify log containing NO failure other than the stale-pin message, because six identical manual recoveries is evidence the step is mechanical rather than a judgement.

<!-- orchid:rework-brief candidate=165aaeebbc1410eeb6224a71a8cfab31ab326733 superseded -->
**Superseded rework brief (candidate `165aaeebbc14`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-11T22:51:08Z):** STRANDED BY A FLAKY TIMING ASSERTION, PROVEN BY RE-RUNNING IT. The single failure was 'streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint) -- this is the stall-detector's liveness signal', in tests/test_engine_codex.sh and tests/test_engine_agy.sh. The operator re-ran BOTH files against this same candidate: rc=0, zero failures. The assertion depends on a stub adapter having flushed to its log by a wall-clock midpoint, so under machine load it fails through no fault of the candidate — lesson L020's scheduling-dependent class exactly, and precisely the failure T019's flaky arm exists to stop charging against the attempt budget. Nothing in T021's work is implicated. Re-pinned and rebound. RECORDED FOR T038: this is the SECOND time in this run that the operator has had to prove a failure flaky by hand re-running it (T013 was the first, pass/fail/pass on an unchanged tree), and both times the evidence was only obtainable because the operator happened to suspect it. An attempt budget that charges flakes, in a suite with known timing-dependent assertions, is a budget measuring the machine's load as much as the candidate's quality.

**Operator guidance (2026-08-12T05:05:56Z):** 113 failures from ONE APOSTROPHE. lib/plancheck.sh's awk program opens with a single quote at line 174; line 222's comment reads 'a copy and the caller's string is untouched', and the apostrophe in caller's closes that quote, so from line 223 bash parses awk source and dies with 'syntax error near unexpected token text,'. The library never loads, the crosscheck never runs, and all 18 expected-3-got-2 and 5 expected-0-got-2 assertions are downstream of that. Nothing else in the candidate is implicated. Unblocked with the exact line, the exact character, and an instruction to sweep the rest of the awk block for the same shape - this is the seventh time this run a task has been stranded by text that reads as syntax (lesson L029), and the first where the text was a possessive apostrophe in a comment.

<!-- orchid:rework-brief candidate=04a7410316e480b1225ce96b4ea6688813178d70 superseded -->
**Superseded rework brief (candidate `04a7410316e4`) — 18 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-12T06:21:07Z):** STRANDED BY THE FLAKY TIMING ASSERTION FOR THE SECOND TIME, AND THE APOSTROPHE FIX WORKED. lib/plancheck.sh now parses, and this round has exactly ONE failure: 'streaming stub: job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)'. The operator re-ran both engine suites against this same candidate - tests/test_engine_agy.sh and tests/test_engine_codex.sh - each rc=0 with zero failures. That is the FOURTH time tonight this single assertion has been proven flaky on an unchanged tree (T021 twice, T023 twice), and it has now cost four task-attempts. It is lesson L020's scheduling-dependent class: the assertion requires a stub adapter to have flushed to its log by a wall-clock midpoint, and it fails whenever the machine is loaded - which, with four tasks verifying concurrently at ~31 minutes each, is most of the time. ORCHID'S OWN PARALLELISM IS FAILING ORCHID'S OWN TESTS. Note also that T021's ledger cross-check logic has STILL never been exercised: every previous round died on the parse error, and this one died on the flake, so the feature the task exists to deliver remains unmeasured after five rounds.

**Operator guidance (2026-08-18T03:14:02Z):** SUPERSEDES ALL EARLIER OPERATOR NOTES ON THIS TASK.

DO THIS:
1. Continue the task. Your committed work stands; read your acceptance_criteria and any review findings in the rework history below.
2. Before you commit, run what covers what you changed and confirm each exits 0 — the tests/ files for the areas you touched, plus shellcheck --norc --shell=bash --severity=warning on any file you edited under lib/, libexec/, runners/ or scripts/.
3. Commit your work. A round that commits nothing is charged as a delivery failure.

WHY YOU WERE BLOCKED, AND IT WAS NOT YOUR CODE: on 2026-08-12 a driver defect (lesson L039) began reading an ok envelope over an unmoved HEAD as a failed delivery and charging the job-delivery ladder. It caught twenty-seven of this run's forty tasks, every one at infra_failures 3 of 3, several before they had reached a single review. Your attempts budget was not touched by it. The fix is in verification under T040.

BUDGET: infra_failures is kernel-owned and never reset, so you resume at 3. The operator has raised infra_max to 12 in the run config so you have room, and will restore it to 3 once T026 lands a real budget-recovery verb. If you conclude a listed item genuinely needs no change, make the other changes, commit, and say so in your reply — do not answer with an empty round.

**Operator guidance (2026-08-25T02:37:47Z):** Blocked by the clock, not your code: verify passed with ZERO failures and the block is wallclock budget exceeded while in reviewing (lesson L042 — the budget counts time spent waiting on reviewers, which you do not control). Two of four engines were also excluded from this run until recently, so review slots were queueing behind one engine. Rebase onto current integration is handled for you; before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and if nothing needs changing say so in your reply rather than answering with an empty round.

**Operator guidance (2026-08-25T18:27:03Z):** Unblocked by operator request. The blocks in this run have overwhelmingly not been judgements about your code: a mid-run liveness flake with six sites (L020), a wallclock budget that counts time waiting on reviewers (L042), an infra ladder charged for environment faults (L039), and an engine excluded for days because its envelopes were never drained rather than because it failed (F35). Your committed work stands. Rebase onto current integration is handled for you. Before committing run the tests covering what you touched plus shellcheck --norc --shell=bash --severity=warning on every file you edited, and confirm each exits 0. If nothing needs changing, say so in your reply and commit any rebase result rather than answering with an empty round.

<!-- orchid:rework-brief candidate=148c4021e62aa4cee835ecf8eeae56194384e02b superseded -->
**Superseded rework brief (candidate `148c4021e62a`) — 24 line(s) withdrawn.**
They were reported against a candidate that is no longer the one under
work, so their line numbers describe a tree that no longer exists. Do not
act on them: the locations for the candidate now under work, if the gate
reported any, are in the brief marked with that sha.
<!-- orchid:rework-brief-end -->

**Operator guidance (2026-08-28T13:40:47Z):** Operator rebased the preserved carry-forward cross-check candidate onto integration e8dd137 and semantically joined the current kernel documentation. Focused test_plan and test_lessons pass at c98125045cde; Bash syntax and warning-level ShellCheck pass, with orchid-plan's pre-existing SC2034 unchanged from integration.

**Operator guidance (2026-08-29T04:27:04Z):** Environment recovery, not a candidate judgment: host suspend killed four unrelated live jobs together before this job changed its clean worktree. Resume the existing current-candidate cross-check correction and the attempt-6 review guidance; no attempt was charged.

**Operator guidance (2026-08-29T07:07:49Z):** Ledger item: repair the confirmed attempt-7 surface/prompt mismatch. Update both Claude and Codex orchestrator instructions so journal add --kind ledger is explicitly permitted for out-of-scope findings, while plan_deferral remains forbidden; add focused tests that pin the instructions as well as the broker allowlist. Do not re-pin Formula in the task candidate: T030 moved that operation to integration/release. Two delivery rounds are authorized; retry grants budget without consuming an attempt.

**Operator guidance (2026-08-29T14:21:44Z):** Ledger item: grant two convergence rounds to close the post-planning plan-apply bypass and prove an uncovered revision cannot move integration.

**Operator guidance (2026-08-29T15:09:08Z):** Ledger item: grant two convergence rounds for the confirmed fail-open missing prior archive and missing archived journal paths so the repair cannot immediately re-freeze at its prior cap; this grants budget without consuming an attempt.

**Operator guidance (2026-08-29T15:53:53Z):** ledger item: grant two focused convergence rounds for the current-review legacy-ledger vocabulary gap; attempt 10 produced substantive archive-derived reviewer feedback.

**Operator guidance (2026-08-29T16:24:38Z):** ledger item: grant two focused convergence rounds after the attempt-11 fail-open scratch-allocation review; this is substantive reviewer feedback.
