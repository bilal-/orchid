---
schema: 1
id: T008
title: Version the release 1.0.0-beta.1 until external beta earns 1.0
status: done
archetype: feature
scaffold: false
branch: task/T008
worktree: /Users/bilal/workspace/personal/orchid-task-T008
run_id:
depends_on:
attempts: 0
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 9c5c3a363d469cb26a83d42a586ad1fc0f5e91ee
candidate_sha: 99112dbe5a7e223c7f24faa4838f01222af99e4d
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: Set the shipped version to 1.0.0-beta.1. RATIONALE, and it belongs in the docs you touch: 1.0.0 claims 'hardened and in use', and neither is true yet — this repository has no external users, no third-party beta run has happened, and this hardening run alone found a shell injection reachable from repository content, a headless deadlock, duplicate concurrent implementers into one worktree, a release gate nothing was invoking, and orchid executing pre-merge code from a stale checkout. 1.0.0 is to be EARNED by external beta, not assumed. Keep that framing factual and short; do not editorialize and do not claim a beta has occurred. THE SURFACES, verified by the operator before dispatch: (1) lib/common.sh:43 ORCHID_VERSION="1.0.0" is canonical — change it, and update the comment above it, which currently says 'v1-m4: the release version -- the -mN milestone suffix era ends here'. (2) scripts/release.sh WILL REFUSE the new tag as written: line 46's case wants v[0-9]*.[0-9]*.[0-9]* and line 49 enforces '^v(0|[1-9][0-9]*).(0|[1-9][0-9]*).(0|[1-9][0-9]*)$', neither of which admits a prerelease suffix. Widen BOTH to accept a semver prerelease (e.g. -beta.1) while still refusing moving refs like main or HEAD — that refusal is the check's actual purpose and must survive. Then confirm the downstream agreement checks still hold: tag equals v$version, archive name orchid-<version>.tar.gz, prefix orchid-<version>/, and the lib/common.sh comparison at :156. (3) Formula/orchid.rb carries both a url with the version in the path and a version field — both change. (4) README.md, docs/install.md and docs/quickstart.md mention 1.0.0. (5) PLUGIN MANIFESTS NEED NO CHANGE, verified: lib/manifest.sh:103 does 'local v=${1%%-*}', stripping the prerelease before extracting major.minor, so requires_orchid=>=1.0 is still satisfied by 1.0.0-beta.1. Do NOT change them, and do NOT change the -mN milestone names in docs/specs/roadmap.md or docs/plans/* — those are historical milestone labels, not the shipped version. ADD A TEST that the prerelease tag shape is accepted by scripts/release.sh and that a moving ref is still refused, so the widened regex cannot silently rot into accepting anything. Do not re-pin Formula/orchid.rb's sha256 (operator hand-off) — note it in your report. Do not touch .orchid. Operator authorizes retries until success.
engine: claude
effort: medium
acceptance_criteria:
verification_commands: /bin/bash tests/test_ci_release.sh && /bin/bash tests/test_manifest.sh && /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash && /bin/bash tests/run.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-06T17:16:30Z
created: 2026-08-06T17:16:11Z
updated: 2026-08-06T18:44:37Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
