---
schema: 1
id: T004
title: Establish CI lint and pinned-release gate
status: done
archetype: feature
scaffold: false
branch: task/T004
worktree: /Users/bilal/workspace/personal/orchid-task-T004
run_id:
depends_on:
attempts: 8
infra_failures: 2
session_id:
implementer_engine_id: codex
base_sha: d49df2338e03d43fdb13842b643fd0f3be998690
candidate_sha: 21da12f6d80cd5c65f1d5e4f26f4cbea24a12690
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: CURRENT REWORK IS MANDATORY: preserve all approved CI, ShellCheck --norc isolation, BSD-find, line-scoped exceptions, fail-closed ORCHID_HOME, archive config/attribute isolation, formula fixed-point, cwd-independent pinned installer, and corrected docs. Resolve the remaining candidate-bound attempt-8 defect: release.sh and pin-formula.sh must not derive release/formula checksum bytes from the host unpinned gzip implementation, because same-host double builds do not prove macOS/Linux byte identity. Use Git’s deterministic built-in compressed archive path inside the already isolated disposable repository, or another explicitly pinned/defined compressor whose output is stable across supported CI OSes, while preserving immunity to ambient tar.*.command config and info/attributes. Add adversarial coverage showing a failing or byte-divergent gzip earlier on PATH cannot affect archive/formula bytes, plus the existing hostile Git-config tests and fixed-point checks. The operator expressly authorized retries beyond the default cap until success.
engine: codex
effort: medium
acceptance_criteria: Add secret-free Linux and macOS CI for Bash syntax, full tests, invariants, docs, and ShellCheck. Add one canonical local CI script used by CI and rehearsal; it discovers every shipped shell script, including root scripts, templates, non-.sh executables, runners, adapters, tests, and skill helpers, and accepts an explicit Bash interpreter. Establish zero-warning ShellCheck with narrow inline exceptions and replace mktemp -u FIFO creation. Add version-pinned release tooling that requires a clean tagged commit, resolves the tag to one exact commit, builds a reproducible archive from that exact commit using git archive without reading working-tree files, verifies archive prefix/content/checksum by rebuilding and comparing, extracts and tests that archive, and cross-checks version, tag, formula, and installer metadata. Stable install uses an immutable version/ref; development channel is explicit. Fail on placeholders, mismatch, dirty state, moving refs, installer mismatch, or undocumented lint exceptions. Do not publish. Remove screenshot placeholders or replace with deterministic evidence.
verification_commands: /bin/bash tests/test_ci_release.sh && /bin/bash tests/test_install.sh && /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash && /bin/bash tests/run.sh
resources: ci,release
exclusive: false
wallclock_budget_s: 86400
started_at: 2026-08-03T11:11:10Z
created: 2026-08-03T10:37:55Z
updated: 2026-08-03T22:55:27Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
