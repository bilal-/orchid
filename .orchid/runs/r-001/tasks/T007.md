---
schema: 1
id: T007
title: Clear the inherited ShellCheck findings so the release gate passes
status: done
archetype: feature
scaffold: false
branch: task/T007
worktree: /Users/bilal/workspace/personal/orchid-task-T007
run_id:
depends_on:
attempts: 1
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: aab94a72ca061fd4fc6e5c228949e4dd71a4a26b
candidate_sha: ad471a03cd279dbb38766f8db3c7aa716fff64a2
risk_tier: low
blocking_severity: high
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 2 — FOUR REMAIN. You fixed 13 of 17; 'bash scripts/ci-local.sh --bash /bin/bash' in your worktree still exits 1 with these four, verified by the operator: (1) lib/trust.sh:736 'state=unattended-only' and (2) lib/trust.sh:739 'state=interactive-only' — SC2100 reads the hyphen as arithmetic; both are plain string assignments and quoting the value ('state="unattended-only"') resolves it with identical semantics. (3) libexec/orchid-task:420 'to=done' — SC1010 reads the bare word 'done' as the loop keyword; quoting ('to="done"') resolves it, again identical semantics. (4) tests/test_init_doctor.sh:580 'nfy_dup_rc appears unused' (SC2034) — this one needs JUDGMENT, not a quote: the variable captures a doctor exit code from the duplicate-plugin case and nothing reads it. Decide whether the test MEANT to assert on it (in which case add the assertion — an INV-10 duplicate should be a doctor FAIL per libexec/orchid-doctor's own comment, so a missing assertion is a real coverage gap) or whether the capture is genuinely vestigial (in which case drop it). Prefer adding the assertion if the surrounding case reads like it intended one; say which you chose and why. Rules unchanged: behavior must not change for 1-3, 'bash tests/run.sh' passing is your proof, no file-wide suppressions, and do NOT touch tests/test_e2e_release_rehearsal.sh — T005 owns its two findings and is fixing them concurrently. Re-run the gate yourself before reporting; if your session cannot execute it, say so plainly. Do not re-pin Formula/orchid.rb (operator hand-off). Do not touch .orchid. Operator authorizes retries until success.
engine: claude
effort: medium
acceptance_criteria:
verification_commands: /bin/bash scripts/ci-local.sh --bash /bin/bash && /bin/bash tests/run.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-06T10:30:25Z
created: 2026-08-06T10:30:03Z
updated: 2026-08-06T12:41:26Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
