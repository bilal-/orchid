---
schema: 1
id: T006
title: Recover reviewer findings and probe the notify return leg
status: done
archetype: feature
scaffold: false
branch: task/T006
worktree: /Users/bilal/workspace/personal/orchid-task-T006
run_id:
depends_on:
attempts: 5
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: 713121adfbff4e160fa985bd37cbedad89d7dae0
candidate_sha: 7030095ce01b9e32506d404b6c597efad7743fd2
risk_tier: medium
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 6 — three findings, and the first is the THIRD time this task has shipped an unproven 'ok'. Everything else is cleared and stays: cd_scratch and the suite-wide lint (the reviewer calls it a sound fix for the stray-commit incident, and test_helpers.sh proves both the unsourced and foreign-directory paths fail closed), the set -e handling, the bash-3.2 empty-array guard under set -u, resolve_notify_dir's not-found vs INV-10 distinction, and the expiry/stat-failure bucketing matching orchid-answer's fail-closed rule. Do not re-pin Formula/orchid.rb. (1) BLOCKING — doctor's inbound evidence filter keeps only questions whose task is 'blocked', but libexec/orchid-answer NEVER LOOKS AT TASK STATUS: it gates on the question file existing, expiry, nonce/allowlist and no prior answer, so a question on an arbitrating or merging task is fully answerable. Meanwhile the driver's generic boundary notifier fires for operator-decision, hook-failure, worktree-conflict and non-arbitrable review-conflict boundaries with the task in whatever status it holds — runners/orchid-drive:1053 notifies with the task sitting in merging. Net effect: doctor drops hook failures, stuck merges, worktree conflicts and review boundaries, then prints 'no blocker is waiting for an answer' — an unproven POSITIVE all-clear, which is worse than the silence it replaced because it reads as a statement rather than an absence. It also interacts badly with your own adapter fix: populated findings[] make review-conflict boundaries MORE likely, and those are exactly the questions this filter discards. The narrowing was aimed at 'orchid task unblock' resolving a task without writing an answer, which is a real case — but '!= blocked' is far wider than 'was blocked, then locally resolved'. Filter on what actually makes a question UNANSWERABLE (expiry, which orchid answer does enforce), or on the specific statuses that cannot be answered — not on 'not blocked'. And do not print a positive all-clear the evidence does not support: if the only thing you can say is 'no question is currently unanswered', say exactly that and name what it does not prove. Extend the tests beyond rework and blocked — they currently encode the assumption rather than testing it. (2) BLOCKING — the review prompt hardcodes 'A finding at or above this task's blocking_severity (medium by default)' but the shipped templates differ: templates/task-test.md ships blocking_severity: high while task-migrate and task-refactor ship medium. The prompt therefore states the wrong threshold for some archetypes, on the very gate this task made live. Either state the task's ACTUAL blocking_severity (it is available where the prompt is built) or drop the parenthetical entirely — do not hardcode a default that is not universal. (3) LOW — docs/specs/kernel.md documents 'orchid task set <id> risk_tier low' as a way to relax blocking_severity, which the monotonic risk_tier guard refuses; correct the doc. Do not touch .orchid. Operator authorizes retries until success.
engine: claude
effort: medium
acceptance_criteria: Two independent fixes, both found live by this run's own dogfooding. (1) REVIEWER FINDINGS ARE LOST: plugins/engines/claude/run's REVIEW prompt asks the model for a VERDICT line only, while FINDING: lines are requested by the CRITIQUE prompt alone, so every review envelope arrives with findings[] as the literal empty array and the reviewer's reasoning survives only if it happens to appear in prose in the engine log — which is reaped. This cost real information at least five times in this run, including one round where three reported findings were unrecoverable. Make the review prompt request the same 'FINDING: <low|medium|high>: <title>' lines the critique prompt already specifies, and make the existing findings parser apply to the review operation as well as critique, so findings[] is populated for reviews. Do not change the VERDICT contract or the envelope schema; do not weaken the critique path. Any severity-gating policy that reads findings[] (the deterministic driver's blocking_severity gate) must keep working unchanged when findings[] is empty. (2) THE NOTIFY RETURN LEG IS UNCHECKED: orchid doctor validates the configured notify plugin and its binary but never that anything is listening to receive a reply, so an operator can wire notify, watch blockers arrive on their phone, answer them, and have every answer silently go nowhere. This happened in this run: the openclaw gateway was down all day and a phone answer was lost with no local trace. Add a doctor check that reports whether the configured notify plugin's inbound path is actually reachable, and label it honestly for what it proves: outbound send capability and inbound listener presence are DIFFERENT facts, and doctor must not imply the second from the first. Where liveness cannot be determined portably, say so explicitly rather than reporting ok. Keep it advisory (a warning, never a hard failure) since a run is legitimately usable with no notify channel at all.
verification_commands: /bin/bash tests/test_engine_claude.sh && /bin/bash tests/test_init_doctor.sh && /bin/bash tests/test_notify_channel.sh && /bin/bash tests/test_notify_hermes_channel.sh && /bin/bash tests/test_notify_answer.sh && /bin/bash tests/test_docs.sh && /bin/bash tests/run.sh
resources:
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-06T05:17:55Z
created: 2026-08-06T05:17:06Z
updated: 2026-08-06T10:26:33Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
