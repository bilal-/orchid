---
schema: 1
id: T005
title: Add beta qualification and release rehearsal
status: done
archetype: feature
scaffold: false
branch: task/T005
worktree: /Users/bilal/workspace/personal/orchid-task-T005
run_id:
depends_on: T003 T004
attempts: 7
infra_failures: 0
session_id:
implementer_engine_id: claude
base_sha: dfe1088942f8454517cff80345a4c85d7c823ea7
candidate_sha: ccc635155216c146bd0b1ce632dea236615df6b3
risk_tier: high
blocking_severity: medium
stop_condition: report at most 8 findings at or above medium severity; no style nits; one pass only
hook_guidance: ATTEMPT 8 — one blocking fix and two lows. FIRST, a correction: the reviewer claims 'none of the 2,600 added lines were executed' and that the Bash 3.2 CI criterion is unmet with no verification evidence on the branch. That is FALSE for the THIRD time this task, and the arbiter has refuted it each time — orchid verify ran your full chain against this exact candidate 0152117 (test_beta_qualification.sh, test_e2e_release_rehearsal.sh, test_docs.sh, scripts/ci-local.sh --bash /bin/bash, tests/run.sh), exit 0, zero FAIL lines, recorded SHA-bound in .orchid/reviews/T005-verify.log. Reviewer sandboxes deny execution; the kernel's verification is what covers that. Do NOT add evidence files or restructure to satisfy it. (1) BLOCKING — the tripwire self-test at the top of tests/test_e2e_release_rehearsal.sh invokes REAL remote-capable git subcommands ('git push', 'git fetch', 'git pull', 'git ls-remote origin', 'git remote update', 'git submodule update') while the process cwd is still the operator's LIVE CHECKOUT, which has a real 'origin'. It is currently safe only because the shim is asserted first on PATH immediately beforehand — a single assertion standing between a self-test and the operator's real remote. This run's requirements make 'never push, publish, deploy, or contact a remote' ABSOLUTE, and this is the task that certifies exactly that property, so the shape matters as much as the outcome: move the self-test so it runs with cwd inside the private root (a throwaway git repo you create under $R for the purpose), where even a total shim failure reaches nothing but a scratch directory with no remote. Keep every existing assertion — that each shape refuses with 97 AND logs — since proving the tripwires work before relying on an empty log is the right design; only the ground it stands on changes. (2) LOW — the unattended-gate probe reads the gate twice and never compares the two reads, so its result asserts 'is unchanged after inspection' while printing a differing gate_after. Either compare them and let the result reflect it, or stop claiming 'unchanged'. (3) LOW — 'stale-run-lock-visibility' records 'fail' when 'orchid status --explain' may have bailed for an unrelated reason on an uninitialized scratch repo, so an untested check is recorded as a build gap. Distinguish 'the check ran and the behaviour is missing' from 'the check could not run', and record the second as not-tested, which your own vocabulary already supports. Do not re-pin Formula/orchid.rb (operator hand-off). Do not touch .orchid. Operator authorizes retries until success.
engine: codex
effort: medium
acceptance_criteria: Add a reusable local beta qualification harness and checklist for operator-supplied repos. Record only anonymized metadata, checks, durations, and outcomes; never contents, paths, prompts, diffs, or secrets. Emit concise JSON and text evidence and prove two disposable fixtures. The end-to-end rehearsal must create one private temporary root and set isolated HOME, XDG_CONFIG_HOME, XDG_DATA_HOME, TMPDIR, ORCHID_HOME, CLAUDE_SKILLS_DIR, install prefix, plugin path, Git global/system config, and all fixture/worktree/output paths beneath it; assert cleanup and prove no file outside that root changed except read-only access to the source checkout. Cover unattended refusal, explicit acknowledgement/reason, deterministic drive, one-command setup, beta qualification, and release checks. Run local CI under Bash 3.2. PATH tripwires must fail and log curl, wget, ssh, scp, nc, vendor CLIs (claude, codex, agy, hermes, openclaw), notify senders, package/network tools, and remote-capable git subcommands including push, fetch, pull, clone, ls-remote, remote update, submodule update, and send-pack; assert no tripwire and no remote ref change. Synchronize README, quickstart, threat model, protocol, help, and release docs. Genuine third-party beta and publication remain operator-owned and unclaimed.
verification_commands: /bin/bash tests/test_beta_qualification.sh && /bin/bash tests/test_e2e_release_rehearsal.sh && /bin/bash tests/test_docs.sh && /bin/bash scripts/ci-local.sh --bash /bin/bash && /bin/bash tests/run.sh
resources: qualification,rehearsal
exclusive: false
wallclock_budget_s: 28800
started_at: 2026-08-06T08:58:29Z
created: 2026-08-03T10:37:55Z
updated: 2026-08-06T21:49:57Z
---

(Task spec: goal, constraints, acceptance criteria. Rework history appended below.)
