# Lessons

## L001 [retired] repo
statement: Any commit that changes tracked repo content must re-pin Formula/orchid.rb in the SAME commit (scripts/pin-formula.sh), because the aggregate suite gate recomputes the source-archive checksum from the tree and fails on a stale pin -- a content-only fix that skips the re-pin always costs a whole extra rework cycle
evidence: T001 attempts 21, 23, 25 and 26 each reworked solely on a stale formula pin
first: 2026-08-04T21:29:44Z
last_confirmed: 2026-08-04T21:29:44Z
invalidate_when: Formula/orchid.rb stops pinning a source-archive checksum, or the freshness gate in tests/test_ci_release.sh is removed

## L002 [active] repo
statement: Orchid runs under TWO different PATHs: bin/orchid and the runners pin a fixed system PATH (/opt/homebrew, /usr/local, linuxbrew, MacPorts, system dirs), while lib/common.sh restores the operator PATH for every verb unless __orchid_entry_defer_restore=1. The unattended trust gate in runners/orchid-pump therefore probes tools BEFORE that restore (fixed PATH) and orchid doctor probes them AFTER (operator PATH). Any tool-availability check on a user-facing surface must also be evaluated on the unattended PATH, or the two surfaces contradict each other for a tool installed outside the fixed prefixes (~/.local/bin, nix, asdf, cargo) -- and no remedy that only changes the PATH the scheduler exports can fix it, because the runners overwrite PATH unconditionally before the gate.
evidence: 
first: 2026-08-04T22:02:36Z
last_confirmed: 2026-08-04T22:02:36Z
invalidate_when: the entry scripts stop pinning a fixed system PATH, or doctor/status begin evaluating tool availability on the same PATH the unattended gate uses

## L003 [active] repo
statement: L002's converse, and the trap its own fix falls into: an entry point that sets __orchid_entry_defer_restore=1 must call _orchid_entry_restore_operator_path as soon as its trust decision and tool probe are done (libexec/orchid-doctor does this right). Leaving the fixed PATH pinned for the REST of the command silently degrades unrelated in-process work that resolves tools by PATH -- ledger_show (lib/ledger.sh) needs jq, and orchid-status guards it with '2>/dev/null || true', so a jq installed only outside the fixed prefixes makes 'status --explain' print the FALSE line '(no engine events yet)' over a populated ledger, rc 0, no warning. The gate/probe needs the fixed PATH; the report after it does not. Subprocesses are not affected (they restore from the exported __orchid_entry_operator_path), which is why this only ever shows up in-process and survives review.
evidence: 
first: 2026-08-04T22:53:37Z
last_confirmed: 2026-08-04T22:53:37Z
invalidate_when: lib/common.sh no longer supports __orchid_entry_defer_restore, or no entry point pins the fixed unattended PATH before its authorization decision

## L004 [active] repo
statement: Unattended trust derives its root SET from the HEAD of the worktree being inspected -- _unattended_root_commit_compute runs 'rev-list --parents $head' after 'rev-parse --verify HEAD' in that repo path -- not from the common directory, a canonical branch, or --all. Consequence reviewers keep re-raising as a defect: linked worktrees share trust (tests/test_unattended_trust.sh:340-345 proves it) only while their HEAD shares the recorded root set; a linked worktree checked out on an unrelated-root/orphan branch computes a different set and is denied with 'repository root commit changed (recorded X, current Y)'. That is the conservative side of a tension the acceptance criteria themselves contain -- 'linked worktrees share trust' vs 'root-history replacement is untrusted' -- and it is fail-closed, named in the diagnostic, and recoverable by re-acknowledgement, so it is NOT a defect. Settled non-blocking alongside three other claims reviewers have now raised across multiple attempts: st_dev instability after a remount (REFUTED twice -- :574-578 accepts an existing exact 2-link pair whose identity re-reads equal and re-stamps device/inode, so re-ack succeeds), the duplicate full-history walk in pump and then tick (each entry point default-denying independently is exactly what the criteria require; cost only), and the /bin/bash platform note (docs nit, excluded by stop_condition). Do not spend another rework attempt on any of the four without new evidence.
evidence: 
first: 2026-08-04T23:49:48Z
last_confirmed: 2026-08-04T23:49:48Z
invalidate_when: lib/trust.sh stops deriving the root set from the inspected worktree's own HEAD (rev-list --parents HEAD at :983), or the acceptance criteria stop requiring root-history replacement to be untrusted

## L005 [active] repo
statement: tests/helpers.sh sets 'set -uo pipefail', so any assertion shaped 'producer | grep -q PATTERN' is silently FLAKY: grep -q exits at its first match and SIGPIPEs the producer (exit 141), which pipefail then promotes to the pipeline's status, failing the assertion even though the pattern WAS found. It bites only when the match is early relative to the producer's total output, so the same idiom passes in small files and flakes in large ones -- and flakes intermittently, on scheduling. There are ~137 such occurrences across 33 test files already on the integration branch, so treat a lone 'grep -q' assertion failure as this idiom until proven otherwise: verify by running the same pipeline standalone several times before ever calling it an implementation defect. Write new assertions as 'out=$(producer)' then 'case $out in *pat*)' (or grep -c on a captured variable), never a bare producer|grep -q.
evidence: 
first: 2026-08-05T12:34:48Z
last_confirmed: 2026-08-05T18:22:05Z
invalidate_when: tests/helpers.sh's assert_match stops using a herestring, or pipefail is removed

## L006 [active] repo
statement: The claude adapter's REVIEW operation never populates findings[] — by design, not by accident: plugins/engines/claude/run:244 asks a review reply for a VERDICT line ONLY, while the FINDING: line shape is requested solely by the CRITIQUE prompt (:279-281), and :333-341 documents that for review 'findings stays the literal empty array it always was'. Consequences to plan around: (1) every claude review envelope arrives verdict-only, so any reviewer rationale exists ONLY in .orchid/runtime/logs/<job_id>.log and MUST be journaled by hand at arbitration or it is lost when the log is reaped; (2) any deterministic policy that branches on findings[] severity — including T002's drive_envelope_has_blocking_finding — is DEAD CODE whenever the resolved reviewer is claude, so deterministic approval rests entirely on a single VERDICT line plus scope_complete and never sees severity data. That is a supported-but-degraded shape, not a defect to fix inside a task: label it honestly wherever the driver's approval path is documented rather than implying severity gating is active for all engines.
evidence: 
first: 2026-08-05T13:31:41Z
last_confirmed: 2026-08-05T13:31:41Z
invalidate_when: plugins/engines/claude/run's review branch starts requesting FINDING: lines, or review.<tier> stops resolving to claude

## L007 [active] repo
statement: Any component that judges a reviewer-evidence set MUST mirror the kernel's reviewing->arbitrating gate exactly, and the kernel's rule is COUNT-BASED, not reject-based: libexec/orchid-task:301-313 counts only envelopes whose candidate_sha equals the task's current candidate_sha AND whose status is ok, and SILENTLY SKIPS everything else — a stale sha, a status:error file, an unreadable envelope. It never treats the presence of a bad sibling as a failure; sufficiency is 'enough valid ok current envelopes', full stop. Diverging from this in either direction deadlocks a task: boundarying on a non-ok sibling means the ordinary recovery path (slot errors, reconcile lands status:error, relaunch lands ok) advances to arbitrating through the kernel gate and is then permanently refused deterministic approval by the stricter component, with no verb able to remove the dead envelope. This was gotten wrong TWICE in T002 in opposite directions — first by boundarying on stale siblings, then by boundarying on non-ok ones after a reviewer asked for fail-closed behavior and the arbiter's rework guidance endorsed it. 'Fail closed' is the right instinct for trust and for verification evidence, but review-evidence sufficiency is a COUNTING problem the kernel already solved; when a reviewer requests fail-closed semantics here, check the kernel gate's actual code before agreeing.
evidence: 
first: 2026-08-05T15:24:33Z
last_confirmed: 2026-08-05T15:24:33Z
invalidate_when: libexec/orchid-task's reviewing->arbitrating gate changes how it counts reviewer envelopes

## L008 [active] repo
statement: Do not commit repo config (orchid config commit) to the integration branch while any task is in flight toward merge. The integration ref IS the merge base, so any commit to it — even a config-only one touching nothing the candidate reads — advances base_sha, and orchid merge then returns rebase_rereview_required (exit 5), rebases the candidate, and invalidates every SHA-bound verify log and review envelope. On this repo that costs about 15 minutes of re-verify plus a full re-review round, for a diff that changed no code at all. Batch config changes BEFORE dispatching work or AFTER a merge lands; if a config change is genuinely urgent mid-flight, expect and budget the re-verification rather than being surprised by it. This is INV-07 working correctly, not a defect: evidence is bound to a base and a moved base is new evidence.
evidence: 
first: 2026-08-05T17:40:45Z
last_confirmed: 2026-08-05T17:40:45Z
invalidate_when: orchid merge stops re-verifying on a moved base, or config commits stop landing on the integration branch

## L009 [retired] repo
statement: UNATTENDED DEADLOCK, found live: orchid merge's rebase path can produce a candidate that CANNOT pass its own verification without an operator, and in a headless run there is no operator to fix it. Mechanism: scripts/pin-formula.sh pins the SOURCE-ARCHIVE checksum into Formula/orchid.rb, and only Formula/ and .orchid/ are export-ignored — so any OTHER tracked file that changes on the integration branch (orchid.config counts) changes the archive bytes. When orchid merge returns rebase_rereview_required and rebases the candidate onto that new base, the rebased tree's archive checksum no longer matches the pin the candidate carries, tests/test_ci_release.sh fails, and the task is sent to rework. The implementer cannot fix it either when it runs under a no-shell profile (the shipped claude adapter's, which cannot execute scripts/pin-formula.sh) — so the deterministic driver would rebase, re-verify, fail, rework, re-dispatch and loop forever, burning an engine attempt per pass, with the FAIL text naming the one command nobody in the loop is able to run. Only an operator re-pin breaks it. This is the single highest-value thing found in this run for genuine unattended operation. Candidate fixes to weigh: have orchid merge's rebase arm re-pin as part of the same transaction; make the pin check advisory rather than fatal when the only delta is the pin itself; or teach the driver to run pin-formula (a deterministic, argument-free script) before declaring a verify failure. Related: [[orchid-t004-formula-freshness]].
evidence: 
first: 2026-08-05T17:55:33Z
last_confirmed: 2026-08-05T17:55:33Z
invalidate_when: Formula/orchid.rb's pin stops being recomputed from the tree, or orchid merge stops rebasing onto a moved base

## L010 [active] repo
statement: ENGINE INDEPENDENCE AND REVIEW DEPTH ARE DIFFERENT AXES, and this run has direct evidence. On T003 the engine-independent slot (agy) APPROVED a candidate whose primary acceptance criterion was unmet, with a one-sentence unsupported rationale and a null findings array; the session-independent slot (claude, the implementer's own engine) caught it, cited files and line numbers, and cross-checked the new code against the kernel functions it mirrors. The arbiter confirmed the claim in the code and rejected. Why: agy is an INLINE, diff-only reviewer by design (plugins/engines/agy/run — 'no tools, no worktree', print mode auto-denies tools), so it judges from diff text alone and cannot open the file a change must stay consistent with, run a verb, or check a claim against existing behavior. Its independence is real and worth having; its depth is bounded by what a diff shows. PRACTICAL RULES: (1) never treat an agy approve as sufficient evidence on its own for a task whose criteria involve interaction with existing kernel behavior; pair it with a worktree-capable slot, which review.medium/high already do; (2) at arbitration, weigh a specific, checkable, file-and-line rationale above a generic approve REGARDLESS of which slot is engine-independent — and verify the specific claim yourself before acting on it; (3) an approve carrying no findings and no specifics is a signal to look harder, not a clean bill of health. Do NOT read this as a reason to drop agy: on a diff it can actually inspect it is the only genuine engine independence available when codex is out, and independence protects against a failure mode depth cannot.
evidence: 
first: 2026-08-05T20:06:04Z
last_confirmed: 2026-08-05T20:06:04Z
invalidate_when: agy gains worktree access or a review prompt that requires per-finding evidence, or review.<tier> stops pairing an inline-only reviewer with a worktree-capable one

## L011 [active] repo
statement: T009 closed the inbound-visibility half of the lost-phone-answer incident: doctor now runs a plugin-declared return-leg probe and distinguishes reachable, unreachable and unknown. The remaining defect is durable refusal evidence and ownership: nonce, allowlist and differing-answer refusals can still die only to caller stderr, and an operator must not answer locally while a notified qid is still owned by the remote channel. Journal every refused answer attempt with qid, offered choice and reason, and let remote ownership settle before any local answer.
evidence: T009 merged the doctor inbound probe; libexec/orchid-answer still has unjournaled nonce, allowlist and differing-answer orchid_die paths at the 2026-08-30 T015 audit.
first: 2026-08-06T02:55:16Z
last_confirmed: 2026-08-30T19:11:04Z
invalidate_when: every refused answer attempt is durably journaled and a notified qid has an explicit ownership or settlement rule that prevents a local answer racing the remote reply

## L012 [active] repo
statement: THE BLOCKER LIST HAS NO WAY TO SHRINK, and the phone reply is too long to type. Three separate gaps, all found live 2026-08-06 while making the operator's phone workflow usable. (1) BLOCKERS.md is APPEND-ONLY: orchid answer writes runtime/answers/<qid>.answer and never prunes the entry, so an answered question still looks open to anything reading that file — which is the operator-facing surface the docs point at. (2) An EXPIRED question can never be cleared at all: orchid answer refuses anything unanswered longer than answer_expiry_s (default 86400s) with 'has expired', and no verb dismisses or reaps it, so it is stuck in BLOCKERS.md permanently — this run carries two such entries (q-4-df78, q-4-2711) for tasks that completed and merged days ago. Distinguishing open from settled therefore requires cross-referencing runtime/answers/ AND the question file's mtime against answer_expiry_s; the blocker file alone cannot tell you what is waiting on you. Needs a dismiss/reap path, or expiry-aware pruning. (3) The reply line orchid notify composes is built for correctness from any cwd, not for thumbs: ORCHID_REPO=<abs path> orchid answer <qid> <choice> --nonce <hex>. The nonce is pure noise whenever answer_allowlist is unconfigured (libexec/orchid-answer only checks it when an allowlist exists) yet notify emits it unconditionally, and ORCHID_REPO is only there because answer resolves the run from cwd. A phone-side wrapper reduces it to 'oa <qid> <choice>'. hermes send is plain text only — no polls, buttons or inline keyboards — so a tap-to-answer flow needs a bot handler to receive the callback, which orchid does not have. That is the real fix for phone UX and it is a feature gap, not a configuration mistake.
evidence: 
first: 2026-08-06T04:19:32Z
last_confirmed: 2026-08-06T04:19:32Z
invalidate_when: orchid gains a blocker-dismiss verb or prunes BLOCKERS.md on answer/expiry

## L013 [retired] repo
statement: SHELL INJECTION IN SHIPPED ORCHID 1.0.0 — the most serious defect this run has found, and it is PRE-EXISTING, not new work. lib/common.sh:548 (verb_lock_acquire's owner-reading path) builds a shell fragment from a lock's owner.json and evals it, @sh-quoting .hostname and .pid_start but passing .pid through 'tostring' UNQUOTED: eval "$(jq -r '\"pid=\" + (.pid|tostring) + \"; host=\" + (.hostname|@sh) + ...')". PROVEN, not theorized: an owner.json carrying {\"pid\":\"0; touch /tmp/orchid-injection-proof\"} generates 'pid=0; touch /tmp/orchid-injection-proof; host=...' and eval executes it — reproduced end to end on this machine. ATTACK PATH: a hostile repository commits .orchid/runtime/lock/owner.json or .orchid/runtime/verb-lock/owner.json (the .gitignore entry stops accidental tracking, not a deliberate 'git add -f', and a clone carries the file), the operator points any lock-taking verb at that repo, and arbitrary commands run as the operator. Most durable-mutating verbs take the verb lock, so the surface is broad, and it needs no unattended-trust acknowledgement — it fires on ordinary interactive use. This lands squarely inside Track 1's own threat model ('treat target-repository content as potentially prompt-injecting input, including the special risk of an orchestrator that can invoke shell commands') and is exactly the class that model exists to stop; it was simply never audited for shell injection specifically. libexec/orchid-start:166 reproduces the same pattern because T003's implementer copied the idiom from lib/common.sh. FIX BOTH, and do not merely add @sh to .pid: drop eval entirely and read each field into its own variable via separate jq invocations, so no repository-controlled bytes ever reach the shell as code. Then assert it: a test with a crafted .pid must prove no command runs.
evidence: 
first: 2026-08-06T04:22:26Z
last_confirmed: 2026-08-06T04:22:26Z
invalidate_when: every owner.json read stops using eval, or a test asserts a crafted .pid cannot execute

## L014 [retired] repo
statement: A TEST FIXTURE CAN COMMIT INTO THE REPO UNDER TEST, and it did. tests/test_init_doctor.sh line 3 is 'cd $WORK || exit 1' followed by 'git init -q .' and 'git commit -q --allow-empty -m root', then later 'git add -A && git commit -m fixture: engines + config' after overwriting orchid.config with a six-line fake (verify=true, role.*=fake). In bash, 'cd ""' SUCCEEDS and does not change directory, so any invocation where $WORK is unset or empty runs git init and two commits in whatever cwd the caller had. That is not hypothetical: it happened on task/T006 on 2026-08-06 when the operator ran an instrumented copy of the file under a different name, which changed $0's dirname and therefore how $WORK was derived. The branch silently gained commits 'root' and 'fixture: engines + config', and HEAD's orchid.config became the fixture — verify=true, so every later orchid verify on that branch would have passed WITHOUT RUNNING ANY TEST. TWO DURABLE LESSONS. (1) Guard it: assert $WORK is non-empty AND a directory this run created, before the first cd and before any git write; every fixture in the suite that does git init/commit needs the same guard. (2) OPERATOR DISCIPLINE, the more expensive half: after ANY diagnostic run inside a task worktree, check 'git log' and 'git status' — restoring a dirty FILE is not the same as checking whether COMMITS were created, and the operator here restored the file, declared the worktree clean, and was wrong. An independent reviewer caught it. Instrument OUTSIDE the worktree, or on a throwaway clone, never in the candidate's own tree.
evidence: 
first: 2026-08-06T07:12:44Z
last_confirmed: 2026-08-06T07:12:44Z
invalidate_when: tests/test_init_doctor.sh guards $WORK before its first cd, or the suite stops running git init/commit in fixtures

## L015 [active] repo
statement: TWO TRAPS THE ARBITER FELL INTO ON 2026-08-06, both caught by review, both worth avoiding by anyone editing this repo. (1) assert_match IS grep -E, so ANY ERE metacharacter in the pattern changes its meaning: 'FINDING: <low|medium|high>: <title>' parses as the alternation 'FINDING: <low' OR 'medium' OR 'high>: <title>', and therefore matches any text containing the bare word 'medium' — proven live against 'blocking_severity (medium, by default)', a string the help already contained, so the assertion passed on help text with no FINDING line shape in it at all. tests/test_engine_claude.sh:75-77 documents this exact hazard and uses a plain-substring prefix instead. Assert a metacharacter-free substring, or escape deliberately; never paste a syntax example containing | < > ( ) [ ] . * + ? into assert_match. (2) FIXING A STALE CLAIM IS NOT THE SAME AS MAKING A TRUE ONE. A help string wrongly said the severity gate was inert for all adapters; the fix said 'the shipped default reviewer now does' parse findings — but the default reviewer is agy (lib/resolver.sh:12), which writes no findings key, and only the claude adapter gained the capability. A stale claim became an over-claim, in the one place an operator reads at the moment it matters, in a run whose Track 1 exists to stop exactly that. Before asserting what a DEFAULT does, resolve the default (lib/resolver.sh) and check that specific adapter, rather than reasoning from the change just made. GENERAL: operator edits to a candidate, made to save an implementer cycle, cost one anyway here AND introduced two defects. Prefer routing unless the change is provably mechanical, and re-run the exact tests that cover it before committing.
evidence: 
first: 2026-08-06T08:16:08Z
last_confirmed: 2026-08-06T08:16:08Z
invalidate_when: assert_match stops using grep -E, or the arbiter stops making operator edits to candidates

## L016 [retired] repo
statement: A GATE NOBODY RUNS IS NOT A GATE. T004 built scripts/ci-local.sh — bash syntax, the full suite, invariants, docs checks and a zero-warning ShellCheck policy — and its own verification_commands ran it. No task dispatched afterwards did: T001, T002, T003 and T006 each merged with a chain that never invoked it, verified by inspecting every task file (only T004 and T005 mention ci-local). Seventeen ShellCheck findings accumulated across those merges, and this run's own run-level acceptance criterion 'The configured ShellCheck gate passes' was failing on the integration branch the whole time, with every individual task's suite green. It surfaced only when the release-gate task wired the gate into its own chain rather than assuming it was already running. LESSON: a per-task verification_commands chain is authored per task, so a repo-wide gate added mid-run reaches only the tasks written after it AND told about it. When a task's deliverable is a GATE, the same task must also make every future task run it — put it in the merge path, in a shared chain fragment, or in a plan-time template — because 'it exists in the repo' and 'it runs' are different facts. GENERALLY: when a task ships an enforcement mechanism, ask what makes the enforcement fire, and treat the answer 'someone will include it' as no answer at all.
evidence: 
first: 2026-08-06T10:29:35Z
last_confirmed: 2026-08-06T10:29:35Z
invalidate_when: every task's verification_commands includes scripts/ci-local.sh, or the merge path runs it

## L017 [retired] repo
statement: A LINT GATE THE IMPLEMENTER CANNOT RUN IS A GATE IT CANNOT SATISFY EXCEPT BY LUCK. T005 was told twice to fix two ShellCheck findings in a file it had written. Attempt 4 wrote a documentation paragraph and changed nothing else; attempt 5 edited the file with 18 insertions and touched neither offending line. Not negligence — the shipped claude implementer profile cannot execute shellcheck, so it never saw WHICH lines were flagged and was guessing from a summary. The findings were both SC1010, a bare word 'done' read as the loop keyword, at two specific lines; quoting the literals fixed them in seconds once someone could see them. This is the same structural class as the formula pin and the exec bit: a mechanical, deterministic operation the in-loop actor provably cannot perform. TWO REMEDIES, either sufficient: put the exact 'file:line: RULE: message' text into the rework brief every time a lint gate fails, since the failure output already contains it and the operator is the one reading it; or recognize lint fixes as an operator hand-off for no-shell profiles and stop routing them. What must NOT happen is what happened here — two full attempt cycles spent asking an actor to fix something it cannot see. RELATED SEQUENCING TRAP found at the same moment: a task whose verification_commands includes a WHOLE-TREE gate (scripts/ci-local.sh) cannot pass while any inherited finding remains anywhere in the tree, even in files it never touched — so such a task is implicitly blocked on whatever other task is clearing those, and that dependency is invisible in depends_on.
evidence: 
first: 2026-08-06T11:32:46Z
last_confirmed: 2026-08-07T02:39:29Z
invalidate_when: the implementer profile gains shell access, or rework briefs carry exact lint file:line:rule text

## L018 [retired] repo
statement: THE STALE-CHECKOUT WARNING IS NOT COSMETIC: IT MEANS ORCHID IS RUNNING OLD CODE. bin/orchid resolves ORCHID_ROOT from its own location, so every verb, every lib/*.sh, every runners/* and every plugins/engines/*/run the launcher executes comes from the integration checkout's WORKING TREE — not from the branch head. When that checkout is stale (the branch pointer advanced from outside it, which is exactly what 'orchid merge' does by design with update-ref alone), the tooling driving the run is the PRE-MERGE code, indefinitely, while every merge appears to succeed. Observed live 2026-08-06: T006's review-adapter fix merged, and reviewer findings[] stayed empty for two further rounds because the launcher kept executing the pre-T006 adapter from the stale working tree — 'grep -c FINDING plugins/engines/claude/run' gave 6 on disk against 10 at HEAD. The operator had seen 'WARNING: integration checkout is stale' on the very first command of the session and filed it as the known F7 cosmetic trap. It is not cosmetic: for an entire day, none of the merged improvements were in effect for the code actually running the run. REMEDY, immediate: refresh with 'git checkout HEAD -- . \':(exclude).orchid\'' after every merge — the exclude is essential, since a bare refresh would clobber uncommitted durable run state, which is the r-001 incident. REMEDY, structural, and this is the real fix: either make the verbs refuse to run from a checkout whose working tree does not match HEAD, or resolve ORCHID_ROOT from HEAD, or have 'orchid merge' refresh the other checkouts of the branch it just advanced (the m3 ledger already lists that third option and it was never scheduled). A warning that must be obeyed but is never enforced will be ignored, and was.
evidence: 
first: 2026-08-06T13:46:33Z
last_confirmed: 2026-08-06T13:46:33Z
invalidate_when: orchid refuses to run from a stale integration checkout, or the launcher resolves ORCHID_ROOT from HEAD rather than the working tree

## L019 [active] repo
statement: A CI WORKFLOW THAT HAS NEVER RUN IS NOT A GATE, AND 'the local gate passed' IS NOT 'CI passed'. r-001's T004 added .github/workflows/ci.yml and its acceptance criterion was that the CONFIGURATION is valid with Linux/macOS jobs — not that it ever executed. Nothing was pushed for the whole run, so the workflow first ran on 2026-08-06 when the branch was finally pushed for PR #11, and it FAILED on ubuntu-latest and macos-latest, on all three runs that exist. Meanwhile the operator's run-level acceptance was recorded as PASS on the strength of a local 'scripts/ci-local.sh' run. Both statements were true and they were about different things. TWO ROOT CAUSES, both worth generalizing. (1) THE SUITE IS NOT HERMETIC: tests/test_plugins_test.sh asserts binaries_present for codex, claude and agy, which are installed on the author's machine and absent on a runner — so the local gate passes for an environmental reason the CI cannot reproduce. Any suite that can pass because of what happens to be installed is measuring the machine, not the code; PATH-restrict a run and see what actually survives. (2) A CORRECT FIX THAT WAS NEVER GENERALIZED: 'stat -f %m || stat -c %Y' selects the platform on exit status, but GNU stat -f is filesystem mode and can succeed with non-numeric output, so the fallback never fires and the following arithmetic dies under set -u ('File: unbound variable'). T005's implementer diagnosed precisely this and fixed it correctly at libexec/orchid-start:162-165, with the rationale in a comment — and five other sites still carry the raw form, including lib/common.sh:465 which every durable verb's lock acquisition runs through. Same shape as L016: the mechanism existed and nothing made it apply beyond the file that found it. RULE: when a task fixes a portability or safety idiom, grep the tree for the same idiom in the same commit, or file the sweep as its own task — do not leave the fix stranded in the file that discovered it.
evidence: 
first: 2026-08-08T23:25:42Z
last_confirmed: 2026-08-08T23:25:42Z
invalidate_when: CI runs green on main, or a task's acceptance requires an actual CI run rather than a valid config

## L020 [retired] repo
statement: THE ENGINE-ADAPTER STREAMING LIVENESS ASSERTION IS TIMING-SENSITIVE AND FLAKES, and it has now cost time in two consecutive runs. tests/test_engine_claude.sh and tests/test_engine_agy.sh both assert 'job log must have grown WHILE the adapter was still running (was 0 bytes at the midpoint)' — a stall-detector signal that depends on the stub producing output before an arbitrary midpoint sample. r-001's dogfood notes recorded it flaking once and passing 3/3 isolated; r-002 hit it twice inside a single T013 verification, once in the outer suite and once in the nested vendor-CLI-free suite that tests/test_hermetic_suite.sh runs, where everything is slower and the race widens. Measured: the same assertions pass 3/3 in isolation and the hermetic harness passed cleanly on a re-run with no code change. CONSEQUENCE, which is the expensive part: a flaky assertion inside a whole-suite gate makes VERIFICATION itself unreliable, so a good candidate is sent to rework and burns an attempt for nothing — T013 and T014 between them lost several attempts to this and to the pin hand-off. TREAT A LONE STREAMING/LIVENESS FAILURE AS SUSPECT: re-run the file in isolation before routing a rework, and never let it consume an attempt without that check. The durable fix is to make the assertion wait for a deterministic signal rather than sampling at a midpoint, and it belongs in the same family as the r-001 pipefail flake (L005) — a test whose result depends on scheduling rather than on the code under test.
evidence: 
first: 2026-08-10T00:16:04Z
last_confirmed: 2026-08-10T00:16:04Z
invalidate_when: the streaming liveness assertion becomes deterministic, or the engine adapters stop asserting mid-run log growth

## L021 [active] repo
statement: REVIEWERS CANNOT TELL AN OPERATOR HAND-OFF FROM AN IMPLEMENTER VIOLATION, and will file the correct action as a defect. On r-002 T013 a reviewer returned a HIGH finding — 'Formula/orchid.rb re-pinned despite the mandatory operator hand-off, with a checksum this session could not have computed' — which is accurate on its facts and wrong in its conclusion: the implementer indeed could not have computed that checksum, because the OPERATOR computed it, which is exactly what the task's own mandatory hand-off clause instructs. The candidate carries operator commits (the formula re-pin, exec bits) interleaved with implementer commits, the review pack shows only a diff, and nothing marks which is which. Consequences: a spurious high finding drives a review-conflict boundary, costs an arbitration, and — worse — teaches whoever reads the record that the hand-off rule was broken when it was followed. This compounds the L017 hand-off design: the more work correctly moves to the operator, the more of the candidate looks like a violation. FIX DIRECTION: mark hand-off commits so they are identifiable (a trailer, a convention, or a field the pack builder reads) and state in the review pack which commits were operator-performed and why, so a reviewer can judge the implementer's work rather than the operator's. Until then, an arbiter must check WHO made a flagged commit before treating it as a defect.
evidence: 
first: 2026-08-10T01:58:15Z
last_confirmed: 2026-08-10T01:58:15Z
invalidate_when: review packs distinguish operator hand-off commits from implementer work

## L022 [retired] repo
statement: PER-CANDIDATE FORMULA PINNING DEADLOCKS EVERY MERGE AFTER THE FIRST. Formula/orchid.rb carries a single content-derived checksum line, and T004's freshness gate made that pin part of EVERY task's verification chain — so every candidate rewrites the same line to a different value. The first task merges fine; the second and every one after it hits 'CONFLICT (content): Merge conflict in Formula/orchid.rb' during merge's stale-base rebase. libexec/orchid-merge:164 aborts the rebase and advances the task to rework, rework dispatches the implementer, and the implementer provably CANNOT resolve it: under the restricted profile (L017) it cannot run git, cannot run scripts/pin-formula.sh, and cannot compute a SHA-256. It therefore produces no commit, the candidate is unchanged, merge is retried, and the same conflict recurs forever. On r-002 this cost T013 a full cycle once T014 became the first merge, and it would have deadlocked all 27 remaining tasks identically. THE DEFECT IS THE DESIGN, NOT THE CONFLICT: a derived artifact must not be pinned per candidate. The pin belongs on the integration branch, regenerated after each merge (or at release), so no task branch ever touches the file and there is nothing to conflict. WORKAROUND UNTIL FIXED: rebase the task branch onto the integration head BEFORE re-pinning, dropping any existing pin commit, so the pin commit never participates in a rebase.
evidence: 
first: 2026-08-10T02:57:40Z
last_confirmed: 2026-08-10T02:57:40Z
invalidate_when: Formula/orchid.rb is no longer pinned per task candidate — the pin is regenerated on the integration branch after merge, or the freshness gate no longer runs in a task's verification chain

## L023 [retired] repo
statement: REWORK DELETES THE EVIDENCE IT TELLS THE IMPLEMENTER TO READ. libexec/orchid-task:346 removes reviews/<id>-verify.log on any advance to rework, while the same advance journals the reason 'verify failed: see .orchid/reviews/<id>-verify.log'. The pointer is dangling the instant it is written, so every rework driven by a verification failure — the MOST common rework loop, as the code comment itself says — reaches the implementer with no failure output at all. The implementer then re-implements blind, guesses, and burns an attempt. Observed on r-002 T023: it was demoted to rework, the log was gone, and re-running its verification by hand showed a single actionable line ('PROTOCOL.md names orchid creates but libexec/orchid-creates does not exist') that the implementer never saw. The deletion is deliberate — it arms INV-11's gate so a stale log cannot satisfy a later advance — so the fix is not to keep the file under the same name: capture the failing output into the rework brief (or an attempt-scoped log the INV-11 gate does not accept as evidence) BEFORE deleting it.
evidence: 
first: 2026-08-10T04:44:49Z
last_confirmed: 2026-08-10T04:44:49Z
invalidate_when: advancing a task to rework preserves the verify evidence, or the journalled reason no longer points at a file the same call deletes

## L024 [active] repo
statement: THE SUITE IS NOT ISOLATED FROM A CONCURRENTLY RUNNING ORCHID, SO VERIFICATION IS FLAKY BY DESIGN. On r-002, candidate 4bb8d03 of T013 was verified three times with no change to the tree: PASS at 03:44, FAIL at 04:45, PASS again at 05:20 when re-run by hand with nothing else touching the repository. The failing run reported roughly twelve tests/test_notify_channel.sh assertions — the openclaw stub never invoked, the outbox never written — none of which depend on the tree. The distinguishing factor is that the two drive-run verifications ran while the drive loop kept polling the repository every 20 seconds, because the driver dispatches verification as a job and continues its passes; the clean re-run had no concurrent orchid. tests/test_notify_channel.sh reaches the operator's REAL $HOME for plugin trust state, so ambient mutation by another orchid process can change what it observes. CONSEQUENCES: a green suite means 'green while the machine was quiet', which is not what CI or a beta qualification claims; and because a spurious failure charges an attempt, flakiness alone walked T013 to 'attempts exhausted' — a block with no supported recovery. This is a hermeticity defect of the same family as the vendor-CLI one, and the hermetic proof does not currently cover it: it removes binaries from PATH but says nothing about ambient state or concurrent writers.
evidence: 
first: 2026-08-10T05:02:24Z
last_confirmed: 2026-08-10T05:02:24Z
invalidate_when: the deterministic suite passes repeatably while a drive loop is polling the same repository, or verification no longer runs concurrently with driver passes

## L025 [retired] repo
statement: VERIFY EVIDENCE CAN BE BOUND TO A COMMIT THAT WAS NEVER THE ONE VERIFIED, WHICH DEFEATS INV-11 FROM THE INSIDE. runners/orchid-drive:634 captures candidate_sha by reading the task worktree's HEAD at the moment it reconciles the implementer envelope, and libexec/orchid-verify then runs the verification command in that same LIVE worktree — at whatever HEAD it holds when the command actually runs, which is not necessarily the captured one. On r-002 T013 the two diverged by 19 minutes: drive advanced implementing -> testing at 05:04:31Z with candidate_sha 4bb8d03, the implementer job kept running and committed 680cfc0 at 05:23:10Z, and the verification that finished at 05:50:15Z therefore exercised 680cfc0 while its evidence header recorded 'sha: 4bb8d03'. Because INV-11's gate on advance testing -> reviewing compares the evidence's recorded sha against the task's CURRENT candidate_sha, and both were 4bb8d03, a PASSING run would have sailed through the gate and certified one commit on the strength of executing a different one. It failed here only by luck — the extra commit made Formula/orchid.rb stale. TWO DEFECTS, both needed: the envelope is treated as a completion signal while the job is still alive and still committing, and verification trusts the worktree instead of the candidate it claims to be verifying. Verification must refuse to run when HEAD does not equal the recorded candidate_sha.
evidence: 
first: 2026-08-10T05:52:47Z
last_confirmed: 2026-08-10T05:52:47Z
invalidate_when: verification refuses to run when the worktree HEAD differs from the task's recorded candidate_sha, or the implementer envelope is only reconciled once its job has actually exited

## L026 [retired] repo
statement: A JUDGMENT BOUNDARY ON ONE TASK MUST NOT HALT THE WHOLE RUN. runners/orchid-drive exits 16 as soon as ANY task needs an operator, and the natural reading of that exit — the one this operator's pump implemented, and the one PROTOCOL.md's unattended story invites — is 'stop and fetch a human'. On r-002 that cost seven hours: the pump stopped at T013's review conflict at 07:32Z, the operator arbitrated it within minutes but did not restart the pump, and twenty-eight tasks that had nothing to do with T013 sat idle until 14:42Z. The boundary belonged to ONE task; every other task was dispatchable the entire time. Exit 16 means 'a decision is outstanding somewhere', not 'nothing further can be done', and drive is idempotent, so a boundaried task simply re-reports each pass at no cost. An unattended pump must therefore report the boundary through the notify channel and CONTINUE, halting only when it can make no progress at all. This is not a driver defect — drive did the right thing and reported honestly — it is a defect in what the pump concludes from the report, and it goes directly to the run's claim that unattended operation is real: a pump that stops on the first arbitrable disagreement is attended operation wearing an unattended label.
evidence: 
first: 2026-08-10T15:27:35Z
last_confirmed: 2026-08-10T15:27:35Z
invalidate_when: the unattended pump continues past a judgment boundary on one task instead of halting, or drive's exit 16 is no longer treated as a whole-run stop signal

## L027 [retired] repo
statement: A REVIEW PLAN RECOMPUTED FROM LIVE ENGINE HEALTH CAN STRAND A TASK IN 'reviewing' WITH NO LEGAL EXIT. On r-002 T024 the driver collected two valid, candidate-bound review envelopes — agy approve, claude request-changes — and then refused to reconcile them: 'judgment boundary [review-evidence]: 2 of 2 review envelope(s) bound to the current candidate, but slot(s) 1 have no review of their own — the tier's engine independence is unproven'. The envelopes were not stale and not wrong. What changed underneath them was the PLAN: 'orchid jobs review-plan T024' is computed live from engine health, and agy reached status failing (3 consecutive failures) AFTER its review was filed, so the plan for slot 1 (engine-independent) moved from agy to codex-review and the agy envelope stopped counting for the slot it was produced for. NOTE, because the obvious diagnosis is wrong: this is NOT a stale rate-limit. codex-review displays 'rate_limited until 2026-08-04T20:32:21Z', six days past, but lib/ledger.sh:75 computes availability as 'rate_limited_until <= now', so an expired window IS available — only the cached status STRING is stale, which is a reporting bug, not an exclusion. THE TRAP IS THE STATE MACHINE: the feature archetype declares reviewing -> arbitrating as the only forward edge, and the driver gates that edge on slot coverage, so a task whose plan no longer matches its evidence cannot advance, cannot go back to rework, and cannot be arbitrated ('T024 is not arbitrating (status: reviewing)'). It sits until an operator intervenes, and nothing in the boundary text says which of the two — re-dispatch the newly-required engine, or accept the evidence already gathered — is intended. Either pin the slot plan for the life of an attempt so collected evidence keeps counting, or give a task in this state a legal exit that does not require hand-editing durable state.
evidence: 
first: 2026-08-10T23:40:36Z
last_confirmed: 2026-08-10T23:40:36Z
invalidate_when: review slot assignment is pinned for an attempt, or a task whose recomputed plan no longer matches its collected envelopes has a path forward that is not an operator edit

## L028 [active] repo
statement: ORCHID'S CHARACTERISTIC DEFECT IS THE UNESCAPABLE STATE, and r-002 found four independent instances of it in two days. Not four bugs — one shape, four mechanisms. (1) L022, the merge deadlock: every candidate must re-pin one content-derived line, so the second and every later merge conflicts, routes to rework, and the implementer provably cannot resolve it, forever. (2) L027 / T039, the reviewing dead end: a review plan recomputed from live engine health orphans evidence already collected, and the only forward edge is gated on that evidence, while no backward edge exists — a task sits in reviewing with no legal move at all. (3) T026, attempt exhaustion: the budget is a hardcoded 'attempts >= 3' in the driver, 'attempts' is in task set's kernel-owned deny-list, and unblock and retry both route to rework WITHOUT resetting it, so a task that exhausts its budget for environmental reasons can never be recovered. (4) T027's own proposed fix: an exit-17 refusal in PLANNING, a phase where jobs gc never runs, so the stranded manifest is never reaped and the refusing condition never clears. THE COMMON SHAPE: a guard that is individually correct — do not merge a conflicted tree, do not reconcile unproven independence, do not retry forever, do not launch twice — combined with no defined exit when the guard fires under conditions its author did not picture. Each was written defensively and each converts a recoverable situation into a permanent one. TWO CONSEQUENCES WORTH STATING. The operator's only escape in every case was to edit durable state by hand or to hand-craft an override, which this run's own constraints forbid and which an UNATTENDED run cannot do at all — so each of these silently converts 'unattended' into 'attended'. And the fourth instance arrived inside a FIX for the third class, which is the strongest evidence that this is a pattern in how the system is designed rather than a set of unrelated oversights. DESIGN RULE THIS ARGUES FOR: a guard that can refuse must name, at the point of refusal, the supported action that clears it — and if no verb provides that action, the guard is not finished.
evidence: 
first: 2026-08-11T02:23:21Z
last_confirmed: 2026-08-11T02:23:21Z
invalidate_when: every state a task or run can enter has a documented, verb-supported exit, and a test asserts that no reachable state requires editing durable state by hand

## L029 [retired] repo
statement: TEXTUAL GATES CHARGE TASKS FOR WRITING ABOUT CODE, AND ON r-002 IT COST FIVE ATTEMPTS AND STRANDED THREE TASKS. Five instances, all the same shape — a gate matching text that DESCRIBES an operation rather than performing one: scripts/ci-local.sh's documentation gate read the prose 'orchid creates' as a verb invocation and demanded libexec/orchid-creates (T023); the same gate later read 'no orchid verb ever runs a verification suite there' as a verb named 'verb' and stranded THE SAME TASK a second time (T023, PROTOCOL.md:799); INV-13's scan matched 'git worktree add' inside a printf of a human-readable diagnostic (T019); ShellCheck parsed backticks inside a green_case annotation as command substitution (T017); and ShellCheck read a bare 'done' passed as a string argument as the loop keyword (T006). THREE OF THOSE FIVE EXHAUSTED A TASK'S ENTIRE ATTEMPT BUDGET. Not one was a defect in the behaviour under test. TWO CONSEQUENCES THAT MATTER MORE THAN THE COST. First, the natural workaround makes the product worse: the implementer is told to reword a diagnostic, quote a keyword, or avoid a token, so error messages and test annotations are degraded to satisfy a linter, and the better the prose the likelier it trips. Second, and worse, the pressure is to EDIT THE GATE — which is exactly what a run about gates that fail closed must never do, so the operator has had to forbid it explicitly in every brief. FIX DIRECTION: for the gates orchid owns, ignore string literals and prose as well as comments, or key the check on something structural rather than textual — and ship a RED case proving the gate still catches a genuinely undocumented verb. For gates orchid does NOT own (ShellCheck), the idioms that trip them must be named in the contributing docs so an implementer is warned before it costs a round rather than after.
evidence: 
first: 2026-08-11T05:12:54Z
last_confirmed: 2026-08-11T05:12:54Z
invalidate_when: orchid's textual gates ignore string literals and prose as well as comments, and the contributing docs name every idiom that trips an unfixable external linter

## L030 [active] repo
statement: assert_match CANNOT MATCH A PATTERN THAT STARTS WITH A DASH, AND FAILS AS THOUGH THE CODE WERE WRONG. tests/helpers.sh:41 is 'assert_match(){ grep -Eq "$1" <<<"$2" || fail "$3 (no match ...)"; }' — the pattern is grep's FIRST argument, so a pattern like '--attempts N' is parsed as a long option, grep prints its usage message and exits non-zero, and the helper reports a perfectly ordinary 'no match' failure. On r-002 T026 this consumed an attempt on a task already at 2 of 3: the assertion 'assert_match "--attempts N"' could never pass no matter what the implementation did, and the only clue was grep's usage text buried in the verify log. THE FIX IS ONE TOKEN — 'grep -Eq -e "$1"' — but it is a shared helper the entire suite calls, so it must be changed deliberately with the suite re-run, not slipped in by a task at its budget limit. This is the same family as the run's other textual-gate defects (L029) and as r-001's L005 SIGPIPE fix in this same helper: assert_match has now produced two distinct classes of false failure, which argues for reviewing every helper that wraps a command in an assertion for arguments it can misparse.
evidence: 
first: 2026-08-11T05:53:49Z
last_confirmed: 2026-08-11T05:53:49Z
invalidate_when: tests/helpers.sh's assert_match passes its pattern after -e, so a pattern beginning with a dash is matched rather than parsed as an option

## L031 [retired] repo
statement: THE OPERATOR'S WORKAROUND FOR THE HAND-OFF GAP COST MORE ATTEMPTS THAN ANY PRODUCT DEFECT IN THE RUN. Because the implementer profile cannot execute anything (L017) and the formula pin is part of every task's verification chain (L022), the operator's supervising loop re-pins Formula/orchid.rb on each pass. That workaround stranded THREE task-attempts by itself. T021 lost its entire budget to a single failure, 'Formula/orchid.rb checksum is stale for the current tree', with nothing else wrong in the candidate. T006 lost two rounds the same way, and its timeline shows the mechanism exactly: the operator's pin commit landed at 01:13:49, the implementer committed at 04:24:40 — three hours later — and the loop then skipped re-pinning that worktree on every pass because a guard refused to touch a worktree whose job was still alive; when the job finally exited, the driver reconciled and verified in ONE pass against a pin four hours stale. The same run also recorded 'candidate: ec054ba' while the worktree HEAD was 89ca781, so it produced the L025 mis-binding at the same time. TWO CONCLUSIONS. First, the guard was wrong and is fixed: job liveness must gate the REBASE, which rewrites history under a live process, but not the PIN, whose commit touches exactly one file the implementer is forbidden to touch and therefore cannot clobber anything. Second and more important: a gap whose workaround is itself a defect generator is not a papercut. Every hour the pin stays inside the per-task verification chain, an operator script races an implementer over one line of a file neither of them cares about, and tasks lose budgets over it. That is the argument for removing the pin from the chain rather than automating the hand-off better.
evidence: 
first: 2026-08-11T09:55:24Z
last_confirmed: 2026-08-11T09:55:24Z
invalidate_when: the formula pin is no longer part of a task's verification chain, or the kernel performs the hand-off itself so no operator workaround is needed

## L032 [active] repo
statement: A CANDIDATE IS FROZEN FROM REVIEW DISPATCH THROUGH ARBITRATION. Any operator or automation commit in reviewing, arbitrating or merging changes the candidate SHA and invalidates evidence already paid for; do candidate-local mechanical work before review, then reverify and review the final SHA. T030 removed the historical per-candidate Formula pin that triggered this incident, so no current procedure should re-pin a task candidate, but the SHA-freeze rule applies to every other write.
evidence: r-002 T024 lost two envelopes when the old supervisor pinned during reviewing; T030 removed candidate pinning, while INV-11 and pinned review plans retain the general SHA-bound evidence rule.
first: 2026-08-11T19:47:20Z
last_confirmed: 2026-08-30T19:11:05Z
invalidate_when: the kernel refuses candidate writes while review evidence is outstanding, or verification and reviews stop binding to an exact candidate SHA

## L033 [active] repo
statement: AN OPERATOR TOOL THAT REWRITES FILES MUST PRESERVE THEIR MODES, AND AN OPERATOR WHO CANNOT TELL THEIR OWN DAMAGE FROM THE SYSTEM'S WILL BLAME THE SYSTEM. The conflict resolver written during r-002 wrote every file it touched through 'awk ... > tmp && mv tmp file'. The redirection creates the temp at the default 644 and mv then replaces the original, so each executable it resolved silently lost its exec bit. Three files were hit: T025's libexec/orchid-task, whose loss made every verb fail with 'orchid: unknown command task' and cascaded into 176 failures that stranded the task; T027's tests/test_drive.sh; and T019's runners/orchid-drive, whose loss produced rc=126 Permission denied and 116 failures. THE SECOND HALF IS WORSE THAN THE FIRST. When T019 stranded, the operator diagnosed a missing exec bit, attributed it to an implementer hand-off, unblocked the task on that basis, and wrote a journal entry about the irony of T019 — the task whose purpose is to classify exec-bit failures as hand-offs — being destroyed by one. Every step of that reasoning was coherent and the conclusion was false: it was not a hand-off the kernel failed to recognise, it was operator damage misread as one. The mistake survived two hours and one journal entry before the same defect recurred on T025 and exposed it. WHAT THIS ARGUES FOR: any tool that rewrites a tracked file must copy the mode across (or edit in place rather than replace), and any operator diagnosis that lands on 'the implementer did something impossible' should first ask what the operator did to that worktree. The run's attribution machinery — hand-off trailers, lesson L021, the reviewer guidance — all exists to stop reviewers blaming operators for operator work; this is the mirror case, an operator blaming an implementer for operator damage, and nothing in the system catches it.
evidence: 
first: 2026-08-12T01:50:32Z
last_confirmed: 2026-08-12T01:50:32Z
invalidate_when: the operator's conflict tooling preserves file modes, and no operator-caused damage has been attributed to an implementer in the journal

## L034 [active] repo
statement: GATE TASKS OSCILLATE BETWEEN TOO PERMISSIVE AND TOO STRICT, AND EACH SWING COSTS A FULL ROUND. Three r-002 tasks did this on the same check, repeatedly. T018's capability gate first FAILED OPEN — a third-party qualified id like 'acme/foo' did not resolve, so the no-shell check passed and verification proceeded — and its fix made it FAIL CLOSED PERMANENTLY, holding every third-party candidate at a hand-off no operator action can ever clear. T019's failure classifier went from forgiving every failure while a state gap was open, to a regex too broad, to a proof that was genuinely evaluated but proved something AMBIENT (this repository contains mode-644 files with shebangs, so the condition was true by design), to a name list where an unrelated .cache directory plus any 'command not found' line waived an entire round; six rounds, every one ending on 'still forgives too much'. T026's reverify went from certifying a dirty tree, to a clean-tree refusal with an unguarded public transition beside it, to a filter a staged rename steps around. IN EVERY CASE THE REVIEWERS CAUGHT BOTH EDGES, which is the two-slot policy earning its keep — but catching a swing costs the same round as catching a defect, and an oscillation converges slowly because each fix is aimed at the last failure rather than at the boundary. WHAT THIS ARGUES FOR: a task whose deliverable is a GATE must state, in its acceptance criteria and before implementation, a PAIR of RED cases — one input the gate must refuse and one it must admit — chosen at the edges rather than in the middle. Orchid already requires a RED case for every gate (T017); this says the requirement is not sufficient, because a single RED case pins only the direction the author was already thinking about. Pin both edges and the oscillation cannot start.
evidence: T015 targeted preflight confirmed the same oscillation in candidate cb78ab6d: string elision fixed an INV-13 false positive but erased a quoted boundary path and a shell-c worktree mutation; paired production-scan RED cases were required before the full suite.
first: 2026-08-12T08:04:36Z
last_confirmed: 2026-08-30T19:11:06Z
invalidate_when: a gate task states its correct behaviour as paired RED cases (one too-permissive, one too-strict) before implementation, so both edges are pinned in advance

## L035 [active] repo
statement: R-002'S COST IS ROUNDS-PER-TASK, NOT TASKS-PER-HOUR, AND INV-07 MAKES CONCURRENCY SELF-LIMITING. Measured 2026-08-12. Eight tasks merged between 08-09 16:22 and 08-12 05:47: 61h25m wall clock, 7.7h per merged task, trend worsening (last three gaps 8h51m, 8h, 15h15m). The eleven tasks still in flight have accumulated 38 attempts between them, 3.5 rounds each, none merged — roughly 95 hours at the measured ~2.5h per verify-and-review round, spent on work that has not landed. Rounds, not scheduling, are where the time goes. FIRST STRUCTURAL REASON: 'a moved base forces re-verify plus re-review' (docs/architecture.md:150), so every merge invalidates the evidence of every OTHER in-flight task and the cost of a merge scales with the width of the run. Widening from 4 to 8 does not double throughput, it doubles the invalidation each merge causes. The machine is not the constraint — 18 cores at 19% load, verification I/O-bound at 66% CPU — so the instinct to raise 'concurrency' is wrong here and would likely have made the run slower. What DOES work is rebasing eagerly while a task is in rework or implementing, where there is no review evidence to lose and no verify has yet happened, so the later invalidation never occurs; rebasing a task in reviewing, arbitrating or merging destroys exactly what INV-07 protects (L032). SECOND: verification is not the lever either. scripts/ci-local.sh is 30m52s but its parts are individually cheap — the 14-file invariant rehearsal is 31s, the largest single test file (tests/test_drive.sh, 2208 lines) is 109s. There is no hot spot to cut; the cost is breadth, and halving it would save about an hour a day against a round count that is the real multiplier. THE LEVER THAT REMAINS is how many rounds a task needs, set by how well it was scoped and how consistently the arbiter judges it — T018 five rounds, T022 and T026 six each. L034 records the oscillation that produces this. Scoping gate tasks so they ship with PAIRED red cases is worth more to a run's calendar than any amount of parallelism.
evidence: PER-FILE TIMING, 67 test files measured individually 2026-08-12: 1927s total, matching scripts/ci-local.sh's 30m52s. tests/test_hermetic_suite.sh alone is 976s — 51% of ALL test time — because it re-runs the ENTIRE suite a second time inside an isolated HOME with codex, claude, agy, hermes and openclaw unresolvable, alongside a concurrent writer of the ambient home. Second place is tests/test_unattended_trust.sh at 121s (6%); every other file is under two minutes, and the largest by line count (tests/test_drive.sh, 2208 lines) is only 109s. The 14-file invariant rehearsal that scripts/ci-local.sh runs a second time after tests/run.sh already ran it is 31s, so that duplication is not worth removing. THIS CORRECTS THE STATEMENT'S SECOND POINT, which said there was no hot spot and the cost was breadth: there IS a hot spot and it is a nested full-suite run. Verification is roughly halvable without touching any other test, by scoping the hermetic re-run to the tests that can actually leak HOME or PATH rather than re-running all 67. What that would give up is real and must be stated if it is ever done — the hermetic suite is what catches machine-dependence, and its own failure message says machine-dependence 'is what kept hosted CI red'. The safer version of the same saving is to run it once per MERGE rather than once per candidate per round, since it certifies the environment rather than the diff.
first: 2026-08-12T10:31:55Z
last_confirmed: 2026-08-12T11:07:58Z
invalidate_when: a run's calendar is dominated by wall-clock per round rather than by the number of rework rounds each task needs, or INV-07 stops forcing re-verify plus re-review on a moved base

## L036 [active] repo
statement: A GATE THAT RUNS IN A TEMP WORKTREE CANNOT SEE BEHAVIOUR CONDITIONED ON THE CHECKED-OUT BRANCH, AND WILL PASS VACUOUSLY. Proven on r-002's integration branch 2026-08-12. T006 added a stale-ORCHID_ROOT guard to lib/common.sh that answers its first condition from file reads alone and returns early for any root not parked on the configured integration_branch — its own comment lists 'an install prefix, a development checkout, a task worktree, every root in an ordinary run' as exiting without a subprocess. Only when the root IS on the integration branch does it run 'git -C $root diff --cached --name-only HEAD', which is exactly the call tests/test_unattended_trust.sh forbids before an acknowledgement is found. Same commit 416fcc9, checked out on orchid/integration: 5 FAIL. Checked out on a branch called not-integration: 0 FAIL, rc=0. THE POINT IS NOT THE BUG BUT THAT EVERY GATE PASSED IT HONESTLY. The task's own verify ran the file and it printed nothing, which is what green looks like for it (a passing run emits zero lines). The MERGE revalidation then ran the whole suite again on the merged tree and reported CI PASS with zero FAILs. Neither was skipped, neither was stale, and re-running either a hundred times would never have caught it — because a task worktree and a merge temp worktree are BY CONSTRUCTION not on the integration branch, the one dimension the guard branches on. A revalidation environment that differs from the deployed environment in the dimension under test converts a real failure into a silent pass. WHAT THIS ARGUES FOR: any kernel code path conditioned on the checked-out branch (or on being the install root, or on any property the merge temp worktree cannot have) needs a test that CONSTRUCTS that condition explicitly rather than relying on the ambient environment, because the ambient environment of every gate orchid runs is the one where the condition is false. It also argues that run-level acceptance must include one run of the suite on the integration branch itself, since that is the only place this class shows up.
evidence: EXTENT BOUNDED 2026-08-12: all 67 test files run individually against commit 416fcc9 in a clean isolated clone checked out on branch orchid/integration. Exactly TWO fail, and they are one defect: tests/test_unattended_trust.sh (5 FAIL, 105s) and tests/test_hermetic_suite.sh (6 FAIL, 989s), the latter being the wrapper that re-runs the whole suite in an isolated HOME and therefore re-reports the same five plus its own summary FAIL. Every other file is green. Total suite 1945s. The same 67 files on the same commit checked out on a non-integration branch: zero failures. So the integration branch's red is entirely attributable to the branch-conditioned guard in lib/common.sh:444-465 and nothing else has regressed.
first: 2026-08-12T11:25:47Z
last_confirmed: 2026-08-12T12:05:22Z
invalidate_when: the merge revalidation runs in an environment that reproduces the deployed one in every dimension the kernel branches on — in particular the checked-out branch name — or no kernel code path is conditioned on being parked on the integration branch

## L037 [active] repo
statement: A BLOCKING PRECONDITION MUST NAME THE ACTOR WHO WILL MEASURE IT, OR IT COMES BACK UNMET FOREVER. r-002 T022, three occurrences across one task. The operator wrote the blocking criterion as 'a zero-failure run of tests/test_drive.sh for this candidate'. Phrased that way it reads as an instruction to whoever is judging the candidate — and no configured judge can execute anything: L017 records that the restricted implementer profile denies on the command string, and a review session is no different. So the reviewer did the only correct thing available to it and reported the condition UNMEASURED, three times, accurately, each time costing a full round on an objection it was structurally incapable of clearing. The candidate was never at fault; the drafting was. THE FIX IS ONE CLAUSE: write 'the operator will run X and record the evidence' rather than 'X must pass'. The first is discharged once, by someone who can actually run it, and never blocks another round; the second is an instruction to an actor who cannot follow it. THE GENERAL SHAPE, which is worth more than the instance: an operator authoring criteria has capabilities no participant in the run has, and will unconsciously write conditions in terms of them. Before a criterion becomes blocking, ask which slot discharges it and whether that slot can. If the answer is the operator, say so in the criterion and then actually do it — an unmeasured blocking condition is indistinguishable from a failing one to everybody downstream, and it converts operator convenience into implementer attempts. THIS IS THE SAME ERROR CLASS AS TELLING AN IMPLEMENTER TO REBUILD ON A NEW BASE when it cannot run git: an instruction that is impossible for its recipient does not produce a refusal, it produces a wrong attempt or a permanent stall.
evidence: 
first: 2026-08-12T14:00:00Z
last_confirmed: 2026-08-12T14:00:00Z
invalidate_when: every blocking precondition an operator writes names the actor who will measure it, and no review or implementer slot is asked to clear a condition it cannot execute

## L038 [active] repo
statement: AN ORCHID_* ENVIRONMENT OVERRIDE SET ON A SUPERVISOR LEAKS INTO EVERY TEST FIXTURE THAT SETS THE SAME KEY IN A FILE, AND FAILS THE SUITE FOR EVERY CANDIDATE. Operator error in r-002, 2026-08-12, caught by orchid's own merge gate. config_get resolves a key from the environment FIRST (lib/common.sh, _cfg_env_name maps 'concurrency' to ORCHID_CONCURRENCY) and only then from orchid.config, and that is by design. The operator raised run width by exporting ORCHID_CONCURRENCY=8 into the supervising drive loop, reasoning that this avoided modifying a tracked file. Every process the loop spawned inherited it — including verify and merge, which run the suite. tests/test_e2e_concurrency.sh:64 writes 'concurrency=2' into its own fixture's orchid.config and line 178 asserts the refusal names 'concurrency-cap (2/2)'; the inherited variable overrode the fixture's file, so four assertions failed in every candidate's suite run. T004's merge validation failed with 51 FAILs and the task was returned merging -> rework. THE TELL WAS IN THE REPOSITORY ALL ALONG: tests/test_jobs.sh sets ORCHID_CONCURRENCY=8 as a PER-COMMAND PREFIX on three individual invocations, never as an export. That is the correct shape and it is the shape to copy. THE FIX IS TO SET RUN-WIDE CONFIG IN THE FILE THE RUN READS, not in an environment that descends into fixtures: the run root's orchid.config is read by the driver for dispatch decisions, is not in ORCHID_KERNEL_PATHS so it does not trip the stale-root guard, and does not reach task worktrees, which carry their own committed copy from their branch. THE GENERAL RULE: an environment variable is a per-invocation instrument. Exporting one into a long-lived supervisor turns it into a global that follows every descendant into contexts that deliberately configure themselves, and a test fixture writing a config file is exactly such a context. WORTH RECORDING IN ORCHID'S FAVOUR: the merge revalidation caught this before it reached the integration branch, and merging -> rework charges no attempt, so the operator's mistake cost one merge round and no task's budget.
evidence: 
first: 2026-08-12T15:46:25Z
last_confirmed: 2026-08-12T15:46:25Z
invalidate_when: orchid config values can no longer be set by an inherited environment variable, or the test harness clears ORCHID_* from the environment of fixtures that write their own orchid.config

## L039 [retired] repo
statement: A NO-OP DELIVERY RULE THAT CANNOT SEE "THE WORK IS ALREADY DONE" BLOCKS AN ENTIRE RUN. r-002, 2026-08-12/13: T022 merged at 18:30Z and within about four hours TWENTY-SIX of the runs forty tasks were blocked, including ten that had never spent an attempt. The run has not advanced since. THE RULE, lib/drive.sh drive_delivery_is_noop and drive_delivery_floor: an ok implement envelope whose worktree HEAD still equals the delivery floor delivered nothing, where the floor is the tasks existing candidate_sha, or base_sha on a first dispatch. Its stated premise is that "the round was dispatched precisely to change that candidate, so a HEAD still sitting on it delivered nothing". THE HOLE IS IN THAT PREMISE. It assumes every re-dispatch requires a change. It cannot distinguish a round that produced NO CANDIDATE from a round that correctly produced NO NEW COMMIT on top of a candidate that already exists and is already complete. The T040 implementer said so in its own log, and was right: "I found nothing to change and made no edits — manufacturing a diff to look productive would be worse than reporting the work as delivered." That honest answer is charged as an infra failure, the implementer is relaunched, it truthfully reports the same thing again, and at infra_max the task is blocked. The evidence that this is a false positive rather than a real no-op: every blocked worktree is CLEAN and sits three to five commits AHEAD of the integration branch. A candidate demonstrably exists. HEAD equals candidate_sha but differs from base_sha, and the rule only compares against the floor, so it never asks the question that separates the two cases. THE TRIGGER WAS A REBASE, which makes this reachable by ordinary operation and not just by a lazy engine. Rebasing a task rewrites its commits, the driver re-stamps the new HEAD as candidate_sha, the next round dispatches, the implementer finds its own already-delivered work in place and reports ok without committing — and the rule fires. Any run that rebases in-flight tasks onto a moved integration branch walks into this, which is every run with concurrency above one. WHAT THE RULE SHOULD DO: when HEAD equals candidate_sha but is ahead of base_sha, a candidate exists and the round added nothing to it, which is a routing question and not a delivery failure — advance to testing, or raise a boundary, but do not spend the infra ladder. Reserve the refusal for HEAD equal to base_sha, where genuinely nothing was ever produced. THIS IS L034s OSCILLATION AT RUN SCALE: T022 was written to catch an engine returning ok with pure commentary over an untouched worktree, and the single RED case pinned that edge only. The opposite edge — an honest ok over a worktree that is already correct — was never pinned, and it cost the run twenty-six tasks in four hours.
evidence: 
first: 2026-08-13T14:04:11Z
last_confirmed: 2026-08-13T14:04:11Z
invalidate_when: the no-op delivery rule distinguishes a round that produced no CANDIDATE from a round that correctly produced no NEW COMMIT on top of an existing one, and stops charging infra failures for the second

## L040 [active] repo
statement: THE OPERATOR BRIEFS WERE A CAUSE OF THE FAILED ROUNDS, NOT JUST A RECORD OF THEM. r-002, reviewed 2026-08-16 after the user asked whether the implementers were failing because the instructions were poor. They partly were, in four diagnosable ways, each with a round lost to it. ONE, A STALE INSTRUCTION LEFT OPERATIVE. The unblock reason said the candidate was green and nothing further was being asked; the review round then found three defects, but the earlier text was still what the implementer read, so it filed "candidate complete, no changes needed", committed nothing, and blocked the task. That is dogfood finding F46 -- the authoritative instruction and its retraction living in two fields -- hit by the operator who had just triaged F46. TWO, INSTRUCTIONS THE ACTOR CANNOT FOLLOW: lesson L037, three occurrences on one task, plus telling an implementer to rebuild on a new base when it cannot run git. THREE, OFFERING A MENU INSTEAD OF DECIDING. The brief said "either corroborate the flat-CPU verdict with filesystem progress, or default cpu_stall_min_s to 0". The implementer took the cheaper branch, which was legitimate, and the operator had delegated a design decision to an actor with less context than himself. FOUR, LENGTH. T015 acceptance criteria run to 10,489 characters, T016 to 7,032, and one unblock reason to roughly 600 words with its three required changes buried mid-document under history and context. WHAT DEMONSTRABLY WORKED: the single brief that opened with THIS SUPERSEDES ALL EARLIER NOTES and then gave three numbered blocking items produced exactly the three commits asked for. Front-loading the ask worked; burying it did not. THE MISSING INSTRUCTION THAT COST THE MOST was never written at all: the implementer role here is claude,codex and it demonstrably executes, since it commits -- so it can run the suite, and the brief never told it to. A round then changed the exit-status recorder and broke fifteen salvage assertions in tests/test_jobs.sh that had passed the day before, because nothing required re-running the file that reads what the recorder writes. THE RULE: name the exact commands that must exit 0 before the round may commit, put them second after the required changes, and never make them optional.
evidence: 
first: 2026-08-16T14:14:06Z
last_confirmed: 2026-08-16T14:14:06Z
invalidate_when: operator briefs put the required actions first as a short numbered list, name the exact commands that must pass before committing, decide rather than offer options, and open by superseding earlier notes

## L041 [active] repo
statement: ORCHID HAS NO WAY TO SAY WHICH TASK MATTERS FIRST, SO THE TASKS THAT REPAIR THE RUN ARE SCHEDULED LAST. runners/orchid-drive:1238 dispatches from "task list | LC_ALL=C sort" -- plain task-ID order. depends_on expresses CORRECTNESS ordering (this needs that to exist) and is the only lever there is; nothing in the plan, the roadmap or the task schema expresses PRIORITY (do this before that, even though neither needs the other). The roadmap file contains no ordering at all. THE CONSEQUENCE ON r-002 WAS NOT SUBTLE. Six tasks in this run exist to repair defects that stop runs: T026 (retry restores status but grants no attempt budget, so exhaustion is permanent), T027 (a launch that fails before the job runs retries invisibly forever), T030 (the per-candidate formula pin conflicts on every merge after the first), T035 (no process table, so a dead job is indistinguishable from a working one), T039 (a review plan recomputed mid-flight orphans collected evidence and leaves reviewing with no legal exit), and T040 (an already-delivered candidate read as a no-op delivery). Every one of them is numbered above T025, so ID order schedules them AFTER the twenty-five tasks that need the run to work in order to finish. The run duly hit each defect while the fix for it sat pending. Nine tasks merged in eight days and five of those days were a dead run. WHAT THIS ARGUES FOR: a plan must be able to declare a bootstrap phase -- tasks whose deliverable is the runnability of the run itself -- and the driver must dispatch that phase to exhaustion before anything else, regardless of id. Failing a phase mechanism, at minimum a priority field the dispatch sort honours. THE OPERATOR WORKAROUND IS NOT A SUBSTITUTE: on r-002 the ordering exists only in the operator head and is applied by choosing which blocked task to unblock, which is invisible to the run, lost on a handover, and unavailable to an unattended one.
evidence: 
first: 2026-08-17T21:01:52Z
last_confirmed: 2026-08-17T21:01:52Z
invalidate_when: a plan can express dispatch priority or phases, so tasks that repair the run itself are scheduled before tasks that depend on the run working

## L042 [active] repo
statement: A WALLCLOCK BLOCK OUT OF REVIEWING DESTROYS REVIEW EVIDENCE ORCHID ALREADY PAID FOR. r-002 T003, 2026-08-22. The candidate passed verification with zero failures, reached reviewing, and BOTH review envelopes landed — claude approve, codex request-changes. The attempt then exceeded wallclock_budget_s while sitting in reviewing and the driver moved it reviewing -> blocked. Recovery is the problem: task unblock and task retry both route to REWORK, which invalidates review evidence by design, and blocked -> arbitrating is refused as an illegal transition. So the only supported exit from the block throws away two complete, candidate-bound reviews and re-runs a full verify-and-review cycle, roughly an hour of machine time, to rediscover verdicts already on disk. TWO SEPARATE FAULTS. First, the budget counts wall time the task spends WAITING ON REVIEWERS, which is not work the task controls and not a signal about the task at all; a slow or queued reviewer therefore blocks a healthy candidate. Second, and worse, the state machine has no edge that carries a blocked-from-reviewing task back to judgement, so the recovery cost is the evidence itself. This is the unescapable-state shape (L028) with a new mechanism, and it is the fifth instance found in this run. WHAT IT ARGUES FOR: either stop the wallclock while a task is in reviewing and awaiting envelopes, or provide a blocked -> arbitrating edge for the case where the collected evidence is intact and candidate-bound. Discarding sound evidence to recover from a clock is the most expensive possible answer.
evidence: 
first: 2026-08-22T01:49:27Z
last_confirmed: 2026-08-22T01:49:27Z
invalidate_when: a task blocked out of reviewing can return to arbitration with its collected review evidence intact, or the wallclock budget stops counting time a task spends waiting on reviewers rather than working

## L043 [active] repo
statement: REPEATED OPERATOR PROCEDURES MUST BE SCRIPTS THAT VERIFY THEIR OWN POSTCONDITIONS. r-002 repeatedly raced a supervisor, used zsh loops that ran zero iterations, broke shell quoting, hid stale epochs, leaked ORCHID overrides, stripped modes, carried stale guidance and read stale-round envelopes. The original rebase example also re-pinned Formula/orchid.rb; T030 removed that obsolete candidate policy. A current rebase helper must refuse a live supervisor, dirty or evidence-bearing states, preserve modes, rebase, add an empty Orchid-Handoff operator commit when provenance is needed, and verify target, cleanliness and modes with no candidate pin. Arbitration must verify attempt and candidate bindings; briefs must supersede stale guidance and lead with numbered actions.
evidence: L043 incident count from the frozen r-002 retrospective; T030 supersedes its original formula step, and the resumed run follows fresh epoch reads, visible stderr, reconcile-before-drive and current-envelope checks.
first: 2026-08-23T17:19:14Z
last_confirmed: 2026-08-30T19:11:06Z
invalidate_when: repeatable operator procedures are executed by checked scripts that verify their postconditions, and no repeated run operation depends on a remembered shell sequence

## L044 [active] repo
statement: CONCURRENT EDITS TO ONE ORDERED PROCEDURE CAN EACH LOOK LOCALLY CORRECT AND STILL MAKE THE ASSEMBLED DOCUMENT WRONG. When independent tasks touch numbered arms of PROTOCOL.md, diff-local review does not reveal duplicated closes, superseded steps, broken numbering or sentence splices introduced across branches. Serialize ownership of ordered procedures, or assign one final owner to reread the whole merged procedure against the code rather than reviewing only each diff.
evidence: r-002 T022 and T019 independently left PROTOCOL.md implementing and testing-failure arms with superseded steps, broken numbering, duplication and a sentence splice; T015 repaired the assembled file.
first: 2026-08-30T19:11:06Z
last_confirmed: 2026-08-30T19:11:06Z
invalidate_when: ordered procedural documents have serialized ownership or an assembly check that validates the complete merged sequence after concurrent edits

## L045 [active] repo
statement: A VENDOR WEEKLY-QUOTA EXIT IS CAPACITY, NOT A GENERIC ENGINE FAILURE. If reconcile leaves the engine status ok with no retry horizon, the resolver immediately selects the known-unavailable engine again; repeated undifferentiated failures can then exclude it permanently and charge a healthy task. Parse long-horizon quota signals into explicit unavailable-until state, fail over without consuming task attempts, and preserve the vendor reset time as evidence.
evidence: T015 Claude attempts on 2026-08-30 exited with a weekly limit resetting Sep 1, but reconcile recorded generic failures, left status ok and rate_limited_until 0, and selected Claude again immediately.
first: 2026-08-30T19:11:06Z
last_confirmed: 2026-08-30T19:11:06Z
invalidate_when: vendor long-horizon quota exits are classified as capacity with a retry horizon, trigger failover, and do not charge either the engine-failure exclusion ladder or the task attempt budget

