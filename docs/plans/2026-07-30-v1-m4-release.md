# Orchid v1-m4 — Ecosystem, Packaging & Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** The final v1 milestone: pay down the live-run ledger (operator-writes safety, lease/reap/push guards), promote worktree-read review packs, package the pump as a service, generate the static status page, ship the complete docs suite gated on the 15-minute rehearsal, and build + LIVE-DOGFOOD the Hermes engine adapter and the OpenClaw channel + AgentSkill. Ends with the public-release checklist ready for the operator.

**Architecture:** Per `docs/specs/roadmap.md` (v1-m4 core + conditional adapters + release checklist), `docs/specs/operations.md` (remote three-actor topology, docs bar), `docs/specs/plugins.md` (notify kind), and the m4 ledger accumulated in roadmap.md + the r-001/r-002 journals. Both adapter CLIs are INSTALLED and probed: `hermes --safe-mode -z "<prompt>"` works headless (v0.19.0); `openclaw` (2026.7.1-2) exposes `message/channels/skills/gateway/directory/pairing`.

**Tech Stack:** bash 3.2, jq, git; launchd (macOS) + cron (fallback); harness `tests/run.sh`.

## Global Constraints

- NO Claude/AI references anywhere (commits, files, PR). Post-commit `git log -1 --format=%B`; single-line subject; amend if violated.
- Branch `v1m4-release` from main; never commit to main. No live run is executing against this checkout (r-002 complete; pumps stopped) — but tests still never touch `~/workspace/alislam/*`.
- Tier rules, atomic writes, bash 3.2, parsed-never-sourced, INV statics green. Exit registry: 2/3/5/12/13/14/15 unchanged.
- Every new config key → `lib/config-keys.txt` + `orchid.config.example` in its task.
- Adapters spend REAL quota only in CONTROLLER-executed dogfood steps; suite tests use stubs/DRYRUN.
- OUTWARD-FACING actions (publishing a Homebrew tap, flipping the repo public, posting to awesome-lists, registering the AgentSkill anywhere shared) are prepared in-repo but EXECUTED only by the operator — the plan never performs them.

---

### Task 1: Operator-writes safety — `run accept` commits, config-commit verb, recipe scoping

**Files:** Modify `libexec/orchid-run` (accept), `libexec/orchid-config` (new `commit` subverb), `bin/orchid` (dispatch note), `libexec/orchid-doctor` + `libexec/orchid-status` (recipe wording), `lib/common.sh` (shared temp-worktree commit helper extracted from orchid-plan), `libexec/orchid-plan` (use the helper), `PROTOCOL.md` (COMPLETION + stale-checkout notes), `tests/test_run.sh`, `tests/test_ownership_verbs.sh`, `tests/test_init_doctor.sh`.

**Interfaces (the r-001 journal-loss incident, closed):**
- `orchid_commit_durable <repo> <message> <path...>` in lib/common.sh — the plan-apply temp-worktree + CAS + sync-back transaction, extracted verbatim into a reusable helper; `orchid-plan apply` rewires to it (behavior identical — existing tests are the net).
- `orchid run accept` — after setting `run_status: complete`, COMMITS all durable `.orchid/` state via the helper (message `orchid: run accepted (<run_id>)`); a completed run's record is never uncommitted-only. CAS failure → journal + die (state already set locally; commit retryable).
- `orchid config commit --reason "..."` — the safe operator path for repo-config changes: stages EXACTLY `orchid.config` from the CURRENT checkout into a temp worktree of the integration branch, commits, CAS-advances (never touches the operator's index — the stale-checkout config-commit hazard structurally closed). Journals `intervention`.
- Doctor/status stale-checkout remedy text becomes `git checkout HEAD -- . ':(exclude).orchid'` (never clobbers run state); PROTOCOL's stale-checkout note synced.

- [ ] **Step 1:** RED — accept commits durable state (integ branch gains the commit; roadmap at HEAD says complete); config commit lands only orchid.config from a DIRTY checkout without touching its index; recipe strings updated (greps); plan apply still green via the helper.
- [ ] **Step 2:** Implement. Full suite green. — [ ] **Step 3:** ONE commit: `v1m4: run accept commits state; safe config-commit; scoped stale remedy`.

---

### Task 2: Lease release, pid-0 reaping, push prevention, small fixes

**Files:** Modify `libexec/orchid-run` (release-lease), `libexec/orchid-jobs` (gc `--reap-prepared`), `runners/orchid-launch` + `libexec/orchid-init` (pre-push hook install), `lib/common.sh` (`_cfg_env_name` hyphen), `PROTOCOL.md` (merging section `--engine <name>`; release-lease in COMPLETION), `runners/orchid-pump` (honor released lease), `tests/test_run.sh`, `tests/test_jobs.sh`, `tests/test_launch.sh`, `tests/test_common.sh`, `tests/test_pump.sh`.

**Interfaces:**
- `orchid run release-lease` — clean-session-exit affordance: writes `released: true` into lease.json (epoch-fenced); the pump treats a released lease as stale immediately; `run new`'s freshness guard likewise. PROTOCOL COMPLETION ends with it.
- `orchid jobs gc --reap-prepared [--older-than-s N]` (default 3600): quarantines pid-0 never-launched manifests older than N as `.reason-gc-prepared` (closes the perpetual ghost-row noise; plain gc still skips them).
- Push prevention (defense-in-depth under the m3 policy): task worktrees created by orchid (`orchid-launch`'s prepare path doesn't create them — the ORCHESTRATOR does per PROTOCOL; so the enforcement point is INIT + a documented hook): `orchid init` installs `.git/hooks/pre-push` (guarded: only when no user pre-push exists) that refuses pushes of `task/*` and the integration branch unless `ORCHID_ALLOW_PUSH=1`; PROTOCOL notes it. Config `push_guard=true` default (disable-able).
- `_cfg_env_name` maps `-` → `_` (hyphenated keys get working env overrides); test with `role.code-reviewer` shape.
- PROTOCOL merging section `--engine <plugin-id>` → `<name>` (the 4th drift site).

- [ ] **Step 1:** RED per item (released lease → pump ticks immediately + run new proceeds; reap-prepared quarantines old pid-0 only; pre-push hook blocks task-branch push in a fixture repo, ORCHID_ALLOW_PUSH=1 allows, user's existing hook never overwritten; hyphen env override wins; PROTOCOL greps).
- [ ] **Step 2:** Implement. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: release-lease, prepared-manifest reaping, push guard`.

---

### Task 3: Worktree-read review packs

**Files:** Modify `lib/pack.sh`, `lib/review.sh` (routing awareness), `runners/orchid-launch` (pass engine dir), `libexec/orchid-jobs` (prepare plumbing if needed), `docs/specs/plugins.md`, `lib/config-keys.txt`, `orchid.config.example`, `tests/test_pack.sh`, `tests/test_review_routing.sh`.

**Interfaces (promotes the r-001 prototype into the kernel):**
- `pack_build` review/critique packs: when the RESOLVED engine's manifest declares `workspace_read` AND the diff exceeds `pack_diff_inline_max_bytes` (config, default 262144), the pack contains `diff.stat` (name-status + stat) and `symbols.txt` but NOT `diff.patch`; `pack.json` records `{"name":"diff.patch","omitted":"worktree-read"}`. Inline-only engines keep the full-diff behavior and the existing overflow path (a too-big diff for an inline engine still fails with input_overflow → routing upgrades per m2 rules).
- The launcher passes the resolved plugin dir/manifest facts to pack_build (5th-arg or env — match the hook-point pattern from m3).
- Request document unchanged (worktree path already present); adapters unchanged (codex-review/scoped pattern already reads the worktree).
- plugins.md's input-pack section documents the omission rule.

- [ ] **Step 1:** RED — big-diff fixture + workspace_read engine → pack has diff.stat + symbols, no diff.patch, omission recorded, NO overflow; same diff + inline-only engine → overflow path unchanged; small diff + workspace_read engine → full diff still packed (threshold respected).
- [ ] **Step 2:** Implement. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: worktree-read review packs`.

---

### Task 4: Pump service packaging

**Files:** Create `runners/orchid-service`, `templates/launchd.orchid-pump.plist`, `tests/test_service.sh`. Modify `bin/orchid` (service verb → tier-2 runner delegation like jobs launch), `docs` references later (Task 8), `lib/config-keys.txt` (`pump_interval_s` default 240).

**Interfaces:**
- `orchid service install [--repo <path>] [--interval-s N]` — macOS: renders the plist template (label `com.orchid.pump.<repo-hash>`, ProgramArguments = orchid-pump with ORCHID_REPO + TMPDIR of the repo's parent, StartInterval = pump_interval_s) into `~/Library/LaunchAgents/` and `launchctl load`s it; Linux: installs a crontab line (`*/N * * * *` flooring at 1 min) guarded by a marker comment. `service uninstall` reverses precisely; `service status` reports loaded/next-run + last pump.log lines. Refuses install when the repo isn't initialized; multiple repos = multiple labels.
- The plist/cron runs the PUMP (one-shot, lease-gated) — the design's semi-attended story finally without nohup loops.
- Tests: template rendering (paths/label/interval correct), uninstall removes exactly what install created, status parses; NO real launchctl in the suite (guarded: only rendering + file placement asserted; a `--dry-run` flag prints actions for the launchctl/crontab steps and tests use it).

- [ ] **Step 1:** RED per above. — [ ] **Step 2:** Implement. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: pump service packaging (launchd/cron)`.

---

### Task 5: Static status page

**Files:** Create `libexec/orchid-statuspage` (or extend orchid-status with `--html`; pick: extend `orchid status --html`), `templates/status.html` (self-contained shell), `tests/test_status.sh` (extend). Modify `PROTOCOL.md` (tick step 5 regenerates it), `lib/config-keys.txt` (`status_page` default `runtime/status.html`).

**Interfaces:**
- `orchid status --html` — writes a SELF-CONTAINED static page (inline CSS, no external assets, no JS required) to `runtime/status.html` (config-able path): run header (repo, run_id, run_status, epoch, lease age), task table with status + explain predicates, engines ledger, open blocker questions with their qids, last 10 journal entries. Read-only; atomic write. Prints the path.
- PROTOCOL tick step 5 adds `orchid status --html` after `status --explain` (best-effort; failure never blocks the tick).
- The page is the "check from another room" surface — NOT served; documented as such.

- [ ] **Step 1:** RED — page generated from a fixture run contains task rows/blockers/ledger; valid HTML shell (grep-level checks); atomic (tmp+mv); PROTOCOL lint covers the new invocation.
- [ ] **Step 2:** Implement. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: static status page`.

---

### Task 6: Hermes engine adapter (build)

**Files:** Create `plugins/engines/hermes/plugin.conf`, `plugins/engines/hermes/run`, `docs/engines/hermes.md`, `tests/test_engine_hermes.sh`, `tests/probes/probe-hermes.sh` (+ README row).

**Interfaces:**
- Probed reality: `hermes --safe-mode -z "<prompt>"` = headless one-shot, stdout reply (verified: returns exact requested marker). Adapter branches: `review|critique` → `--safe-mode -z` with the agy-style two-line VERDICT/REASON contract (+ no-tools instruction; findings via `FINDING:` lines for critique); `implement` → `-z` WITHOUT --safe-mode but with `--worktree`?/cwd-scoped flags — RESEARCH the flag set in the task (read `hermes --help` details for toolsets/worktree/yolo; choose the most restrictive write-enabled invocation; document the chosen flags + rationale in docs/engines/hermes.md); adapter commits (F3 pattern); `orchestrate`: NOT offered in v1 (manifest capabilities exclude shell/git → orchestrator-ineligible by capability math; documented).
- `plugin.conf`: `id=orchid/hermes`, `capabilities=structured_text,workspace_read,workspace_write` (+git only if the flag research proves clean git usage; be conservative), `requires_binaries=hermes,jq`, `permissions=` empty.
- Uses `orchid_run_engine_cli` (heartbeat) like the other adapters; DRYRUN short-circuits per op; streams via the m3 idiom.
- `probe-hermes.sh`: real one-shot review-shaped probe + an implement-shaped probe in a scratch dir (guarded, quota-warned, PROBE-RESULT lines).
- `plugins conform plugins/engines/hermes` must pass 7/7 (add to test via DRYRUN).

- [ ] **Step 1:** RED — stub-CLI tests per the codex/claude test patterns (review contract parse, implement commit capture, DRYRUN unions, heartbeat, no-tools line); conform 7/7.
- [ ] **Step 2:** Implement + docs/engines/hermes.md (install/login/flags/gotchas per operations.md's per-engine guide spec). Suite green. — [ ] **Step 3:** ONE commit: `v1m4: hermes engine adapter`.

---

### Task 7: OpenClaw notify channel + inbox hardening + AgentSkill (build)

**Files:** Create `plugins/notify/openclaw/plugin.conf`, `plugins/notify/openclaw/send`, `skills-external/openclaw-orchid/` (the AgentSkill bundle: skill manifest per OpenClaw's `skills` format + README), `docs/engines/openclaw.md` (channel setup guide), `tests/test_notify_channel.sh`. Modify `libexec/orchid-notify` (channel dispatch), `libexec/orchid-answer` (nonce + allowlist + expiry), `lib/manifest.sh` (kind=notify entrypoint=send), `libexec/orchid-plugins` (list/validate/conform cover notify kind minimally), `lib/config-keys.txt` (`notify.channel`, `notify.to`, `answer_allowlist`, `answer_expiry_s` default 86400), `PROTOCOL.md` (blockers section).

**Interfaces (operations.md's three actors, exactly):**
- Notify channel contract (plugins.md): executable `send <question-id> <text>`; the openclaw plugin shells `openclaw message send --channel <configured> --to <notify.to> --text "<qid>: <text> (reply: orchid answer <qid> <choice>)"` — flag names verified against `openclaw message --help` DURING the task (probe first, adapt; record the verified invocation in the plugin header + docs). No repo access, no stdin, launched via the launcher with the hygiene env.
- `orchid notify` — after the existing BLOCKERS+terminal write, when `notify.channel` is configured: launches the channel plugin (tier-2 delegation — notify is tier-1, so it delegates the SEND to `runners/orchid-launch --notify <qid>`-style… simplest compliant shape: notify writes a `runtime/outbox/<qid>` file and the TICK/pump launches the channel send — decide in-task, document; INV-01 must hold: notify itself never spawns).
- `orchid answer` hardening: each question mints a nonce recorded in `runtime/answers/<qid>.question`; `answer` requires `--nonce <n>` when the question carries one EXCEPT from the local terminal (no nonce needed locally — remote surfaces include the nonce in the outbound message); sender allowlist via `answer_allowlist` config consulted when `ORCHID_ANSWER_SENDER` is set (the AgentSkill sets it); questions expire after `answer_expiry_s` (expired → die, journal note).
- AgentSkill bundle: an OpenClaw skill exposing exactly two operations — `orchid status` (read-only, runs `orchid status` in the configured repo, returns the text) and `orchid answer <qid> <choice> --nonce <n>` — with the repo path + allowlist configured skill-side; README documents registration (`openclaw skills ...`, verified during dogfood Task 10) and the security posture. NOT registered anywhere by the plan (operator action).
- PROTOCOL blockers section: outbound send noted; unanswered questions still just block tasks.

- [ ] **Step 1:** RED — stub `openclaw` binary on PATH captures the send invocation (qid+text+flags); notify with channel configured produces the outbox/launch behavior chosen; answer with wrong nonce dies, right nonce ok, expired dies, allowlisted sender ok / unlisted dies; kind=notify validates/lists; INV-01 static still green.
- [ ] **Step 2:** Implement + docs. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: openclaw notify channel, hardened inbox, agentskill bundle`.

---

### Task 8: Docs suite

**Files:** Create/rewrite `README.md`, `docs/quickstart.md`, `docs/quickstart-greenfield.md`, `docs/configuration.md`, `docs/troubleshooting.md`, `docs/research.md`, `docs/engines/codex.md`, `docs/engines/claude.md`, `docs/engines/agy.md`, `docs/engines/codex-review.md` (hermes.md + openclaw.md from T6/T7 — cross-link). Modify `docs/extending/*` (cross-links), `install.sh` (only if the quickstart exposes gaps), `tests/test_docs.sh` (create: link/verb/key lint).

**Interfaces (operations.md's stellar bar):**
- README per roadmap's exact outline: hero + screenshot placeholder markers (real screenshots at release), how-it-works diagram (mermaid), one task's journey, the **who-runs-whom panel** (verbatim design points: engines never spawn engines; the kernel launches and brokers; the orchestrating engine needs exactly one power; every other combo disabled until capsuite passes), why-this-design, **any-engine-any-role matrix** (tested defaults vs capability-gated vs degraded-independence labels, worked `role.*` swap example), install/uninstall, quickstart links, state files/guardrails/operator verbs, Extending (5 extension points + patterns glossary + first-adapter-in-under-an-hour link), FAQ, Research grounding table.
- `docs/research.md`: the roadmap's pillar map with citations **link-checked via live web lookup during the task** (WebSearch/WebFetch each paper; fix titles/venues/years/links to the real published versions; drop any that can't be verified rather than guessing — the spec's rule is relevance and honesty).
- `docs/configuration.md`: generated-faithful complete key table from `lib/config-keys.txt` + defaults + layer + stage; a test asserts every key in config-keys.txt appears.
- Troubleshooting: rate limits, resume, stale locks/lease, blocked tasks, stale checkout, split-brain, pack overflow — each with the REAL remedy verbs (source: our dogfood incidents).
- `tests/test_docs.sh`: every `orchid <verb>` named in README/quickstarts exists; every relative doc link resolves; every config key mentioned exists in config-keys.txt.
- Commit hygiene per roadmap: no personal paths (use `$HOME`/placeholders), no AI trailers.

- [ ] **Step 1:** RED — test_docs.sh lints against the (unwritten) files. — [ ] **Step 2:** Write everything; link-check research.md live. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: docs suite and research grounding`.

---

### Task 9: Hermes LIVE dogfood (CONTROLLER-EXECUTED)

- [ ] Run `tests/probes/probe-hermes.sh` (real quota) — record PROBE-RESULT in dogfood notes.
- [ ] `orchid plugins conform` + `install`-from-repo-dir sanity + `plugins test hermes reviewer` / `hermes implementer` (capsuite, real DRYRUN only).
- [ ] Scratch-repo run with `review.low=hermes,codex-review-scoped,...` and a real task: hermes reviews a real diff (engine-independent vs codex implementer); then swap `role.implementer=hermes,codex` for a second trivial task — hermes implements, adapter commits, verify+review green. Record independence labeling + ledger behavior.
- [ ] Findings → fix loop; notes to `docs/dogfood-notes.md`.

---

### Task 10: OpenClaw LIVE dogfood — the hero demo (CONTROLLER-EXECUTED)

- [ ] Verify the user's OpenClaw setup: `openclaw doctor` / `channels list` / gateway status; verify `openclaw message send` flags against a self-message; adjust the channel plugin if flags drifted (fix loop).
- [ ] Register the AgentSkill bundle into the LOCAL OpenClaw instance (`openclaw skills` flow — local instance only; nothing published).
- [ ] Hero demo on a scratch run: a real blocker question flows orchid → channel plugin → the operator's phone (WhatsApp/Telegram — whichever channel is paired); the operator answers FROM THE PHONE via the AgentSkill (`orchid answer <qid> <choice> --nonce ...`); the next tick consumes it and the task proceeds. Screenshot the phone exchange for the README (operator captures; placeholder markers exist from T8).
- [ ] Also demo `orchid status` from the phone. Record round-trip latency + any friction; findings → fix loop; notes.

---

### Task 11: Homebrew formula + installer polish (prepare only)

**Files:** Create `Formula/orchid.rb` (tap-ready formula: installs bin/libexec/lib/runners/plugins/templates/roles + PROTOCOL to libexec-style prefix, wrapper for bin/orchid resolving ORCHID_ROOT), `docs/install.md`. Modify `install.sh` (idempotence + `--prefix` support if trivial), `README.md` (install section links), `tests/test_install.sh` (formula file lint: ruby syntax via `ruby -c` when ruby present, SKIP otherwise; wrapper resolution test).

**Interfaces:** the formula references the GitHub tarball URL with a `VERSION-PLACEHOLDER` sha; `docs/install.md` documents the operator's release-day steps (create `bilal-/homebrew-orchid`, push formula, pin the curl one-liner) — PREPARED, not executed (outward-facing).

- [ ] **Step 1:** RED (lints). — [ ] **Step 2:** Implement. Suite green. — [ ] **Step 3:** ONE commit: `v1m4: homebrew formula and install docs`.

---

### Task 12: 15-minute rehearsal + spec sync + version + release checklist (CONTROLLER-EXECUTED finale)

- [ ] **Rehearsal (the docs gate):** clean-machine profile (fresh sandbox HOME, PATH with only the real CLIs, fresh clone of the branch): follow `docs/quickstart.md` ONLY, as written, timing it — clone → install → doctor → init → first completed task in under 15 minutes. Any deviation needed = a docs bug → fix loop → re-rehearse. Repeat for the greenfield quickstart (no timer, correctness only).
- [ ] Spec sync task (subagent): roadmap.md v1-m4 SHIPPED + drop-list honesty (Kimi/Perplexity dropped per escape hatch — README/matrix/tutorials contain no mention); kernel/plugins/operations sync for T1-T7 features; `ORCHID_VERSION="1.0.0"` (the release version — the -mN suffix era ends); config-keys/docs lint green. ONE commit: `v1m4: spec sync, version 1.0.0`.
- [ ] Whole-branch review (strongest model) + ONE fix wave + scoped re-review; adjudicate.
- [ ] Dogfood notes commit; PR `v1m4-release` → main.
- [ ] **Release checklist handed to the operator (never auto-executed):** make repo public; create+push the Homebrew tap; pin the curl one-liner; capture/commit real screenshots (hero + phone exchange from T10); GitHub topics; awesome-list PRs; OpenClaw skill-ecosystem submission; CONTRIBUTING.md + community listing section (drafted in T8's README work).

---

## Self-review notes
- Roadmap m4 core mapped: status page (T5), service packaging (T4), Homebrew (T11), docs suite + rehearsal gate (T8, T12); conditional adapters: Hermes (T6+T9), OpenClaw channel + AgentSkill (T7+T10); Kimi/Perplexity dropped honestly (not installed — escape hatch exercised, recorded in T12's sync).
- Ledger paydown mapped: run-accept-commits + config-commit + recipe scoping (T1 — the journal-loss incident), release-lease + pid-0 reap + push guard + hyphen env + PROTOCOL name drift (T2), worktree-read packs (T3 — prototype promoted).
- Live-dogfood separation: build tasks (T6/T7) are suite-testable with stubs; ALL real-quota + user-hardware work is controller-executed (T9/T10/T12) with fix loops.
- Outward-facing discipline: every publish/registration action is prepared as files + operator checklist, never executed by the plan (Global Constraints + T11/T12 wording).
- Cross-task: T1's `orchid_commit_durable` helper consumed by accept/config-commit (and available for future run-new refactor); T2's release-lease consumed by T4's service story + PROTOCOL; T3's pack changes feed T9's hermes review dogfood (big-diff realism); T6/T7 docs land under docs/engines/ consumed by T8's README matrix; version goes straight to 1.0.0 in T12 (no 1.0.0-m4 intermediate — the release checklist is the gate).
